# 上游跟踪台账：llama.cpp PR/Issue 状态 + 集群引擎基线（单一真源）

***

id: upstream-tracker
type: tracker
version: 1.8
status: active（维护中）
date: 2026-09-08（**1.8 更新 2026-09-17（`backend` 断言进门禁 + 后端清减 + 回滚基线补齐）**：用户令「执行遗留 3 + 后端清减 + 补 C 站回滚基线」。**① 门禁 14 → 15 项**：新增断言 `backend`（引擎后端与回滚基线，quick=False 因需三站 ssh）—— 四条判据全落到**二进制**（单站引擎 `ldd` 含 `libggml-hip` / `/opt` 含 `libggml-vulkan` / 三站 ROCm 版本串一致 / 回滚基线在位），**正负双向自证**（真实负向：加入当天即抓到 **C 站回滚基线真缺失**；合成负向 **4/4 专属文案命中**）。**② 补 C 站 `/opt/llama.cpp-9859`**（A→主控→C；包 md5 + 文件清单 hash `ce4d55174452` + MANIFEST **61/61** 三重校验，`RUNPATH=$ORIGIN` 无需 patchelf）。**③ 清减**：三站删 `/opt/llama.cpp-master-d2e206c4`（tar 备份 27M/站 + 可解性自证）、A 删 `~/build/llama.cpp.old`(1.5G)、B 删 v0.2.0 三项(1.5G) 并把 2 个引用已删路径的 `.bak` conf 移出活动配置目录 ⇒ **三站 `/opt` 均恰为规则要求的 2 个目录**（现役 symlink + `9859`）。**④ 一处不可逆损失，如实留档**：A 站 `~/Applications/llama-gfx1151` 残件按「零可执行件 + 零引用」删除后，交叉印证发现它**并非 C 站的同构子集**（A 382 文件 vs C 381）—— 判据强度不足（"判据本身要先自证"**第五次复发，这次造成实际损失**）；影响面已复验为**零引用 ⇒ 零功能影响**，证据文件已追加"事后审计修正"注记。**⑤ 顺带登记**两处既有站间漂移（内核 A `6.17.0-40` vs B/C `6.17.0-23`；LM Studio C `0.4.14+4` 落后）。**⑥ 本轮取证结论回写**：§2.6 增补「**ROCm 版本矩阵**（精确值：studio 引擎 `7.16.26332` / studio python 栈 `7.13.0` / C 站 gfx1151 `7.14.60850` / A 站系统 `6.4.1`）」+「**系统 ROCm 安装面事实**（in-tree 驱动 / ld 污染面 / soname / 工具版本漂移）」+ 明确「**系统 ROCm = 零推理依赖、装它不改变推理版本**」；调研文档升 **v3** 并新增 **§9 ROCm 6.x-vs-7.x 专项**（含官方出处、安装风险、受控对照实验设计）。详见 **§2.7**。1.7 更新 2026-09-17（遗留 1+2 闭环：悬空 `LLAMA_SERVER_PATH` + `infer-load` 注释/注入）**：用户令"执行遗留 1+2"。**改动**：① 删 A/B 站 `.bashrc` / `.profile` 中指向已删目录 `~/llama.cpp-vulkan-b10715/llama-server` 的悬空 `export LLAMA_SERVER_PATH`（A `.bashrc` 156→155 / B 152→151；`.profile` 35→34 / 34→33）；② `ops/station-bin/infer-load` 头部注释改为**实测口径**（引擎真身 = studio 自带 `~/.unsloth/llama.cpp/build/bin/llama-server`，HIP/ROCm b10715 / 92cedc867）+ **移除原 `:212` 的变量注入**；三站部署后 md5 统一 `e474b761198dde596b86ab0edfad19be`（旧件保留 `.bak-20260917-llamaserverpath`，md5 `d57a9021…`）。**判据（自证）**：profile 用 `diff` 断言"恰好删 1 行且被删行 == 备份内容"；端到端在 B 站实跑 `infer-load gpt-oss-20b` → rc=0 / 6s / `READY ✓`，派生进程 exe = studio 自带 HIP 引擎、maps 含 `libggml-hip.so.0`、`--device ROCm0`、**`/proc/<pid>/environ` 已无该变量**（断裂的直接证据），`/v1/models` 200，`infer-unload` 占用归 0。**背景**：该 export 原为 2026-06 前后"把 studio 顶到 Vulkan 引擎"的**故意**手段（见 `spec/d6-agent-standard/unsloth-a-station.md` §509/531），b10715 于 9/8 清除后意图已废，变成"pin 到不存在路径" —— studio 静默落到自带引擎故**无可见故障**，但一旦有人重建该目录，A/B（有 profile export）与 C（无）会**静默分叉**。详见 §2.6 顺带实测①。**同步回写**：`docs/三机推理集群使用手册.md` 引擎真值表、`ops/station-bin/README.md`（引擎说明 + 三站一致性 md5）。1.6 更新 2026-09-17（后端口径订正 + 新建 §2.6 现役后端矩阵）：用户指出「§1 的『对本集群影响』未按现役框架更新」且「C 站 unsloth 用 HIP/ROCm、A/B 似亦有、单站加载疑似默认走 HIP/ROCm」⇒ 经统一管理入口全套取证（`cluster.py versions|models meta` + `station-bin/{infer-load,llama-serve-instance,rpc-nodes}` 决策链 + 三站 `ldd`/`--list-devices`/`/proc/<pid>/maps`/`systemctl cat rpc-server@*`/`/etc/llama-instances/*.env`）确认：**集群是双后端并存，不是"Vulkan 路线"** —— ① **默认单站加载 = HIP/ROCm**（`infer-load` 缺省 `BACKEND=unsloth` → studio 自带引擎 `~/.unsloth/llama.cpp/build/bin/llama-server`，0.3.0-dev build 10715 / commit 92cedc867，`--list-devices` = `ROCm0: AMD Radeon 8060S`，ldd 含 `libggml-hip.so.0`）—— **三站同构**（用户判断正确，此前"纯 Vulkan / 无 ROCm"写法错误）；② **分布式/RPC 路径 = Vulkan**（master `/opt/llama.cpp/llama-server` 0.4.0-dev b1533 / 91f6a6cf + worker `/opt/llama.cpp/ggml-rpc-server`，三站 ldd 均 `libggml-vulkan.so.0` + `libvulkan.so.1`；`nodes.env` **仅 master 站 B 有** = `10.10.10.1:50052 10.10.11.3:50052`）；③ **系统 ROCm 仅 A 站有（6.4.1，`hipcc 6.4.43483`）**，B/C 无系统 ROCm，三站 HIP 面均靠 **studio 自带 bundled ROCm 运行时**（23 个 `libamdhip64/libhipblas/librocm_*`）；④ **C 站 conf 现役引用 HIP 单机引擎** `~/Applications/llama-gfx1151/llama-server`（A 站同目录亦存在）⇒ **非"残留/清理候选"**。**据此订正 §1.1 #28282、§1.2b #25452、§1.4 #27825 与 §1.4 判定、EV-2 / EV-3 的后端口径，并修正 §2.0 / §2.3 / §2.5 三处目录事实漂移**（`~/llama.cpp-vulkan-b10715` 9/8 已删却仍被列为"保留"；`~/App/llama-gfx1151` 实为 `~/Applications/llama-gfx1151`）。1.5 更新 2026-09-16（v2 取证修正）：用户给出 `jcbtc/GLM5.3-Flash-CIRU-STRIX-IU4` 出处后经 hf-mirror 重新取证 —— CIRU StrixLink 确认为"两台 Strix Halo 跑 GLM-5.3-Flash 320B 的 TP=2 部署包"（权重已按 `rank-0`/`rank-1` 切好、各 ~83.25 GiB；vLLM/ROCm 10 + Python 3.14；实测 decode 23.686 t/s），**v1 的"未确认"是假阴性**（真因：HF 在本地网络不可达，而站上 `hf-mirror.com` 可达）；EV-3 由"单站试点"改为**双机部署评估**、EV-6 闭环、新增 **EV-7（HF 通道）**。1.4 更新 2026-09-16：新建 §1.5「待评估事项」动作队列（EV-1…EV-6）+ 分布式路线可行性调研落档**（[调研文档](../../docs/research/2026-09-16_分布式推理路线可行性调研_CIRU-Skulk-多机TP.md)）—— 用户要求把两个待定动作入队，并评估 CIRU/StrixLink 与 Skulk；结论：Skulk 已确认但后端即 llama.cpp（不改上游事实）且与"零自加载/看门狗禁用/唯一管理面"冲突、CIRU 部分确认（GLM-5.3 构建未证实、无多机声明）、**StrixLink 三轮检索未命中 ⇒ 不予评估（需来源）**，另带出 vLLM 多机 TP（需 100GbE）与 AMD 官方 playbook 两条。**1.3 更新 2026-09-16：新增 §1.4「分布式推理相关 PR 集中索引」** —— 核查发现 V4 侧已有层级标注、GLM 侧散落未标注，且两模型均缺 4 条关键条目；集中补录 #26490（V4 `-sm tensor` 主体，Merged 8/24，现役已含）/ #27825（HIP AllReduce，Merged 9/15，现役不含）/ #28360（GLM-5.3 的 RPC issue，报告者第二机为 **GFX1151 同架构**）/ #27311（调度器 UMA ring buffer，活跃）。1.2 更新 2026-09-16: GLM 首选线 #27754 → 45c 且 `mergeable_state=unstable` + 当日有活动 = 9/13 以来首个「接近可合并」信号，仍未合入。1.1 更新 2026-09-10: #26578 实测落地）
depends: [vulkan-version-control-UPGRADE_SOP v1.0, operator-optimization-DESIGN v1.1, model-eval-MODEL-SOURCING v1.0]

> **用途**: 本集群所有「上游活数据」的**单一真值台账**——llama.cpp 相关 PR/Issue 合并状态、引擎基线（各构建目录/现役实例/版本）、以及上游变动对集群的触发动作。**状态变更必须回写本表，禁止散落各处**。
> **背景**: 2026-09-08 交叉核实时发现同一 PR 状态在 4+ 文档重复维护且新旧不一（GLM-5.3-Flash 曾从 3 PR 扩到 4 PR 而 docs 未同步）；现役引擎存在 3 套构建目录且文档记载（b10715/v0.3.0）与实际运行进程（0d18aaa 系）不一致。
> **关闭标准**: 条目失效（PR merged 且集群已升级并验证）→ 移入「已关闭」区；引擎基线随每次 UPGRADE_SOP 升级原子更新本表。
> **核查仪式**: 每次会话触碰本表主题 → 先看 `last-checked`；> 2 天则 fetch 上游核对（fetch 原页，不依赖二手转述）；状态变化只改本表 + 在相关调研文档留一行『状态见 tracker』链接。

***

## 1. llama.cpp 上游 PR/Issue 跟踪

### 1.1 GLM-5.3-Flash（glm5next 架构）——通用

| PR/Issue | 作者 | 内容 | 状态 | 对本集群影响 | 触发动作 |
|---|---|---|---|---|---|
| [#27754](https://github.com/ggml-org/llama.cpp/pull/27754) | danielhanchen（unsloth） | 文本+视觉 **45 commits / 42 files**（9/13→9/16 **+2**）；含 MTP 投机解码（16K ctx 55→77.2 t/s）；两前置 flag：`NVIDIA_TF32_OVERRIDE=0` + `-fa off`；分支 `unslothai:glm5next/upstream` | 🟠 **Open，`mergeable_state=unstable`，9/16 有活动**（9/13 起首个「接近可合并」信号，**仍未合入**） | 首选候选（unsloth 官方，vision+MTP） | **状态转 `clean` 且获 reviewer 批准 → 立即评估选向** |
| [#27752](https://github.com/ggml-org/llama.cpp/pull/27752) | eauchs | 文本 only **11 commits**（未变）；KDA 线性注意力映射 kimi-linear，mHC 沿用 DSV4；`mergeable_state=dirty` | 🔴 Open（9/16 复核，11c / 更新 9/11 / **dirty=有冲突**） | 最简文本线 | 合并 → 评估 |
| [#27773](https://github.com/ggml-org/llama.cpp/pull/27773) | timkhronos | 文本+视觉 **46 commits**；logits 对齐 HF；quantized GGUF 已发布（avar6/GLM-5.3-Flash-BF16-gguf）；`mergeable_state=dirty` | 🔴 Open（9/16 复核，46c / 更新 9/12 / **dirty**） | 视觉备选 | 合并 → 评估 |
| [#27917](https://github.com/ggml-org/llama.cpp/pull/27917) | timkhronos | MTP draft head（NextN），31 commits，依赖 #27773，含 index_share_forward，`--spec-type draft-mtp` 启用；`dirty` | 🔴 Draft（9/16 复核，31c / 更新 9/11） | 仅随 #27773 | — |
| [#27922](https://github.com/ggml-org/llama.cpp/issues/27922) | mirek-vl | **"Feature Request: Support GLM5.3 (flash)"** —— 主线 `unknown architecture 'glm5next'` 佐证（mmproj 加载失败 + UD-Q2_K_XL 文本加载失败双证） | 🔴 Open（9/16 复核，更新 9/11） | 确认主线未合 | 关闭即该来信号 |
| [#28282](https://github.com/ggml-org/llama.cpp/issues/28282) ⭐新 | —— | **CUDA illegal memory access on GLM-5.3-Flash 长 prefill（`-ub 2048`, Blackwell/sm_120）**；作者自述"built from PR #27754 HEAD，只能在 PR 分支上测"；label `bug-unconfirmed`，2 评论 | 🟡 Open（9/16 复核，创建 9/2 / **更新 9/15**） | **对我们无关键**（本集群**无 CUDA 路径**：**分布式 = RPC/Vulkan**、**默认单站 = HIP/ROCm**，均非 CUDA sm_120 —— 口径见 §2.6），但提示 **#27754 线的长 prefill 侧仍有未确认 bug** | 合并后评估时**必须压测长 prefill**，勿只看 decode |
| [#28727](https://github.com/ggml-org/llama.cpp/pull/28727) / [#28729](https://github.com/ggml-org/llama.cpp/pull/28729) ⭐新 | AIalliAI | `glm5next: avoid soft_max gridDim.y overflow in the indexer`（同分支两次提交，45c/42 files，`mergeable=clean`） | ⚫ **Closed 未合并**（9/11）—— 该修复**未进任何线** | GLM indexer 的 soft_max 溢出问题被提出但无人承接 | 观察：若 GLM 线合并后长 prefill 有异常，先查此项 |
| [#28106](https://github.com/ggml-org/llama.cpp/issues/28106) ⭐新 | —— | `glm5next: resolve_fused_ops validates device placement but not tensor …`（op 校验器只查设备位置、不查 tensor —— 正对应我们实测过的 HC `resolve_fused_ops` 警告路径） | 🟡 Open（创建 8/31，本次核查补录） | 解释"HC 融合警告"一类现象的上游视角 | 与 #28144（SOFT_MAX failed）同批观察 |

**结论（2026-09-16 核查）**: glm5next **仍未合入 master**，但出现 **9/13 以来第一个「接近可合并」信号** —— **#27754（unsloth 首选线）45c + `mergeable_state=unstable` + 9/16 当日有活动**（此前三线全 Open 且无活动）；另两线为 46c（#27773，9/12 后未动，`dirty`）与 11c（#27752，`dirty`），#27917 仍 Draft。**新增风险面两条**：① **#28282** 报告在 **#27754 HEAD 上长 prefill（`-ub 2048`）出现 CUDA 非法访存**（`bug-unconfirmed`，非本集群后端，但说明该线**长 prefill 侧未验证**）；② 针对 GLM indexer `soft_max gridDim.y` 溢出的修复 **#28727/#28729 双双 closed 未合并**（无人承接）⇒ 若合并后长 prefill 异常，**先查此项**。**判定**：GLM-5.3-Flash 本地部署**维持等待**，事件驱动前置维持**三变二**不变（① 任一 PR 合并；② RPC crash 已由 #26500 修复，前置满足；③ 可选与 #26610 同窗原子升级），并**新增一条预警触发**：`#27754 转 clean + reviewer 批准` → 即启动评估环境准备（不必等合并）。 [行为快照追溯](file:///d:/RPC/spec/model-eval/FRAMEWORK-SURVEY-2026-09.md)（附录 H.4，原 docs/GLM-5.3-Flash-分布式部署调研并入）｜[模型选型](file:///d:/RPC/spec/model-eval/MODEL-SOURCING-2026-09.md) §6a

> **分布式相关性标注（2026-09-16 补）**：GLM-5.3-Flash 的分布式相关条目**此前散落在 §1.2 的 #28047 与上面结论文字里、表格内无显式标注**（本次核查发现）。现已集中到 **§1.4「分布式推理相关 PR 集中索引」**；其中最直接的一条是 **#28360**（GLM-5.3-Flash 的 RPC issue，**报告者第二台机器为 Strix Halo / GFX1151 —— 与本集群 A/B/C 同架构**）。⇒ 合并任一线后、上分布式前，先按该 issue 条件做前置自测。

### 1.2 DeepSeek V4（deepseek4）算子链——Vulkan 后端

| PR/Release | 内容 | 状态 | 集群影响 |
|---|---|---|---|
| **v0.3.0**（8/25） | DS4 tensor-split（`-sm tensor`）、多序列 rollback 修复 | ✅ release | 基线；⚠️ 不含 Vulkan Lightning Indexer（8/27 才合入） |
| [#26585](https://github.com/ggml-org/llama.cpp/pull/26585) | vulkan tiled transpose 0↔2（V4 indexer 瓶颈 ~43% prefill → 修复） | ✅ **Merged 8/19** | 已在 ≥8/19 master |
| [#26548](https://github.com/ggml-org/llama.cpp/pull/26548) | vulkan DSV4_HC 融合（原案） | ❌ Closed 2026-09-07? | 由 #26578 承接并最终合并 |
| [#26578](https://github.com/ggml-org/llama.cpp/pull/26578) | vulkan DSV4_HC_COMB/PRE/POST 融合（decode 1.50× / prefill 1.12×） | ✅ **Merged 9/7**（0cc4m, 2 commits） | **V4-Flash decode 提速最大单点已入主线**；现役 /opt(d2e206c4 8/31) 缺 → 升级窗口即得 |
| [#27453](https://github.com/ggml-org/llama.cpp/pull/27453) | **vulkan LIGHTNING_INDEXER**（16931/16931 通过） | ✅ **Merged 8/27** | 现役引擎缺；master 已有 |
| [#27970](https://github.com/ggml-org/llama.cpp/pull/27970) | **CUDA+ggml sparse-fa for DSV4/GLM** | ✅ **Merged 9/2** | 新发现；Vulkan 侧影响待评估 |
| [#28133](https://github.com/ggml-org/llama.cpp/pull/28133) | mtmd 支持 DeepSeek-V4-Flash-Vision-Exp | ✅ **Merged 9/2** | 文本模型需先 reconvert #28154 |
| [#28047](https://github.com/ggml-org/llama.cpp/issues/28047) | loganbruns | RPC ≥2 worker 下 V4-Pro/GLM-5.3 确定性 crash（split scheduler 发错 worker） | ✅ **Closed 9/5 — 由 [#26500](https://github.com/ggml-org/llama.cpp/pull/26500) 修复**（"rpc: avoid serializing buffers from other servers"，8/30 merged） | **Pro/GLM-5.3 分布式禁用前置已解除** — 升级引擎（含 #26500）后按闪存纪律实测确认 |
| [#26500](https://github.com/ggml-org/llama.cpp/pull/26500) | rpc: avoid serializing buffers from other servers | ✅ **Merged 8/30** | #28047 修复源；在 ≥8/30 master |
| [#27332](https://github.com/ggml-org/llama.cpp/pull/27332) | theycallmeloki | vulkan density gate（#25356 启发，MMV 路径；+36%@B9, +27%@B16, +21%@B64, B≤8 中性；gfx1151/RDNA3/gfx1013 验证） | ⚠️ Open（9/13 核查，1→2 commits，更新 9/9） | 合并即评估（MoE decode 密度门） | 合并 → 评估 |

**结论（2026-09-10 更新）**: master（≥9/7）已**全齐** V4-Flash Vulkan 算子链（indexer 8/27 + transpose 8/19 + HC 融合 9/7 + sparse-fa 9/2 + vision 9/2 + RPC 修复 8/30）。**✅ #26578 已实测落地（2026-09-10）**：三站引擎升级 master-91f6a6cf（v0.4.0-dev build 1533），A/B 两机 V4-Flash 层分布 decode **9.56→14.02 t/s（+46.7%）**，日志 `resolve_fused_ops` HC 警告清零 → **升级闭环**。详见 metrics-log Phase 6。⚠️ 归位/base 记录：0d18aaa 系（B 站会话级 llama-server）缺全部，禁止在升级前承载 V4-Flash。 [算子调研](file:///d:/RPC/spec/rpc-optimization/research/AMD平台算子层优化与USB4分布式调研.md)｜[选型](file:///d:/RPC/spec/model-eval/MODEL-SOURCING-2026-09.md) §6a

### 1.2b DeepSeek V4-Flash 框架级 PR（open，2026-09-13 补充调研）

> **层级界定**: 本节为「框架级」——架构/转换/分布式形态，区别于 §1.2 的 Vulkan 算子优化。直接关系到三机分布的 `-sm layer` → `-sm tensor` 演进与 V4.1 迁移。**分布式相关条目已集中索引于 §1.4**（含每条"现役引擎是否已含"的判定）。

| PR | 层级 | 内容 | 状态（9/16 核查） | 对集群影响 | 触发动作 |
|---|---|---|---|---|---|
| [#26610](https://github.com/ggml-org/llama.cpp/pull/26610) | RPC/分布式 | RPC 层 add `-sm tensor`（全后端拉通） | 🔴 Open，**非 draft，3 commits / 2 files，更新 2026-09-16（当日有活动）** — `mergeable_state` 本次读数为 `unknown`（GitHub 惰性计算，非状态回退）；**最可合并候选** | **三机 tensor-split 中枢**：若合，配合 #25860/#26490 可把现役 `-sm layer` 演进到 `-sm tensor`（层均衡更细）| 合并 → 与该 PR 同窗评估是否升级（与 GLM 侧引用同一 PR）|
| [#25860](https://github.com/ggml-org/llama.cpp/pull/25860) | 架构/前端 | Deepseek V4: split-mode tensor | ⚪ Draft，2 commits，更新 7/30，停滞 | 设计草案，配套 #26610 | 待其/上游转绿 |
| [#28696](https://github.com/ggml-org/llama.cpp/pull/28696) | 转换 | convert: add DeepSeek V4.1 (DeepseekV41ForCausalLM) | ⚪ **Draft**，2 commits，**更新 9/13**（9/13 台账记 9/12，有触碰） | V4.1 新架构转换入口（9/10 发布 V4.1-Flash）| 观察（V4-Flash 上游重心已移向 V4.1）|
| [#23122](https://github.com/ggml-org/llama.cpp/pull/23122) | 底层算子 | ggml: add DSV4 hyperconnection + KV ops (CPU) | 🔴 Open，非 draft，2 commits，更新 8/1 | CPU 底层 dsv4 算子入主线 → 基线 | 合并即入基线 |
| [#28569](https://github.com/ggml-org/llama.cpp/pull/28569) | 架构 | model: re-enable `-sm tensor` for qwen4exp | 🔴 Open，1 commit，更新 9/8 | 姊妹架构 tensor-split 验证 | 观察 |
| [#25452](https://github.com/ggml-org/llama.cpp/issues/25452) ⭐新 | 运行时/KV | **Eval bug: DSV4-Flash churned-reuse SWA KV-cache exhaustion（crash + stall）** —— SWA（滑窗注意力）cache 在"轮换复用"路径下被耗尽，表现为崩溃与停顿；label `bug-unconfirmed,stale`，**12 评论**（社区在跟） | 🟡 Open（创建 **7/8**，**更新 9/15**） | **与本集群直接相关**：我们跑 V4-Flash 层分布长上下文，SWA cache 行为受 `-c`/ctx 与 slot 复用影响 —— 但**报告者环境为 CUDA 5 卡**，与本集群两条现役后端（分布式 RPC/**Vulkan**、默认单站 **HIP/ROCm**，§2.6）**均不同**，后端相关性未定 | **待自测甄别**：复现条件（长 ctx + 多次轮换复用 + churn）落成一条对照实验；若 Vulkan 侧也复现 → 直接影响分布式长上下文可用性 |

**架构核心历史（已 merged，v0.3.0/v0.4.0 基线，非待跟踪）**: DeepSeek V4 arch 支持（框架主线）、tensor-split、多序列 rollback、sparse-fa、Vision (#28154/#28133)、MTP/NextN、RPC event/async 基建。均已并入现役引擎升级链，状态见 §2.4。

**⚠️ Vulkan 专有缺口（观测，2026-09-13 社区调研口述待核实，不入台账真值）**: 社区反映 V4 关键 dsv4 自定义 op（compressor decode / hyper-connection sinkhorn / lightning indexer / FP8-KV quantize / NextN heads）在主线 **Vulkan 无 kernel**（`feat/v4-port-cuda` 分支注明 "ROCm/Vulkan/Metal-on-AMD fail at first dsv4 op"）——仅 CUDA/Metal 保证。**但本集群实测与台账冲突**: V4-Flash 已在 `/opt` Vulkan 三站层分布跑通（§2.4，decode 14.02 t/s）→ 需实测甄别「哪些 op 缺失/回退 CPU」后再定论，勿采信单一社区说法。分发框架支持（vLLM/SGLang/KTransformers/LM Studio）为参考，未原页核实，不在此列。

### 1.3 其他 gfx1151 算子波（跟踪中，合并即评估）

| PR/Issue | 内容 | 状态 |
|---|---|---|
| ~~[#27332](https://github.com/ggml-org/llama.cpp/pull/27332)~~ | ~~MoE decode 密度门~~（已并入 §1.2 详列） | ⚠️ Open（9/13 核查，1→2 commits，见上） |
| [#27554](https://github.com/ggml-org/llama.cpp/pull/27554) | mmq 大 tile（dense prefill 1.76×） | ❌ Closed；方向由 [#27553](https://github.com/ggml-org/llama.cpp/issues/27553) 承接 |

### 1.4 分布式推理相关 PR（集中索引，2026-09-16 新增）

> **为什么单列**: 用户 2026-09-16 指出「V4-Flash 与 GLM-5.3-Flash 需要关注**分布式推理相关 PR**」。核查结论：**V4 侧此前已有标注**（§1.2b 的「层级」列 + §1.2 的 v0.3.0 / #28047 行），**GLM 侧则散落在 §1.2 的 #28047 与 §1.1 结论文字里、表格内无标注**；且两模型**均缺 4 条关键条目**（本次补录，行首标 ⭐）。本节作为**跨模型的单一入口**：既收模型专属项，也收**两模型共用的分布式基建**。

| PR/Issue | 归属 | 主题 | 内容/证据 | 状态（9/16 核查） | 对本集群影响与触发动作 |
|---|---|---|---|---|---|
| [#26490](https://github.com/ggml-org/llama.cpp/pull/26490) ⭐ | DS V4-Flash | 架构/分布式 | **Deepseek 4: `-sm tensor`**（am17an；8 commits / 7 files；分支 `am17an:dsv4-sm-tensor`） | ✅ **Merged 2026-08-24** | **重要澄清**：V4 的 `-sm tensor` **主体早已入主线，且现役引擎（master-91f6a6cf，9/10 构建）已含** ⇒ "单机/多进程 `-sm tensor`" 已具备；**缺的只是 RPC 多机形态（#26610）**。此前台账仅在 §1.2b"配合 #25860/#26490"里提及、未标 merged |
| [#27825](https://github.com/ggml-org/llama.cpp/pull/27825) ⭐ | 共用基建（**HIP/ROCm 面，三站均有**） | ROCm/通信原语 | **HIP: Enable AllReduce for ROCm**（1 commit / 3 files） | ✅ **Merged 2026-09-15** —— **现役引擎不含**（9/10 构建） | **AllReduce 是张量并行的通信原语**；**默认单站加载即 HIP/ROCm（三站同构，§2.6）** ⇒ 这是"HIP 侧张量并行"的前置。⚠️ **口径订正（2026-09-17）**：**现役分布式/RPC 路径是 Vulkan**（`/opt/llama.cpp`），故本条**只在把分布式也切到 ROCm 时**才成为硬前置（原写法"C 站为 HIP/ROCm"把 HIP 面误缩到 C 站）。触发：**下次引擎升级纳入**（与 #26610 评估同窗） |
| [#26610](https://github.com/ggml-org/llama.cpp/pull/26610) | 两模型共用 | RPC/分布式 | RPC 层 add `-sm tensor`（全后端拉通） | 🔴 Open，非 draft，3 commits / 2 files，**9/16 有活动** | **三机 tensor-split 中枢**：只有它合并，`-sm tensor` 才在 **RPC** 上可用（#26490 只解决非 RPC 路径） | 合并 → 与 GLM/V4 同窗评估升级（详见 §1.2b） |
| [#28047](https://github.com/ggml-org/llama.cpp/issues/28047) / [#26500](https://github.com/ggml-org/llama.cpp/pull/26500) | GLM-5.3（+ V4-Pro）分布式前置 | RPC/修复 | RPC ≥2 worker 下 V4-Pro/**GLM-5.3** 确定性 crash（split scheduler 发错 worker）→ 由 #26500 修复 | ✅ Closed 9/5 / **#26500 Merged 8/30（现役已含）** | **GLM-5.3 分布式禁用的前置已解除** = §1.1 结论「三变二」的第②条。触发：**已满足** |
| [#28360](https://github.com/ggml-org/llama.cpp/issues/28360) ⭐ | **GLM-5.3** | RPC/运行时 | **Eval bug: RPC Issue in add GLM-5.3-Flash (GLM5-Next) support - #27773**；报告者两机 = **4×9060XT(GFX1200) + Strix-Halo(GFX1151)**，走 RPC | 🟡 **Open**（创建/更新 9/4，1 评论，`bug-unconfirmed`） | **最直接的一条**：报告者第二台就是 **gfx1151（与本集群同架构）**，且明确挂在 #27773 线上 ⇒ GLM-5.3 走 RPC 存在**已知未确认问题**。触发：**合并任一线后、上分布式前，先按该 issue 条件做前置自测** |
| [#27311](https://github.com/ggml-org/llama.cpp/pull/27311) ⭐ | 共用基建（**UMA**） | 调度器 | **Scheduler UMA ring buffer**（+ sanitizer 加固与调度修复；**49 评论、9/16 仍活跃**） | 🟡 Open（8/18 创建，**更新 9/16**） | 本集群是 **UMA（Strix Halo 统一内存）** 且跑 RPC ⇒ 调度器输入张量 ring buffer 与我们的形态**直接相关**（可能关联"长跑稳定 / 内存行为"）。触发：合并 → 评估 |
| [#26384](https://github.com/ggml-org/llama.cpp/issues/26384) | DS V4-Flash（分布式健壮性） | RPC/健壮性 | deepseek_v4 over RPC（2 节点，Thunderbolt）：输出退化为 `====` → worker 侧图失败**直接杀掉整个 server**，原文注明 **"RPC has no error recovery"** | ⚫ Closed（`stale`；7/31 创建、9/14 更新） | **通用风险认知**：RPC 无错误恢复 ⇒ 三机形态下 **worker 故障会连带 head 全挂**（与本集群 2026-09-08 两次 kernel panic 经验同族）。触发：已记入风险认知，非动作项 |
| [#27964](https://github.com/ggml-org/llama.cpp/issues/27964) | `-sm tensor` 风险观察 | 运行时 | Qwen3.8-Flash-Next：`--split-mode tensor` 在 CUDA 双卡上 **abort（`SPLIT_AXIS_UNKNOWN`）** | 🟡 Open（8/29 创建，更新 9/16） | `-sm tensor` 在**其他架构上仍有失败案例**（姊妹架构 qwen4exp / qwen3.8）⇒ 我们启用前**必须先做形态验证**。触发：纳入 `-sm tensor` 验证清单 |

**低相关存档**：[#26906](https://github.com/ggml-org/llama.cpp/issues/26906)（"model stop answering after some hours of work"，`stale`）—— 报告环境为 **CPU 双机、无 GPU**，与本集群形态不符，仅存档不动作。

**§1.4 判定（2026-09-16）**：分布式路径上**真正的两个闸门**是 ① **#26610**（RPC `-sm tensor`，仍 Open，但 9/16 有活动）与 ② **GLM 侧的 #28360**（同架构报告者的 RPC 问题，未确认）。V4 侧的 `-sm tensor` 主体（#26490）与 RPC crash 修复（#26500）**均已在现役引擎内**；HIP 面的 AllReduce（#27825，9/15 合并）**尚未入现役**（现役 `/opt` 面 = 9/10 构建）；但**现役分布式路径是 Vulkan、只有默认单站加载才是 HIP/ROCm（§2.6）** ⇒ 它只在"把分布式切到 ROCm 张量并行"时才成为前置，随下次升级纳入即可。

### 1.5 待评估事项（上游驱动的动作队列，2026-09-16 建）

> **用途**: 把"上游状态变化 ⇒ 我们要做什么"从 §1.1–§1.4 的散落语句收成**一张队列表**；每条含触发条件 / 成本 / 依赖 / 出处。**评估结论回写到本表**，不在此处下结论。

| # | 事项 | 类型 | 触发条件 / 现状 | 成本 | 依赖 | 出处 |
|---|---|---|---|---|---|---|
| **EV-1** | **#28360 前置自测** —— GLM-5.3 上分布式**之前**，按该 issue 条件（同架构报告者：**GFX1151 + RPC**）做复现对照 | 上游风险 | **待做**（GLM 任一线合并后即成为前置）；#28360 仍 Open | 中（需可跑的 glm5next 引擎） | 需先有可跑的 glm5next 引擎 | §1.4 |
| **EV-2** | **#27825（HIP AllReduce）纳入下次引擎升级清单** | 升级项 | **待列入**；已 Merged 9/15，**现役不含**。**注（2026-09-17）**：现役 **HIP/ROCm 面 = 默认单站加载**（三站同构，§2.6），**分布式面仍是 Vulkan** ⇒ 本条**仅在分布式切 ROCm 张量并行时**才是硬前置，否则属"备而不急" | 小（随 UPGRADE_SOP 一次走） | 下次引擎升级窗口 | §1.4 |
| **EV-3** | **CIRU StrixLink 双机部署评估**（`jcbtc/GLM5.3-Flash-CIRU-STRIX-IU4`）—— **GLM-5.3-Flash 320B、TP=2、走 vLLM/ROCm 10 绕开 glm5next 上游依赖** | 引擎换路线 | ✅ **已取证（E1，2026-09-16）**：双机 USB4 + gfx1151 + 各 128 GiB；权重**已按 `rank-0`/`rank-1` 切好**（各 ~**83.25 GiB**）；`INSTALL-RUNTIME.sh` 自包含；实测 **decode 23.686 t/s / 402.5 prompt t/s**（64K+NHI+DFlash2）；**⇒ 不可单机跑（必须两机）**。**现役 ROCm 面（2026-09-17 实测，§2.6）**：三站 HIP 面靠 studio 自带 bundled ROCm 运行时，**系统 ROCm 仅 A 站有（6.4.1）** ⇒ 与 CIRU 要求的 **ROCm 10.0.0** 属**版本代差**而非"从零引入"（**Python 3.14.3 + TileLang 0.1.10 仍是净新增**） | 中高（两机新栈 + 磁盘 + 治理接入） | ① 三站 NVMe 余量；② 内核 **NHI/USB4STREAM**（否则退回 RCCL ≈341 µs）；③ ADR-0004 立项 | [调研 §2](../../docs/research/2026-09-16_分布式推理路线可行性调研_CIRU-Skulk-多机TP.md) |
| **EV-4** | **Skulk 引入可行性** —— 多机 fabric（AMD Linux = `skulk-llama-server-vulkan`；引擎可注入 `SKULK_LLAMA_SERVER_BIN`） | 框架引入 | **待决策**：先答"**谁管生命周期**"（与**零自加载 / load-gate 内存闸门 / C 站看门狗禁用 / ADR-0004 唯一管理面**的冲突）。**注**：其后端**就是 llama.cpp** ⇒ 不改上游事实，**对 GLM-5.3 的性价比低于 EV-3** | 小（决策）→ 中（试点） | ADR-0004 D3 裁决 | [调研 §4](../../docs/research/2026-09-16_分布式推理路线可行性调研_CIRU-Skulk-多机TP.md) |
| **EV-5** | **100GbE RDMA 硬件评估** | 硬件决策 | **待决策**；**EV-3 给出旁路**：CIRU 已证 **USB4 上可做 TP=2**（NHI 硬件环 + DMA-BUF，组件门 110 µs vs RCCL socket 341 µs）⇒ "TP 必须 100GbE"**非普适律** | 大（硬件） | 预算决策 | [调研 §5.1](../../docs/research/2026-09-16_分布式推理路线可行性调研_CIRU-Skulk-多机TP.md) |
| **EV-6** | ~~"StrixLink" 取证~~ | 取证 | ✅ **已闭环（2026-09-16）**：**它不是独立框架**，而是 CIRU 部署包的组成部分 —— HF tag **`ciru-strixlink`** + 包内 `tools/ciru-strixlink-0.2.0 … 0.3.3-linux-amd64.tar.gz`，职责 = **配置/检查/准备两机连接**（含 Launch 页）。**v1 判"未确认"是假阴性** —— 真因见 EV-7 | — | 已闭环 | [调研 §3](../../docs/research/2026-09-16_分布式推理路线可行性调研_CIRU-Skulk-多机TP.md) |
| **EV-7** | **HF 取证/下载通道**（运维事实） | 通道 | **主控与 B 站直连 `huggingface.co` 超时**（`WinError 10060` / `http=000`）；**站上 `hf-mirror.com` 可达（HTTP 200）**，本次 166.72 GiB 仓库的元数据/文件树/README 全部经此取得 | — | ✅ 已确认。**纪律：判"不存在"前必须先证"通道可达"** | [调研 §3.2](../../docs/research/2026-09-16_分布式推理路线可行性调研_CIRU-Skulk-多机TP.md) |

**EV 队列（v2 修订）**：**EV-6 已闭环、EV-7 已确认**。优先级：**EV-1/EV-2 随上游走（零成本）→ EV-3 CIRU 双机部署（收益明确：当场得到可用的 320B GLM-5.3-Flash）→ 其前置「内核 NHI 勘查」→ EV-4 Skulk 治理决策 → EV-5 硬件预算**。详见[调研 §6](../../docs/research/2026-09-16_分布式推理路线可行性调研_CIRU-Skulk-多机TP.md)。

***

## 2. 集群引擎基线（2026-09-08 实测）

> ✅ **2026-09-08 归位完成**：三站分布式链路已切回 `/opt/llama.cpp`（v0.3.0, rpc v6.0.0），MANIFEST I3 首次成立，巡检全绿。见下方「2.4 归位后状态（9/8 17:00 收口）」。
> 🧩 **绕 /opt 成因（用户 09-08 补充）**：当初 `/opt` 写入需 sudo 权限，测试新框架时 LLM 建议绕过 /opt 改用用户目录 → 该建议被沿用到运行链路，形成「散置构建 = 现役」格局。回归受控时必须照顾此历史，不能假设 `/opt` 一直是运行入口。

### 2.0 A/B 站运行链路实测对照（2026-09-08）

| 链路 | B 站（GTR-Pro） | A 站（NEX） | 引擎真身 | 版本 |
|---|---|---|---|---|
| 管理/单站推理 | 18081 已停（现 qwen3.8 在 18081 为会话拉起） | **8080 = unsloth studio run gpt-oss-120b-MXFP4**（`~/.unsloth/studio/unsloth_studio/bin/python ... -c 131072`；**⚠️ 2026-09-17 订正：本行 9/8 记载的 `--device Vulkan0` 已过期 —— 现为 `--device ROCm0`**，见 §2.6） | `~/.unsloth/llama.cpp`（unsloth 自带框架目录，**用户提示 A/B 站 unsloth 已写死此路径**） | 0.3.0-dev 10715 / ggml 0.22.0 |
| RPC 分布式 | **18081 = `~/llama.cpp/build/bin/llama-server`**（qwen3.8 Q8 + --rpc A/C） | **50052 = `~/llama.cpp/build/bin/rpc-server`** | `~/llama.cpp/build`（绕 /opt） | 0d18aaa / ggml 0.13 系（源码 HEAD 05-26） |

**要点**：① 单站链路由 unsloth 管理（含自有引擎路径），**回归不得破坏 unsloth 引擎**；② 分布式链路（llama-server/rpc-server）的现役是绕 /opt 的 `~/llama.cpp/build`——**这是需要归位回 `/opt` 受控版本的部分**；③ A 站另有 defunct llama-server 僵尸进程（389114），清理时一并处置。

### 2.4 归位后状态（2026-09-08 收口；**2026-09-10 升引擎至 91f6a6cf**）

| 项 | 值 |
|---|---|
| 三站 `/opt/llama.cpp` | symlink → **`llama.cpp-master-91f6a6cf`**（v0.4.0-dev build 1533, commit 91f6a6cf；旧 d2e206c4 保留待清理） |
| MANIFEST | **I3 成立**：三站各 79 条 [md5]，`md5sum -c` 全 `成功`；commit/version/rpc_protocol 三字段统一 |
| rpc_protocol | **v6.0.0**（/opt master rpc-server 启动日志实测；旧 9859 为 v4.0.1） |
| 分布式链路 | B `/opt/llama.cpp/llama-server` + `--rpc 10.10.10.x:50052` → A/C `ggml-rpc-server`（v6.0.0, Vulkan0 GFX1151, `-c` cache）。**2026-09-17 复测**：三站这两个二进制 ldd 均为 `libggml-vulkan.so.0` + `libvulkan.so.1`；worker 单元 `ExecStart=/opt/llama.cpp/ggml-rpc-server -H <环网IP> -p 50052 -c`（A=10.10.10.1 / C=10.10.11.3）；`nodes.env`（**仅 master 站 B 有**）= `10.10.10.1:50052 10.10.11.3:50052` —— 见 §2.6 |
| 冒烟 | #26578 实测（9/10）：A/B 两机 V4-Flash decode 9.56→14.02 t/s；HC `resolve_fused_ops` 警告清零 |
| 巡检 | `ops/check_llama_version.py --deep`：三站指纹一致 + 文件集 md5 完全一致 ✅ |
| 已知观察 | ① RUNPATH：新 cmake 产物硬编码 B 站 build 路径 → A/C 必须 patchelf `$ORIGIN`（已修）；② 旧 d2e206c4 目录保留作回滚；③ C 站内核实测 7.0.0-31 观察项（§手册10.1） |

### 2.5 回归后职责边界（更新）

- **分布式链路（llama-server / ggml-rpc-server）** → 一律走 `/opt/llama.cpp`（UPGRADE_SOP 管理）；**禁止**再手动用 `~/llama.cpp/build` 起服务
- **单站管理（unsloth studio）** → 保持 `~/.unsloth/llama.cpp`（写死路径约束，不触碰）
- **散置构建（2026-09-17 清减后现状）**：
  - `~/llama.cpp/build`（0d18aaa / Vulkan）→ **保留**：B/C 站 conf 显式引用（单机线），A 站零引用但作为唯一 Vulkan 单机回退暂留
  - `~/llama.cpp`（0d18aaa 源码树）→ 保留为对照
  - `~/Applications/llama-gfx1151`（HIP/ROCm 7.14）→ **C 站保留**（`BACKEND=llama-single` 现役引用）；**A 站残件已于本日删除**（见 §2.7「一处不可逆损失」）
  - `~/src/llama.cpp-master-91f6a6cf` + `~/build/llama-master-91f6a6cf`（B 站）→ **保留**（UPGRADE_SOP 的唯一构建源）
  - ~~`~/llama.cpp-vulkan-b10715`~~ → **9/8 已清除**；其悬空 `LLAMA_SERVER_PATH` 注入（A/B profile + `infer-load`）**已于本日清理**（§2.6 顺带实测①）
  - ~~`~/build/llama.cpp.old`~~（A）、~~v0.2.0 源码/构建/归档~~（B）、~~`/opt/llama.cpp-master-d2e206c4`~~（三站）→ **本日已清**（§2.7）

### 2.1 B 站（GTR-Pro）现役实例（PID 612738, port 18081, qwen3.8-27b Q8）

| 项 | 实测值 |
|---|---|
| 进程命令 | `/home/scott-lau/llama.cpp/build/bin/llama-server` |
| 拉起方式 | SSH 会话 scope（cgroup `session-8306.scope`，PPID=1 已脱管）→ **非 systemd 服务，watchdog 可见** |
| maps 加载库 | `llama.cpp/build/bin/libggml-*.so.0.13.0`、`libllama.so.0.0.1`（mtime 9/6 重建） |
| 源码 HEAD | `~/llama.cpp` = `0d18aaa`（2026-05-26，`ci: do not allocate ccache ...`） |
| deepseek4 / glm5next | 均 **0**（现役构建不含）→ V4/GLM5 不可加载 |
| 备注 | build/bin mtime 9/6 16:11-16:12（最近重编过）但版本号仍是 ggml 0.13 系——**疑源码树与构建产物版本号未同步**，纳入升级时核查 |

### 2.2 B 站 /opt 正式版本目录（UPGRADE_SOP 管理）

| 目录 | 版本 | 部署时间 | 含 lightning? | 使用方 |
|---|---|---|---|---|
| `/opt/llama.cpp` → `llama.cpp-master-d2e206c4` | **v0.3.0-dev（libllama.so.0.3.0，ggml 0.22.0，deepseek4 字符串 91 处）** | 8/31 | 待核 | **无进程打开（lsof 空）** |
| `/opt/llama.cpp-9859` | build 9859（ggml 0.15.3） | 7/2 预存/8/27 归档 | 否 | 无 |
| `/opt/llama.cpp-v0.2.0` | v0.2.0 | 8/28 | 否 | 无 |

> **现状注记（2026-09-17）**：上表是 **9/8 的实况**。此后：`/opt/llama.cpp` symlink 已于 **9/10 切至 `llama.cpp-master-91f6a6cf`**（§2.4）；`/opt/llama.cpp-v0.2.0` 已于 9/8 清除；**`llama.cpp-master-d2e206c4` 已于本日（9/17）删除**（tar 备份在 `/data/backup/llama-cleanup-20260917/`）⇒ **三站 `/opt` 现均恰为规则要求的 2 个目录**（现役 symlink + `9859` 回滚基线），且 `9859` 已补齐到 C 站。详见 §2.7。

### 2.3 用户目录散置构建（测试残留，勿混用）

| 目录 | 说明 | 处置 |
|---|---|---|
| ~~`~/llama.cpp-vulkan-b10715`~~ | **已于 2026-09-08 清除**（§5 变更日志「llama 残留后端清除完成」）；**2026-09-17 三站实测：目录不存在** | ✅ 已清；**遗留已闭环（2026-09-17）**：A/B 站 `.bashrc` + `.profile` 的悬空 `export LLAMA_SERVER_PATH=<该路径>` **已删除**，`infer-load` 的注入与头部注释亦已修正（详见 §2.6 顺带实测①） |
| `~/.unsloth/llama.cpp/build` | unsloth 构建（**2026-09-17 实测：0.3.0-dev build 10715 / commit 92cedc867，Clang 23，HIP/ROCm**——即**默认单站加载的生效引擎**，见 §2.6） | ✅ **现役**（`infer-load` 缺省后端 unsloth 走它）；复验源之一 |
| `~/Applications/llama-gfx1151` | **HIP/gfx1151 构建**（libggml-hip.so + hipblaslt + libamdhip64 全套；`--list-devices` = `ROCm0`，自带 ROCm **7.14.60850**）——**⚠️ 2026-09-17 订正：不是"残留"**：C 站 `gpt-oss-120b.env` / `qwen3.8-flash-next.env` 以 `BACKEND=llama-single` + `LLAMA_SERVER_BIN=<此路径>` **现役引用**；且「HIP llama 后端已死」的旧判定已被推翻 —— **三站默认单站加载就是 HIP/ROCm**（§2.6） | **C 站 ✅ 保留**（现役单机引擎之一）；**A 站那份已于本日删除** —— 它是"零可执行件 + 零引用"的残件，**但事后核对发现它并非 C 站的同构子集（多 2 个未知文件）**，见 §2.7「一处不可逆损失」 |
| `~/llama-distributed/` | bench/crash_drill/smoke 脚本 + logs | 保留（RPC 实测产物） |
| ~~`~/src/llama.cpp-0.2.0` `~/build/llama-v0.2.0` `~/dist/llama-v0.2.0.tar.gz`~~ | v0.2.0 源码/构建/归档 | ✅ **已清（2026-09-17，B 站）** —— v0.2.0 = 上游 tag 可得，且 `/opt/llama.cpp-v0.2.0` 9/8 已清除 ⇒ 无对应产物；见 §2.7 |
| `~/build/llama.cpp.old` | A 站旧源码树 + 构建（git `5d3a4a7`） | ✅ **已清（2026-09-17，A 站）** —— 其 `build/bin/llama-server` 已损坏（缺 `libmtmd.so.0`）|

**行动（P0）**（**✅ 已闭环 2026-09-10，见 §2.4 / §5**：实际以 **master `91f6a6cf`** 为源完成三站原子升级，V4-Flash decode 9.56→14.02 t/s；**非**文中起点 `~/llama.cpp-vulkan-b10715` —— 该目录 **2026-09-08 已清除**，§2.3。原文保留如下）: 按 [UPGRADE_SOP](file:///d:/RPC/spec/vulkan-version-control/UPGRADE_SOP.md) 以 `~/llama.cpp-vulkan-b10715`（unsloth v0.3.0）或 `/opt/llama.cpp`（v0.3.0）为源，确认 libllama 含 deepseek4 + libggml-vulkan 含 lightning indexer 后做一次两站原子升级 → V4-Flash 解锁。升级前记录现役 0d18aaa 基准（18081 qwen3.8 可跑）。

### 2.6 现役 GPU 后端矩阵（2026-09-17 实测，统一管理入口取证）

> **为什么建**: 用户指出 §1 的「对本集群影响」**未按现役框架更新** —— 多处按"本集群 = Vulkan 路线 / 无 ROCm"书写，与实测不符；并具体质疑「C 站 unsloth 用 HIP/ROCm、A/B 似亦有、单站加载疑似默认走 HIP/ROCm」。本节作为**后端口径的单一真源**：§1 各条「对本集群影响」一律按本节判定。
> **取证方式（统一管理入口）**: `ops/cluster.py versions|models meta`（引擎/版本列）+ `ops/station-bin/{infer-load,llama-serve-instance,rpc-nodes}`（加载决策链源码）+ 三站 `ldd <bin>`、`<bin> --list-devices`、`/proc/<pid>/maps`、`systemctl cat rpc-server@*`、`/etc/llama-instances/*.env` 与 `~/.unsloth/run-*.log`（落到**二进制级**）；ROCm 面另取 `ls -1 <studio>/build/bin/ | grep libamdhip64`、`dpkg -l amdgpu*`、`/etc/ld.so.conf.d/`、`ldconfig -p`、`lsmod/sys module amdgpu`、`/lib/modules/*/updates/dkms/`、`rocm_sdk_*` 包版本。

| 现役路径 | 管理入口决定者 | 二进制 | GPU 后端 | 版本 | A（NEX） | B（GTR-Pro，master） | C（seaviv） |
|---|---|---|---|---|---|---|---|
| **① 默认单站加载**（`infer-load` 无 `--backend` ⇒ 缺省 `BACKEND=unsloth`，仅 `all-minilm*` 走 llama-single） | `infer-load` §6a → `unsloth studio run` | `~/.unsloth/llama.cpp/build/bin/llama-server`（studio 自带；`~/.unsloth/llama.cpp/llama-server` 是指向它的软链） | ✅ **HIP/ROCm**（`libggml-hip.so.0` + `libhipblas.so.3` + `libamdhip64.so.7`；`--list-devices` = `ROCm0: AMD Radeon 8060S (120000 MiB)`） | **0.3.0-dev build 10715 / commit 92cedc867**（Clang 23，unsloth 分发） | ✅ | ✅ | ✅ |
| **② 分布式 / RPC master** | `infer-load` §6b → `llama-server@<alias>` → `llama-serve-instance`（`LLAMA_SERVER_BIN` 缺省 ⇒ `/opt/llama.cpp/llama-server`） | `/opt/llama.cpp/llama-server` | **Vulkan**（`libggml-vulkan.so.0` + `libvulkan.so.1`；`Vulkan0: RADV GFX1151`） | **0.4.0-dev build 1533 / commit 91f6a6cf**（rpc_protocol v6.0.0） | ✅ | ✅ | ✅ |
| **③ 分布式 / RPC worker** | `rpc-nodes --start` → `rpc-server@<alias>.service` | `/opt/llama.cpp/ggml-rpc-server` | **Vulkan**（同上 ldd） | 同上（同受控路径） | ✅ 单元 `-H 10.10.10.1` | 未装（master 站） | ✅ 单元 `-H 10.10.11.3` |
| **④ 单机线**（conf `BACKEND=llama-single`/`llama-rpc` 时，由 `LLAMA_SERVER_BIN` 定引擎） | `/etc/llama-instances/*.env` → `llama-serve-instance` | 由 conf 决定；无 `LLAMA_SERVER_BIN` 时取 `/opt/llama.cpp/llama-server` | 由 conf 决定 | — | 无 conf 指定 ⇒ **`/opt` 缺省（Vulkan）** | `~/llama.cpp/build/bin/llama-server`（**Vulkan**，`0d18aaa`）—— `davidau-q38-27b-q4k.env` / `qwen3.8-27b-mtp.env` | `~/llama.cpp/build/bin`（**Vulkan**）+ `~/Applications/llama-gfx1151/llama-server`（✅ **HIP**，`ROCm0`）—— 后者被 `gpt-oss-120b.env` / `qwen3.8-flash-next.env` 引用 |
| **⑤ 系统级 ROCm** | — | `/opt/rocm*`（A 站为 `/opt/rocm` → `/etc/alternatives/rocm` → `/opt/rocm-6.4.1`，alternatives 管理） | — | **6.4.1**（`hipcc 6.4.43483-a187df25c`；`rocminfo` → `gfx1151`） | ✅ **唯一有** ⚠️ **但不在任何推理路径上** | ❌ 无（`rocminfo`/`hipcc` 均未安装，无 `/opt/rocm*`） | ❌ 无 |
| **⑥ HIP 面运行时来源**（B/C） | studio 分发内自带 | `~/.unsloth/llama.cpp/build/bin/libamdhip64.so.7.16.26332` 等 | bundled ROCm（**非系统 ROCm**） | 23 个 `lib(amdhip64\|hipblas\|hipblaslt\|rocm_*)` | ✅（另有系统 6.4.1） | ✅ bundled | ✅ bundled |

**ROCm 版本矩阵（2026-09-17 实测精确值 —— 回答"7.x 退化是否命中现役路径"）**

| 位置 | 版本 | **是否在推理路径上** |
|---|---|---|
| `~/.unsloth/llama.cpp/build/bin/libamdhip64.so.7.16.26332` | **7.16.26332** | ✅ **默认单站加载（现役主路径）** |
| `~/.unsloth/studio/.../site-packages/`（`rocm_sdk_core` + torch + triton） | **7.13.0**（torch `2.11.0+rocm7.13.0`、triton `3.6.0+rocm7.13.0`） | studio 的 python 栈 |
| `~/Applications/llama-gfx1151/`（C 完整件 / A 残件已删） | **7.14.60850**（`libamdhip64.so.7.14.60850-d34cbb6409`） | ✅ C 站 HIP 单机线（2 个 conf） |
| A 站系统 `/opt/rocm-6.4.1` | **6.4.1** | ❌ **零推理依赖**（`/opt/rocm` 引用反查 = 0；`~/Applications/llama-gfx1151` 的 ldd 对 `/opt/rocm` 依赖数 = 0） |

> **由此得出两条关键定性**：① **没有任何现役推理路径跑在 ROCm 6.x 上** —— 系统 ROCm 在本集群只承担**构建工具链（`hipcc`）+ 诊断（`rocminfo`）**，**装它不会改变推理用的 ROCm 版本**；② **"ROCm 7.x 在 gfx1151 性能退化"的直觉有官方佐证，但只覆盖 7.2.1**（官方 Ryzen 已知问题页明列），而**现役是 7.13 / 7.14 / 7.16 —— 不能外推**。完整调研（官方出处 + 安装风险 + 建议）落档在[调研文档 §9](../../docs/research/2026-09-16_分布式推理路线可行性调研_CIRU-Skulk-多机TP.md)。

**系统 ROCm 的安装面事实（A 站实测，供未来"要不要给 B/C 装"决策用）**

| 项 | 实测 | 为什么重要 |
|---|---|---|
| 内核驱动形态 | `amdgpu` 为 **in-tree**、`/lib/modules/<k>/updates/dkms/` **空**（**未装 `amdgpu-dkms`**） | Ryzen 官方指引**明确要求 `--no-dkms`**；`amdgpu-dkms` 会与 in-tree 驱动冲突并破坏 Strix Halo 上的 ROCm ⇒ **这是必须保持的状态** |
| ld 污染面 | `ld.so.conf.d` 仅 `10-rocm-opencl.conf`（只含 `/opt/rocm-6.4.1/lib`）；**`LD_LIBRARY_PATH` 空**；profile 无 rocm 痕迹 | 官方 post-install 会写 `rocm.conf` 并建议导出 `LD_LIBRARY_PATH` ⇒ A 站当前隔离是**干净**的 |
| **soname** | 系统 ROCm 6.4.x = **`libamdhip64.so.6`**；7.x = **`.so.7`** —— 而 studio bundled 恰是 **`.so.7`** | 装 **6.x 与 bundled 天然错开**（A 站即"系统 so.6 + bundled 7.16 共存且 HIP 推理实测正常"）；装 **7.x 则与 bundled 同名**，存在遮蔽面 |
| `amdgpu-install` 工具版本 | A `6.4.60401` / B `6.3.60303` / **C `30.30.1.0.30300100`** | **三站工具版本漂移**；C 的那个是 ROCm 7.x 时代遗留工具 ⇒ 若未来在 C 装，**必须显式按版本 pin**，否则默认装到 7.x |

**判定（一句话）**：**本集群是「双后端并存」，不是「Vulkan 路线」** —— **默认单站加载（`infer-load` 缺省 `unsloth`）＝ HIP/ROCm（三站同构）**；**分布式/RPC 路径（master + worker，均走 `/opt/llama.cpp`）＝ Vulkan**。⇒ 凡"本集群 = Vulkan / 无 ROCm"的写法**均属过期口径**（本日订正 §1.1 / §1.2b / §1.4 共 4 处 + §2.0 / §2.3 / §2.5 目录事实 3 处）。

**顺带实测（三条，三站一致）**：
1. **`LLAMA_SERVER_PATH` 悬空**：A/B 站 `.bashrc` + `.profile` 与 `infer-load` 缺省值均指向 `~/llama.cpp-vulkan-b10715/llama-server`（**该目录 9/8 已清除**）。studio 的引擎解析顺序**第 1 位就是它**（`~/.unsloth/studio/.../studio/backend/core/inference/llama_cpp.py:7634`），因 `os.path.exists=False` 自动落到第 3 位 `~/.unsloth/llama.cpp/llama-server` ⇒ **当前无实际影响**；但 `infer-load:3` 注释「引擎 = `LLAMA_SERVER_PATH` 指向的 llama-server」**与实测不符**（生效的是 studio 自带引擎）。**C 站 profile 无此 export**。⇒ 属"悬空配置 + 注释漂移"。
   **✅ 已清理（2026-09-17，遗留 1+2 闭环）**：① 删 A/B 站 `.bashrc` / `.profile` 的 export（`diff` 判据 = 恰好删 1 行且被删行与备份内容一致；A `.bashrc` 156→155 / B 152→151、`.profile` 35→34 / 34→33；备份 `~/.bashrc.bak-20260917-llamaserverpath`）；② `infer-load` 头部注释改为实测口径、并**移除**原第 212 行的注入（仓侧 `ops/station-bin/infer-load`；三站部署后 md5 `e474b761198dde596b86ab0edfad19be`，旧件保留为 `…bak-20260917-llamaserverpath` md5 `d57a9021…`）。**端到端自证（B 站实跑 `infer-load gpt-oss-20b`）**：rc=0 / 6s / `READY ✓ :8080`；派生进程 exe = `~/.unsloth/llama.cpp/build/bin/llama-server`、maps 含 `libggml-hip.so.0`、`--device ROCm0`、**其 `/proc/<pid>/environ` 已无 `LLAMA_SERVER_PATH`**（悬空 pin 断裂的直接证据）；`/v1/models` 200；`infer-unload` 干净（占用归 0）。
   **补记（判据自证）**：执行脚本首跑暴露我自己的三处判据缺陷 —— ① `grep -c X f || echo 0` 产出 `"0\n0"` 致 `case` 误判；② "残留检查"把注释里的历史路径也算残留（假阳性，改判 `^[^#]*LLAMA_SERVER_PATH`）；③ 备份名固定 ⇒ 重跑会用已改过的文件覆盖最初备份（改"存在即不覆盖"）。**"判据本身要先自证"第四次复发。**
2. **两套 llama.cpp 版本并存**：studio 面 `0.3.0-dev b10715 (92cedc867)` **vs** `/opt` 面 `0.4.0-dev b1533 (91f6a6cf)` ⇒ **凡说"某 PR 是否已入现役"，必须指明是哪一面**；本台账 §1.4 各条的"现役已含 / 不含"**一律按 `/opt` 面判定**（分布式走它）。
3. **统一管理入口原不显式输出 GPU 后端**：`cluster.py models meta` 的「后端」列 = **引擎路径**（`unsloth` / `llama-single`），`cluster.py versions` 输出 `/opt` 的 commit + rpc_protocol + 单机 commit —— **二者都不含 HIP/Vulkan 判定** ⇒ 本轮后端事实只能靠站上 `ldd` / `--list-devices` 取证。
   **✅ 已闭环（2026-09-17）**：`ops/rpc_check.py` 新增断言 **`backend`**（门禁 14 → **15 项**，quick=False 因需三站 ssh），判据全部落到**二进制**：① 三站 studio 自带引擎在位且 `ldd` 含 `libggml-hip.so`（默认单站路径后端）；② 三站 `/opt/llama.cpp/{llama-server,ggml-rpc-server}` 的 `ldd` 含 `libggml-vulkan.so`（分布式路径后端）；③ 三站 studio 自带 ROCm 运行时版本串一致（防 studio 静默换 SDK 致三站分叉，取不到时**不判**只告警）；④ 三站回滚基线 `/opt/llama.cpp-9859` 在位。**正负双向自证**：正 → `backend PASS · 单站=HIP / 分布式=Vulkan · ROCm libamdhip64.so.7.16.26332`；**真实负向** → 加入当天即抓到 **C 站回滚基线真缺失**（FAIL）；**合成负向 4/4** → 逐条注入故障（studio 引擎路径不存在 / 期望后端写反 / 基线路径不存在 / `/opt` 分布式件不存在），四条判据**各自专属文案**均命中且 FAIL（`tmp/_negtest_backend.py`）。

### 2.7 后端清减 + 回滚基线补齐（2026-09-17）

> **触发**：用户令「后端清减 + 补 C 站回滚基线」。**规则依据**：[UPGRADE_SOP §6](file:///d:/RPC/spec/vulkan-version-control/UPGRADE_SOP.md)「保留最近 2 个版本目录（`<ver>` + `9859`）」+ 回滚流程落点 = `llama.cpp-9859`；TRACKER §2.5 职责边界。ADR-0004 只管 `ops/` 脚本、**不覆盖后端目录**，但其 D5「先补判据 → 验证 → 再删」与"删前引用反查"两条纪律类推适用。

**核账（改前，只读）**

| 对象 | A | B | C | 判据 |
|---|---|---|---|---|
| `/opt/llama.cpp-9859` | ✅ 54 文件 / MANIFEST 61 全过 | ✅ 同 | ❌ **整份缺失** | `[md5]` 段 hash `ba7dab078424` ⇒ A/B **内容完全同一**；`RUNPATH=$ORIGIN` ⇒ **可移植，无需 patchelf** |
| `/opt/llama.cpp-master-d2e206c4` | ✅ 110 文件 79/79 | ✅ 同 | ✅ 同 | `[md5]` 段 hash `802236ce0cdd` 三站同一；**A/C 零引用，B 仅 2 个 `.bak` conf** |
| A `~/Applications/llama-gfx1151` | 2.2G / **0 可执行件** | — | 1.7G / 1 可执行（`llama-server`）/ 3 处 conf 引用 | 见下「⚠ 一处不可逆损失」 |
| B `~/build/llama-v0.2.0` + `~/src/llama.cpp-0.2.0` + `~/dist/llama-v0.2.0.tar.gz` | — | 1.3G+168M+33M | — | 上游 tag `v0.2.0` 可得；`/opt/llama.cpp-v0.2.0` 已于 9/8 清除 ⇒ **无对应产物** |

**执行（判据先行 → 再动手）**

1. **补 C 站回滚基线**：A 站打包（31,215,143 B，md5 `a0c20e05b2aacf805ea5ba4d964ef4d7`）→ 主控中转 → C 站解包。**三重校验全过**：包 md5 一致 / 文件清单 hash `ce4d55174452` 与源站一致 / MANIFEST **61/61**；所有权 `scott-lau:scott-lau` 正确。
2. **删 `/opt/llama.cpp-master-d2e206c4`（三站）**：守卫 = 路径白名单字面量 + 非现役 symlink 目标 + **MANIFEST 79/79 通过才删**；先 tar 备份到 `/data/backup/llama-cleanup-20260917/<host>-…tgz`（27M/站）并**自证可解开**（127 条目）。
3. **删 A 站**：`~/Applications/llama-gfx1151` 残件（2.2G，守卫 = 零可执行件）+ `~/build/llama.cpp.old`（1.5G，git `5d3a4a7`，其 `build/bin/llama-server` 已损坏 —— 缺 `libmtmd.so.0`）。
4. **删 B 站**：v0.2.0 三项（见上表）。**B 站两个引用 d2e206c4 的 `.bak` conf**（9/6）**移出活动配置目录**到备份区（不删，保留可回退）。
5. **验收**：三站 `/opt` 现均恰为**规则要求的 2 个目录**（现役 symlink `91f6a6cf` + 回滚基线 `9859`）；删后引用扫描 `d2e206c4`/`llama-v0.2.0`/`llama.cpp-0.2.0`/`llama.cpp.old` **三站零活引用**，`Applications/llama-gfx1151` 仅 C 站 3 处（**预期正确** —— C 站保留该现役引擎）；`cluster.py versions` 三站 `完整性 79 OK`；**端到端冒烟** B 站 `infer-load gpt-oss-20b` → rc=0 / 6s / READY，引擎 = studio 自带 HIP，unload 后占用 0G。

**⚠ 一处不可逆损失（如实留档，不掩饰）**

删 A 站 `llama-gfx1151` 时我的判据是「**零可执行件 + 零引用**」，并据「单个 `libggml-hip.so.0` md5 与 C 站相同」写下"C 站为完整件 ⇒ 可恢复"的结论。**删后与 C 站交叉印证推翻了该结论**：A = **382** 文件，C = **381**（= 380 + `llama-server`）⇒ A 比「C 去掉 `llama-server`」**多 2 个文件**，清单 hash 也不同（A `a808090b…` vs C 去 exe 后 `723da81b…`）。那 2 个文件**身份未知、未留副本**（删除时只存元数据、未 tar）。

- **判据缺陷**：判"零独有能力"时**未验「与 C 站同构」**，只比了 1 个文件的 md5 ⇒ **判据强度不足**（"判据本身要先自证"第五次复发，**这次造成了实际损失**）。
- **影响面（已复验）**：A 站对该路径**零引用**（conf / systemd / `/usr/local/bin` / `.bashrc` / `.profile` / `scripts` 全空）⇒ **A 站运行链路零功能影响**；核心构建件 md5 与 C 站一致；C 站完整件保留 ⇒ 结论 = "无功能影响 + 不可逆的小量未知文件损失"。
- **处置**：证据文件已追加**事后审计修正**注记（`/data/backup/llama-cleanup-20260917/scott-lau-NEX-gfx1151-残件-证据.txt`），明确**不再声称等价**。

**顺带发现的既有站间漂移（登记，未处置）**

| 项 | A | B | C | 备注 |
|---|---|---|---|---|
| 内核 | **6.17.0-40** | 6.17.0-23 | 6.17.0-23 | A 站已升档；社区普遍认为 Strix Halo 的 KFD 队列/CWSR 修复要 6.18.4+ ⇒ **与"HIP 6.x-vs-7.x 对照实验"耦合，做实验前必须锁死** |
| LM Studio | 0.4.19+2 | 0.4.19+2 | **0.4.14+4** | C 站落后（`cluster.py versions` 已能报出，本次实测 ⚠ 漂移） |
| `/etc/llama-instances/*.bak*` 残留 | 3 | **11** | 3 | 历史 conf 备份；非活配置（`*.env` glob 不匹配） |

***

### 2.8 非 Qwen 单机线引擎统一 + gfx1151 移除 + flash-next 引擎修正（2026-09-17）

> 答复用户「B 站可装 worker 否 / ④ 单机线引擎 A/B/C 能否统一」。前置 §2.5/§2.6 中「C 站 gfx1151 现役引用」均为本节执行**之前**的状态。

**结论先行**
1. **单机线无法一刀切统一**——Qwen3.8 系内部分化：**qwen3 架构（mtp/davidau）由 0d18aaa（`~/llama.cpp/build`，早期 Vulkan）载**；**flash-next 是 `qwen4exp` 架构、由 `/opt/llama.cpp`（b1533，libllama `qwen4exp=93`）载** ⇒ 旧硬约束「Qwen3.8 只能 0d18aaa」**对 flash-next 不成立**，已修正。
2. **非 Qwen 单机线已全部收敛到统一 HIP/studio 缺省**（此前唯一偏离 = C 站 gpt-oss-120b pin 到 gfx1151）。
3. **gfx1151 实验件已移除**（三站唯一残留；删除无损——它 libllama `qwen4exp=0` 本载不了 flash-next；tar 备份在）。

**动作与判据（全自证）**
- **① gpt-oss-120b 统一**：C 站 env `BACKEND=llama-single + LLAMA_SERVER_BIN=gfx1151` → `BACKEND=unsloth`（studio HIP），与 A/B 一致；MODEL_PATH/PORT/CTX/THREADS/RPC_TARGET 不变；`diff` 仅预期差异；`.bak-unify-20260917` 保留原始。
- **② 引用面穷举**（删 gfx1151 前）：三站 conf 精确判据 `^LLAMA_SERVER_BIN=.*llama-gfx1151` ⇒ **唯一活引用 = C 站 flash-next env**；systemd / /opt / `/usr/local/bin` 零引用；A/B 无此目录。
- **③ flash-next 引擎判定（实测翻转三次，最终 /opt 定案）**：先 pin 到 **0d18aaa** → journal 实测 `unknown model architecture: 'qwen4exp'`（失败）；再判证 **gfx1151 libllama `qwen4exp=0`**、**0d18aaa 不支持**、**`/opt/llama.cpp` libllama `qwen4exp=93`** ⇒ 改 pin `/opt/llama.cpp/llama-server` → **试载 `READY ✓ :8080` / `/health http_200`**，进程确认 `/opt/llama.cpp/llama-server`。`.bak-0d18aaa-20260917` 保留中间态。
- **④ 删除**：`rm -rf ~/Applications/llama-gfx1151`（1.7G/381 文件）→ `GFX_GONE`；tar 备份 `/home/scott-lau/backups/llama-gfx1151-20260917.tar.gz`（**466MB / 388 条目**）在；三站活动 `LLAMA_SERVER_BIN=*gfx1151` 引用 **全 NONE**。
- **⑤ 一次判据教训（严格说不是第 N 次复发，是同族变体）**：删 gfx1151 时**测引擎架构用的串是经验映射的 `qwen35`，而目标 GGUF 真实 arch 是 `qwen4exp`** ⇒ 撞对结论但不严谨。纪律：**架构支持必须对目标 GGUF 的真实 arch 串检测，且 arch 表在 `libllama.so`（非 launcher）**；**最可靠判据是实际试载**（journal 的 `unknown xxx` 为铁证）。闪存两次误 pin 因未先试载，最终靠实测定案。
- **⑥ 备份铁律补充**：`/etc` 下文件 `cp` 无 sudo 会**静默失败** ⇒ `~/.bash` 备份 / `sudo` 备份 + size 校验必须。

**遗留**：无功能遗留（flash-next 已修复可见链路、gfx1151 删除可逆）。跨站 `nodes.env` / `rpc-server@` 单工位+端口写死 50052 的僵化、`qwen3.8-flash-next` 在 B 站仍走 RPC(Vulkan/opt) 与 C 站单机(/opt) 的语义差异 → 待处置（非本次范围）。

***

## 3. 已关闭区（merged + 集群已验证）

| 条目 | 关闭日期 | 依据 |
|---|---|---|
| （空，首版） | — | — |

***

## 4. 有状态的反向链接（各文档引用本表）

- [FRAMEWORK-SURVEY 附录 H.4](file:///d:/RPC/spec/model-eval/FRAMEWORK-SURVEY-2026-09.md)（原 docs/GLM-5.3-Flash-分布式部署调研并入）§5.1 —— 架构支持表「快照」→ 状态以本表为准
- [spec/operator-optimization/DESIGN.md](file:///d:/RPC/spec/operator-optimization/DESIGN.md) §6 —— Phase C 跟踪 → 状态以本表为准
- [spec/model-eval/MODEL-SOURCING-2026-09.md](file:///d:/RPC/spec/model-eval/MODEL-SOURCING-2026-09.md) §6a —— PR 表 → 状态以本表为准
- [spec/station-c/DEPLOYMENT.md](file:///d:/RPC/spec/station-c/DEPLOYMENT.md) —— C 站引擎 → 版本描述同步本表 2.x

***

## 5. 变更日志

| 日期 | 操作 | 内容 |
|---|---|---|
| 2026-09-18 | **批 B 收口：缺口 8 遥测落地 —— run.json 从"跑过了"到"花了多少/何时/哪次会话"（v3.8）** | 用户令「执行下一步」。**① 侦察定案**：既有"无头 run 不吐 usage"仍成立（cluster.py reqlog 注）⇒ 不能从输出反推；但站上 `~/.local/share/opencode/opencode.db` 的 **`session` 表**自带 `tokens_input/output/reasoning/cache_read/cache_write`、`cost`、`time_created/updated`、`id`、`directory`，**`part` 表**的 `data.type='tool'` 可数工具调用（参考 run 实测 2 个 `read` ↔ agent 真读 2 个附件，**对得上**）⇒ **唯一可自证口径 = opencode 自己对这次会话的聚合**。**② 改动**：(a) 新增站上 helper [`_oc_session_meta.sh`](../../ops/station-bin/_oc_session_meta.sh)（已登记 [inventory/ops.yaml](../../inventory/ops.yaml)）—— `python3` **只读**（`mode=ro`）按 `directory` + 窗口起点取最新会话 ⇒ `out/.session-meta.txt`；**任何异常落 `FOUND=0` 并 exit 0**（遥测**永不参与成败判定**）且**一定产出文件**（不靠"文件缺失"猜）；(b) console 三态落键 —— `FOUND=1` ⇒ 真实值（`usage.source=opencode-session-db`）/ 取不到 ⇒ `unavailable` + 告警 / claude 备路 ⇒ `not-collected-claude-path`（**绝不把 0 当"用了 0 token"**）；(c) `timestamp_start/end` = 会话 `time_created/updated`（ms→ISO8601）；(d) runDir 增 **`session-meta.txt`** 原件 + 夹具卡 `smoke-dispatch.md` 增声明 `session-meta`（只影响该卡**新** run，历史条目不回溯）。**③ 归属判据（与钟无关）**：窗口起点 = run ts − 5min（容忍主/站钟偏斜）取最新；**歧义只在"另一会话与本会话时间重叠"时标注**（两个时间戳同取站钟）—— 不用"候选 >1 即报警"（背靠背派发必然 ≥2 ⇒ **天天报警 = 判据被忽略**）；实测连跑三次 `CANDIDATES=3` 而 `AMBIGUOUS=0` ✔。**④ 实测（阶段 0.5 第 12–14 次）**：(a) **正向**（run `202609181235380478`）`session_id=ses_f4d335418ffedzt2OvOC9IdUMn`、`source=opencode-session-db`、`total=12451`（input 5 / output 113 / cache_read 12333）、`ts 12:35:52.935→12:36:06.172`（13.2s ↔ `run_s=18` 含取锁入队，口径不同不矛盾）、`ATTACH_MANIFEST_LINES=0`；(b) **独立复核（另一实现 + 另一张表）** —— 站上另跑脚本：`session` 行逐字段一致，**Σ `message` 表的 tokens = 12451 = 会话行聚合（跨表 MATCH: True）**，tool 计数 0，跨度 13.2s 一致；(c) **NA 路径实证**（run `202609181232158097`，意外获得）—— 因 PS5.1 API 误用未取到遥测 ⇒ 落 `unavailable` + 控制台告警，而 `TASK_RC=0`/`exit=0`/accept+golden 全过 ⇒ **"不猜数"在真实链路上跑通**；(d) **负向** 改 `usage.total_tokens` 1 位 ⇒ `✗ [78] digest_mismatch 变了: .agent-run.json` + 门禁 **FAIL/红灯 1** ⇒ 恢复 PASS（遥测字段**确被链钉住**）；(e) helper 本地单测（合成 `opencode.db`）三例全过（命中 / 窗口不匹配 / 工作区不存在，且**三例都产出文件**）。链 **79 条全绿**（A1 15/79 · A2 16/79 · A3 3/79）。**⑤ ⚠ 两个实测发现（记账）**：(i) **PS5.1 无 `[DateTime]::UnixEpoch`**（.NET Core 时代成员；.NET FW 4.8 **静默返回 `''`** ⇒ 相减抛 `找不到 op_Subtraction 的重载`）⇒ 全改 `[DateTimeOffset]`（4.6+ 有 `ToUnixTimeMilliseconds`）；该误用**顺带把 NA 路径跑通**，但教训是**不能让 try/catch 兜底掩盖正常路径的错**；(ii) **缺口 5 的 `.attach/` 清理只覆盖"有附件"路径**（外层套在 `if ($attach.Count -gt 0)`）⇒ **无附件派发留着上一轮附件**（实测两次无附件 run 均 `ATTACH_MANIFEST_LINES=3`）⇒ 改**无条件 reset**（代价：无附件派发多一次 ssh）—— **教训：只覆盖一半的修复看起来是完整的**（与"判据要报覆盖率"同源）。**⑥ 门禁全量 PASS（15/1/0）**。**落档**：ADR-0007 缺口表 **8 → ✅** + 新增「缺口 8 落地与实测（批 B 收口）」节 + **批 B 全部完成** + 验证边界（三处顺序修正）；调研 09-16 §10 **缺口 8 → ✅**；ARCHITECTURE §6（契约行加遥测 + 证据件加 `session-meta.txt` + 四条自证能力）；IMPLEMENTATION（遥测来源段 + `.attach/` 无条件 reset）；DESIGN schema（usage/timestamps）；OPEN-ISSUES 证据流行 09-18 收口 + 新登记 2 行（已闭环）+ 计数说明；TRACKER v3.8。**下一步**：**批 B 已全部完成** ⇒ 只剩 **阶段 3（批 C）异基座管线审计**（挂起，待证据面/需求明确后重启） | 用户令「好的，执行吧」（承接缺口 5）。**① 缺口 5 三处改动**：(a) **远端采集**（fused script，agent 运行**前** / marker 之前）—— `cd $W/.attach && find . -type f -printf '%P\n' \| LC_ALL=C sort \| xargs -r sha256sum` ⇒ `out/.attach-manifest.txt`（逐文件 `<hex>  <relpath>`；**必须在 agent 运行前采样**，记的是"**注入的字节**"）；(b) **console 解析 + 落键** —— 合批通道增件并归档为 runDir `attach-manifest.txt`，按**首段路径**分组算摘要 ⇒ run.json `attach` 升为**对象数组** `{name, src, kind, files, sha256}`（无附件仍 `[]`；清单缺失 ⇒ 记空串 + `ATTACH_MANIFEST_ABSENT` 告警，**不静默当作通过**）；(c) **`.attach/` 生命周期订正** —— 实测站上残留 09-05/09-12 五次派发的附件（`fileA.md`/`fileB.txt`/`inbox.txt`/`_o11_src.txt`/`_o26_src.txt` + `docs/`/`emptydir/`）⇒ 与 IMPLEMENTATION"单次 task 生命周期、结束即回收"**正相反**，且**会污染本次摘要**（旧文件混进本次 digest，"看着正常"却不是本次注入）⇒ 改为**派发前 `rm -rf` 重建**。**② 摘要规则与上限**：组内每行规范为 `"<relpath>:<sha256>"` ⇒ 按相对路径排序 ⇒ `join("\n")` + 尾随换行取 `sha256`（空行集 ⇒ `sha256("")`）；实现**只在 console 一处**（`Get-Sha256Lines`）；**复验侧刻意不重算**（重算=第二实现点，而原始件未被链钉住 ⇒ 也判不出篡改）—— **收益来自"落进 run.json"⇒ 摘要自动被链钉住**。规则已写进夹具卡供独立复算。**③ 实测（阶段 0.5 第 9–11 次）**：(a) **正向**（参考 run `202609181204076459`）`TASK_RC=0`/`ACCEPT_GOLDEN_OK=1`/`ATTACH_MANIFEST_LINES=3`/`WORKSPACE_DIFF_LINES=0`/`exit_code=0(Int)`/`status=completed`/`slot=allow`，且 agent **真读到了附件**（输出含两 marker ⇒ 清空 `.attach/` 未破坏派发）；(b) **跨信任域交叉验证（核心）** —— 站上清单 3 个哈希 == 主控侧对**源文件**的独立哈希**逐位一致**；(c) **自洽复算**（独立实现）摘要 3/3、计数 3/3；(d) **负向①** 改 run.json 摘要 1 字符 ⇒ `✗ [74] digest_mismatch 变了: .agent-run.json` + 门禁 **FAIL/红灯 1** ⇒ 恢复 PASS；(e) **负向②** 改 `attach-manifest.txt`（本卡**已声明** ⇒ 在 v2 件集）⇒ `digest_mismatch 变了: attach-manifest` ⇒ 恢复逐位还原。链 **76 条全绿**（A1 12/76 · A2 13/76 · A3 3/76）。**④ ⚠ 事故与纪律：函数返回值被"非返回值输出"污染（本轮实测，必须记账）** —— **症状**：run.json 出现 `"exit_code": [null, 0]` + `"status": "failed"`（实际 rc=0），复验器**直接崩**（`TypeError: unhashable type: 'list'`），同 session `SLOT-GATE: na (engine port unknown)`。**根因**：PS 函数 = **管道上的全部输出** —— `Invoke-RemoteScript`/`Invoke-StationReady`/`Invoke-SlotGate`/`Invoke-Task` 靠 `return $x` 交付，但体内有未被吸收的 cmdlet 输出；环境层对文件操作的包装器在失败路径吐了 `$null` ⇒ 返回值变 `@($null, <真值>)`。**修法（"归零纪律"）**：通知改 `Write-Host`；`Copy-Item`/`Move-Item`/`Remove-Item`/`Add-Content` 一律 `\| Out-Null`；纪律写入注释。**自证**：再派发 ⇒ `excode=0`（非数组）+ `exit_code=0(Int)` + **槽位门恢复**。**复验器加固**：`_verdict_check` 遇畸形 `exit_code` ⇒ **报 issue 而非抛异常**（崩掉 = **整链判据同时消失**，与批 A 的"判据静默降级"同族两面）。**⑤ 残留处置（诚实）**：首个正向 run `202609181154242184` 契约字段已被环境污染、`.progress` 又被**孤儿采样器**（中止的派发所留）污染 ⇒ 按先例**回收该条**（移出 runDir + 摘链末条 + 重锚 ⇒ 链回 74）；台账保留该行（append-only 观测史）。**顺带**：中止派发后**站上 `out/` 是按文件名共享的**，仅 `.meta` 有 O-22 陈旧守卫 ⇒ 其余件可被孤儿覆盖而无判据（**新登记为未决项**）。**⑥ 顺带订正（文档≠实现，均实测）**：IMPLEMENTATION 的 `--attach` 实现（tar 打包 / `.attach/<ts>/` / ≤50MB / 结束即回收 **全不实**）与**台账列序**（实为 `…,exit,queue_s,run_s`，e2e 行 `…,0,2,56` 与 `.meta` 对齐）；ARCHITECTURE §6 两处过时"缺口"标注（queue_s、diff 证据）；夹具跑法坑（`powershell -File` 下 `-Attach a,b,c` **不被拆成数组** ⇒ 三附件全 `attach missing (skip)`，须用 `&` + `@()`）。**⑦ 编辑安全**：`agent-cli.ps1` 全程 BOM `EF BB BF` 保住 + CRLF=0 + AST 0 错误 + 插入的 bash 段 `sh -n` 通过并**本地功能实测**（有附件 2 行且与独立哈希一致 / 空目录不产行 / 无 `.attach` ⇒ 0 字节）。**⑧ 门禁全量 PASS（15/1/0）**。**落档**：ADR-0007 阶段表 **2-d/缺口 5 → ✅** + 新增「缺口 5 落地与实测」节（含"归零纪律"事故记账、上限声明、顺带订正表）+ 批 B 状态（**仅剩缺口 8**）+ 验证边界（三处顺序修正）；调研 09-16 §10 **缺口 5 → ✅**；ARCHITECTURE §6（契约/证据件/台账三行）；IMPLEMENTATION（attach 两处 + 台账列序）；DESIGN 的 run.json schema；OPEN-ISSUES 证据流行 09-18 更新 + 新登记 2 行（污染闭环 / `out/` 按名共享）+ 计数说明（剩 5 项）；夹具卡 `smoke-attach.md`（**入库**）；TRACKER v3.7。**下一步**：缺口 8（遥测）→ 批 C（阶段 3，待证据面扩宽后重启） |
| 2026-09-18 | **批 B 收官项：缺口 4 diff-scope 闭环 —— readonly"未越界"从无载体到正负双向实测（v3.6）** | 用户令「先做 e2e 负向测试」→「好的，执行吧」（= 补落档 + 推送）。**① 载体必须换（实测结论，非设计推断）**：远端工作区**不是 git 仓库**（实测 `paper` 在 A/B 两站 `git=NO`；全站仅 B 站 `Cpp_Hub` 恰好有 `.git`）⇒ 调研 §7.2 草案的 `git diff` / `git status --porcelain` **在非仓库上静默返回空 = 假的"未越界"**（本框架最怕的假阴性）⇒ 改用**与 git 无关**的 **`find -newer .run-marker -type f -printf '%P\n'`**。**② 两步落地**：(a) **判据 + 正向夹具卡**（提交 `585b94e`，**刻意不碰 `.ps1`**）—— `_diff_scope_check(run_dir,label)`（→ `issues/gaps/judged` 三态）+ `AGENT_DIFF_ALLOW_PREFIXES=("out/",)` + `smoke-dispatch-readonly.md` + 单测 **D1–D5**；(b) **远端采集 + console 回收**（提交 `c14aa3a`）—— 远端 agent 运行**前** `: > $W/.run-marker`、resume 循环后 / **golden 门之前** `find -newer` + 排除清单（`out/`、`.golden/`、`.attach/`、`agent-out/`、`.agentsync/`、`.git/`、点文件、`agent-runs.log`）⇒ `out/.workspace-diff.txt`；console 把 `.workspace-diff.txt` 并入 ADR-0005 **合批单连接**（`$evNames`）并改名 `workspace-diff.txt`；(c) **负向夹具**（提交 `baca9f3`）`smoke-dispatch-breach.md`（`readonly: true` 但任务要求在 `out/` 外建 `SMOKE-BREACH.txt`；manifest 声明全 5 件 + `workspace-diff` ⇒ 保持**单变量**）。**③ 三处承重设计（各有反例）**：marker **必须在 agent 运行前**（否则窗口错位、diff 恒空 —— 而"恒空载体"比"无载体"更危险：它看起来正是"零改动＝未越界"）；采集**必须在 golden 门之前**（golden 自身会产出构建物如 cpphub beta 编译 ⇒ 否则算到 agent 头上）；排除清单**必须完整**（漏一项 ⇒ diff 恒非空 ⇒ 判据退化为噪声 ⇒ 会被整体忽略，与 D4 同纪律）。**④ 判据语义（不新造一层，与 D4 对齐）**：非 readonly ⇒ **不适用**；readonly 无载体 ⇒ **gap 且不得当作通过**；diff 空 / 仅触 `out/` ⇒ **PASS**（`allow` **必须含 `out/`** —— 多张 readonly 卡交付物写在 `out/`，如 `dogfood-research-modulemap` 的 accept 判 `out/.dogfood_module_map.md` ⇒ naive"readonly⇒零改动"会**误杀合法运行**）；触及 `out/` 之外 ⇒ **FAIL**。**⑤ recipe v2 让缺口 4 免于"重新基线化"**：`workspace-diff` 为**卡声明的 `collect` 型 subject**，v2 按 `subjects` 逐条取件、`collect` 型无 `path` ⇒ 按**约定名** `<name>.txt` 找归档件（[cluster.py:3253](../../ops/cluster.py#L3253)）⇒ 新条目**自动 v2**、**历史 v1 条目零影响**（这正是阶段 1 先铺"recipe 按条目分派"的全部理由）。**⑥ 实测（阶段 0.5 第 6–8 次实战）**：(a) 单测 **D1–D5** 全过；(b) **正向 e2e**（run `202609181119254134`）`TASK_DONE exit=0` / `ACCEPT_GOLDEN_OK=1` / `WORKSPACE_DIFF_LINES=0` ⇒ 载体 **0 字节**，`chain` → **74**，`verify` **PASS**（A3 **1/74 可判且干净**）；(c) **负向 e2e**（run `202609181125165520`）`WORKSPACE_DIFF_LINES=1`、载体 = `SMOKE-BREACH.txt` ⇒ `✗ [74] diff_scope: readonly 卡**越界** —— 改动 out/ 之外: SMOKE-BREACH.txt` **FAIL** + 门禁 `evidence` **FAIL + 红灯 1（阻断）**；(d) **顺带**：该卡首版仅声明 `workspace-diff` 而另 5 件也归档 ⇒ 同时报 `manifest_undeclared`（证明阶段 1 收口判据在真实链路上**不是摆设**）。**⑦ ⚠ 实测抓到真缺陷（必须记账）：空文件被静默丢弃** —— 首次派发 `WORKSPACE_DIFF_LINES=0` **但 runDir 无 `workspace-diff.txt`**（A3 0/73）。根因：console 合批通道用 `if ($evBuf[$n])` = **真值判定**，而 `base64 -w0` 对**空文件**输出空串 ⇒ falsy ⇒ 被丢；而 **"零改动"恰恰就是空文件**（readonly 卡**最正常**的结果）⇒ **该例必被丢**（最该通过的样本永远不可能被判定）。修法：`ContainsKey` **存在性判定** ⇒ 修后 A3 **1/74 可判且干净**。**它与批 A 的 `TASK_ID != label` 恒真同族**（静默降级），区别是这次发生在**证据回收通道**而非判据自身 ⇒ 再次印证"**'通过'与'没判'必须可区分，覆盖率是唯一手段**"。**⑧ 测试残留回收（踩到的连锁）**：负向 run 是**合成件**（非真实派发）⇒ 删站上 `SMOKE-BREACH.txt` + 回收 runDir + **摘除链末条** + 重锚 ⇒ 链回 **74** 条、`verify` PASS、门禁 10/0/0。**坑**：只摘链不同步冷路径 ⇒ `cold_mismatch`；再同步冷路径 ⇒ `anchor_mismatch`（锚仍指旧链）⇒ 必须 **"同步冷路径 + `agent chain --reanchor`"两步齐做** ⇒ 反证**锚不是装饰：它会咬人**。**⑨ 覆盖率现状（诚实）**：门禁 note 报 **`A3 diff-scope 1/74 可判`**（**30 个 readonly 卡缺载体 ⇒ 不可判**；43 个非 readonly ⇒ **不适用**）；**已归档的 30 个历史 readonly run 无法回溯补齐**（载体只能在派发时采集）⇒ **永久留在"不可判"**，只能随新派发提升 —— 这是承载体的**时机约束**而非遗漏，门禁 note 始终把"不适用/不可判/可判"**分开报**。**⑩ 明确未做**：交接稿第 3 步写的是"由远端**执行** manifest 声明的 `collect` 命令"，实际落地为"**框架在固定位置采集约定产物**，`collect` 字符串目前**只声明未执行**" ⇒ **`collect` 命令的通用执行器未实现**（将来要支持须补执行器 + 沙箱约束，否则卡可借 `collect` 任意执行）。**⑪ 编辑安全**：`agent-cli.ps1` 改后 BOM `EF BB BF` 保住 + AST 0 错误；插入的 shell 段单独 `sh -n` 通过；顺带订正 [cluster.py](../../ops/cluster.py#L3488) 两处**已过时注释**（"载体尚未落地"）。**⑫ 门禁全量 PASS（15/1/0）**。**落档**：ADR-0007 阶段表 **2-d → ✅** + 新增「缺口 4 落地与实测」节 + 交接区状态行 → ✅ + 状态行/验证边界/D5 批 B 状态订正；调研 09-16 §10 **缺口 4 → ✅**；TRACKER v3.6。**下一步**：缺口 5（`attach` 补哈希）→ 缺口 8（`usage`/`session_id`/`timestamp` 遥测）→ 批 C（阶段 3 异基座审计，待证据面扩宽后重启） |
| 2026-09-18 | **阶段 1 收口：recipe v2 + 「未声明产物即失败」负向判据（v3.5）** | 用户令「执行下一步任务」→「按照建议执行」。**① 复验器 recipe `v2`**（提交 `cfce6f5`）：`AGENT_DIGEST_RECIPES` 增 `v2`；`_run_digest(dir,"v2")` 件集 = **`.agent-run.json` 恒入集 + `evidence_manifest.subjects` 逐条**（`path` 型按 path 哈希；`collect` 型尚无采集物［缺口 4］⇒ 记 `-`，**不当篡改**）；`agent_chain_append` **recipe 自动选择**（有 `subjects` ⇒ v2，否则 v1 ⇒ 未声明 manifest 的卡**链形完全不变**）；`verify` 遇 `rd is None`（v2 但无 subjects）⇒ `manifest_missing` **FAIL**。**② 负向判据「未声明产物即失败」**（借 SLSA「未识别 externalParameters ⇒ 校验失败」）：新增 `AGENT_MANIFEST_REQUIRED`（= 已知证据件去掉 `.agent-run.json`——它是声明载体本身，卡不该声明它）；`manifest_undeclared` FAIL 的判法是 **框架已知证据件已存在于 runDir 却未在 manifest 声明**（只查"已存在"的 ⇒ 缺件的老 run 不误报）。**③ 夹具卡补声明**：`smoke-dispatch.md` 由声明 2 件补为**全 5 件**（否则会被自己的 v2 判据判 FAIL）。**④ 实测（严格双向）**：(a) **v2 单测 8/8** —— 确定性 / 件集构成（`.agent-run.json`+2 声明）/ `collect` 型记 `-` / `v2 ≠ v1` / 未知 recipe ⇒ None / **改动"已声明"文件 ⇒ digest 变** / 无 subjects ⇒ None；(b) **回归**：71 条历史条目（全 v1）**PASS** ⇒ 新增 recipe **不需重建链**；(c) **e2e v2（阶段 0.5 第 5 次实战）**：`load` → 派发夹具卡（run `202609181053055631`）→ `chain` 71→**72**，新条目 **`recipe=v2`**（自动选择生效）→ `verify` **PASS**（A1 **8/72**、A2 **9/72**）→ `unload`；(d) **负向自证**：把旧 run 71（其 manifest 只声明 2 件）**临时标成 v2** ⇒ `✗ manifest_undeclared` **FAIL**（证明该判据**有牙**，非摆设）→ 恢复 ⇒ PASS。**⑤ 顺带修**：`manifest_missing`/`manifest_undeclared` 两种 kind 的打印（原先落到 cold_mismatch 格式，显示成 `cold_n=None chain_n=None`）。**⑥ 门禁全量 PASS（15/1/0）**。**落档**：ADR-0007 阶段表 1 → ✅；TRACKER v3.5。**下一步**：缺口 4（`collect` 型 subject 的实际采集，**已无重新基线化障碍**——新条目自带 v2，历史 v1 不受影响）→ 缺口 5 attach 哈希 → 缺口 8 遥测 |
| 2026-09-18 | **阶段 1「卡侧」落地：evidence-manifest 三级嵌套 + run.json 落键（v3.4）** | 用户令「先推送」（→ 6 笔已推 `cf01b16..1d457db`，pre-push 全量门禁 PASS 15/1/0）再令「先做阶段 1 卡侧」。**① 改动（[agent-cli.ps1](../../ops/station-bin/agent-cli.ps1)）**：(a) `Get-FrontMatter` 增 **`evidence-manifest` 三级嵌套**解析（`version` / `subjects[].name\|path\|collect\|digest`，形状借 in-toto Statement，**不自创语义**——调研 §7.2）；(b) run.json 落 `evidence_manifest`（**原文照收不规范化** —— ADR-0005 D2 纪律；**仅当 subjects 非空才落键**，与 `accept_golden` 同例 ⇒ 老 run.json 形状不变）。**② 关键坑（顺序承重，已实证）**：通用键正则 `^\s*([A-Za-z_\-]+)\s*:\s*(.*)$` **字符类含 `-`** 且允许前导空白 ⇒ `evidence-manifest:` **会被它命中**（实测 `匹配 key='evidence-manifest'`）⇒ 新分支若不在其前，会因 `ContainsKey` 命中而 `$h[$k]=$v`（**空串覆盖整个哈希表**，连带清顶层键）—— 与 `accept-golden` 嵌套键当年同一个坑（P2-1/IMPLEMENTATION §3.1）。**③ 实测（严格）**：解析器夹具 **19/19 PASS**（新增 6 例，含"**顶层键未被清**"这一守顺序的用例）；**端到端（阶段 0.5 第 4 次实战，夹具卡已带 manifest）** —— `TASK_DONE exit=0` / `ACCEPT_GOLDEN_OK=1`，run.json 正确含 `evidence_manifest`（2 subjects），`chain` 70→**71**，`verify` PASS（A1 **7/71**、A2 **8/71**），`unload`；**向后兼容**：改前 run **无** `evidence_manifest` 键；**副作用观察**：夹具 run 的 `prompt_sha256` 与加 manifest **前完全相同** ⇒ manifest 在 front-matter、**不进 prompt**（与 strong-accept 不变式 1 同向）。**④ 编辑安全**：BOM `EF BB BF` 保住 + AST 0 错误。**⑤ 门禁全量 PASS（15/1/0）**。**落档**：ADR-0007 阶段表 1 → ◐ 已落地（仍缺 recipe v2 与负向判据）+ 新增"阶段 1 卡侧"节；TRACKER v3.4。**下一步**：复验器 recipe `v2`（读 run.json 的 `evidence_manifest.subjects` 决定件集）+ "未声明产物即失败"负向判据 → 缺口 4（`collect` 型 subject 的实际采集，**已无重新基线化障碍**）→ 缺口 5 attach 哈希 → 缺口 8 遥测 |
| 2026-09-18 | **阶段 1「架构半」：recipe 按条目分派 —— 缺口 4 的重新基线化堵点解除（v3.3）** | 用户令「执行吧」（= 提交缺口 10 批 + 继续阶段 1）。先提交 `9828889`（缺口 10，6 文件 +72/−3，见 v3.2）。**① 为什么先做这一半**：D2 原规定"加件必须 bump 全局 `AGENT_DIGEST_RECIPE`" ⇒ 缺口 4 一旦把 `workspace-diff` 加入件集，**全部历史条目误判断链** ⇒ 只能重建链 = **重新基线化**（把当时字节"洗白"，丢掉此前篡改的可检测性）—— **这是 D2 自己埋的坑**，而"按条目分派"是解药，也是阶段 1 manifest（"按 run 声明件集"）的**架构前提**。**② 改动**（[cluster.py](../../ops/cluster.py)）：新增 `AGENT_DIGEST_RECIPES=("v1",)`（**已知 recipe 集合**；`AGENT_DIGEST_RECIPE` 语义收窄为"**新建条目**用哪个"）；`_run_digest(run_dir, recipe)` 按传入 recipe 分派、**未知 recipe 返回 `None`（不可验）**；`agent_chain_verify` **按条目自带 `recipe` 复验**，未知 ⇒ `recipe_unknown` 且 **FAIL**（原 `recipe_mismatch` 被"未知即不可验"取代）；`cmd_agent` 与 `check_evidence` 的打印同步。**关键纪律（写入注释）**：未知 recipe **不得静默跳过** —— 否则"工具比链旧"会变成静默通过。**③ 实测（严格双向）**：(a) **回归** —— 70 条历史条目（全 `v1`）复验 **PASS** ⇒ 证明"新增 recipe 不必重建链"**目标达成 = 本改动的全部意义**；(b) **负向** —— 第 1 条 recipe 改 `v99` ⇒ `✗ [0] recipe 不可验 (链=v99 本工具知 ['v1']) —— 工具比链旧, 不得当作通过` + 门禁 **FAIL + 红灯 1**（另含 `cold_mismatch`：只改链未改冷路径，两处不一致被捕获）；(c) 恢复 ⇒ PASS；(d) 其他判据未受影响（解析器夹具 13/13、`evidence` A1 6/70 · A2 7/70）。**④ 仍未做（阶段 1 剩余）**：卡侧 `evidence-manifest:` 的 front-matter 解析（含 `subjects` 三级嵌套）+ run.json 记 `subjects[]` + "未声明产物出现即失败"的负向判据 —— **要动 `.ps1` 解析器，须按阶段 0.5 流程逐项实测**。**落档**：ADR-0007 阶段表 1 → ◐ 架构半 ✅ + 新增"阶段 1 架构半"节；TRACKER v3.3。**门禁全量 PASS（15/1/0）**。**下一步**：阶段 1 卡侧解析 → 缺口 4（作为 manifest subject，**已无重新基线化障碍**）→ 缺口 5 attach 哈希 → 缺口 8 遥测 |
| 2026-09-18 | **批 B 次项：缺口 10 围栏闭环 —— 判断两次被实测推翻 + 门禁盲区（v3.2）** | 承接 v3.1，做批 B 次项（原计划 缺口 4 + 阶段 1 + 缺口 10 同批）。**① ⚠ 我的原判被证伪（记账）**：据 [agent-cli.ps1:621](../../ops/station-bin/agent-cli.ps1#L621) 推断"正文里的 `---` ⇒ 其后正文**被静默丢弃**"（类比 D-13/P1b）。**写用例实测 12/12 全过 ⇒ 推断错了** —— `$bodyRead` 不重置，普通正文行仍走 body 分支。**② 真陷阱（实测得出）**：分隔线后**形如 `key: value` 且命中已知键**的正文行被 front-matter 分支吸收 ⇒ `$h[$k] = $v` **静默覆盖**。用例（正文引用另一张卡 front-matter）实测 **`task`/`readonly`/`model` 三键全被覆盖**（`readonly: true` → `false` ← **安全相关**），且正文行被吸收 ⇒ **卡正文可静默改写卡契约**。**③ 修法（一行）**：围栏判据加 `-not $bodyRead` 守卫（`$bodyRead` 置真即 front-matter 已闭合，此后 `---` 只是正文）⇒ **13/13 PASS**。**④ 顺带发现：守门夹具本身早已静默失效（更值得记）** —— `_fm_golden_test.ps1`（**卡解析器的回归守门人**）**缺 UTF-8 BOM**（首字节 `23 20 3D`）；PS5.1 按 ANSI 解码含中文脚本时**3 字节 UTF-8 序列被当 2 字节 GBK 解码会吞掉相邻换行** ⇒ 语句并进注释 ⇒ `$fns` 从未赋值（`DEBUG fns count=0`）。补 BOM 后立刻恢复 `fns=32`。**这是"含非 ASCII 脚本必须 UTF-8 BOM"铁律的又一次实证**。**⑤ 暴露门禁盲区（已登记）**：`syntax` 抓不到该类 —— PS5.1 对无 BOM 文件按 ANSI 解码**不报错**（只是乱码）⇒ `.ps1:12文件/0失败` 通过 ≠ 语义正确（与 G8「'包在'≠'功能在'」同族）；已登记新行并建议给 `syntax` 加"含非 ASCII 的 .ps1/.sh 必须带 BOM"子判据。**⑥ 端到端确认（阶段 0.5 第二次实战）**：`load` → 派发夹具卡 → `TASK_DONE exit=0` / `ACCEPT_GOLDEN_OK=1`；台账 `…,0,2,18`（B1 持续有效）；`chain` 69→**70**；`verify` PASS（A1 **6/70**、A2 **7/70**）；`unload`。**旁证**：新 run 的 `prompt_sha256` 与改前不同，逐行 diff 存档 prompt.txt 显示**新 prompt 恰多一行 `---`**（4 字节）⇒ 正是"原先被吞掉的正文行现被保留"，**且证实卡正文确实会进 prompt**（修正我早前"正文不进 prompt"的读码印象）。**⑦ ⚠ 执行顺序修正（本轮侦察得出，已写入 OPEN-ISSUES）**：原计划把 `缺口 4 diff + 阶段 1 manifest` 同批，**顺序是反的** —— ① 缺口 4 载体若要**加进入链件集**，按 D2 须 bump `AGENT_DIGEST_RECIPE` ⇒ 全部历史条目误判断链 ⇒ 只能重建链 = **重新基线化**（把当时字节"洗白"，丢掉此前篡改的可检测性）；② **阶段 1 manifest 才是"按 run 声明件集"的机制**，能让新增证据类型**不触碰全局 recipe**；③ 且远端工作区**不是 git 仓库**（实测 `paper` 两站 `git=NO`）⇒ 调研 §7.2 草案的 `git diff` 方案**不可用**（非仓库上静默返回空 = 假的"未越界"），须改**与 git 无关**的载体（`find -newer <marker>` / 前后快照差）；④ `readonly` 现为"**仅记录**"（DESIGN §4.1 层2），真判据须带 **allow-path**（多张 readonly 卡的交付物写在 `out/`，naive"readonly⇒零改动"会**误杀合法运行**）⇒ **正确顺序 阶段 1 → 缺口 4**。**落档**：ADR-0007 缺口表 10 → ✅ + 新增"批 B 次项落地与实测"节；调研 §10 缺口 10 → ✅；OPEN-ISSUES 新增 1 行（BOM 盲区）+ 09-18 计数说明 + **执行顺序修正**块；TRACKER v3.2。**门禁全量 PASS（15/1/0）**。**批 B 剩余**：阶段 1 manifest → 缺口 4（作为 manifest subject）→ 缺口 5 attach 哈希 → 缺口 8 遥测 |
| 2026-09-18 | **批 B 首项：缺口 6 台账接线闭环 —— 阶段 0.5 夹具首次实战（v3.1）** | 用户令「执行吧」（= 提交阶段 0.5 + 进批 B）。先提交 `16fa0b1`（阶段 0.5，5 文件 +93/−1，见 v3.0），再做批 B 首项。**① 改动（一行）**：[agent-cli.ps1:1207](../../ops/station-bin/agent-cli.ps1#L1207) `"$ts,$proj,$id,$sens,$code,0,0"` → `"…,$queue_s,$run_s"`；`$queue_s/$run_s` 在 [:1165-1183](../../ops/station-bin/agent-cli.ps1#L1165-L1183) 的 meta 解析段**早已就绪**（写台账在其后），META_STALE 时二者已主动归零 ⇒ 与 run.json 一致。**② 两条路径不对称（分清才不误改）**：remote opencode 路径**是 bug**（远端 `.meta` 写了真值却没用）；**console claude 本地备路不是 bug**（其 `.meta` 本就写 `QUEUE_S=0` —— 本地执行无远端队列，台账/run.json 同为 0，**自洽**）⇒ **刻意不改**。**③ 实测（阶段 0.5 夹具第一次实战）**：`load gpt-oss-20b` → 派发 `smoke-dispatch.md` → 台账尾三行**直接呈现改前改后对照**：`202609162255192049…,0,0,0`（改前）/ `202609180952112524…,0,0,0`（改前·阶段0.5首跑）/ **`202609180959398587…,0,2,20`（改后：queue=2 / run=20 真值）**，与 `.meta` 的 `QUEUE_S=2/RUN_S=20` 一致；`chain` 68→**69**；`verify` PASS（A1 **5/69**、A2 **6/69**）；跑完 `unload`（三站 0 站在服务）。**④ 为何这 bug 长期没被发现（机制值得记）**：`agent runs` 视图的 queue/run 来自 **join `run.json`**（一直是对的）⇒ **台账列被 join 掩盖**；这是"**同一事实两个定义点**"的又一实例 —— 一个定义点对就没人看另一个，与 ADR-0003 D3 / ADR-0005 D3 的"不立第二定义点"纪律**同源**。**⑤ 编辑安全（该 .ps1 有 BOM 前科）**：改前备份（122404B 一致）+ 改后验 BOM `EF BB BF` **保住** + PowerShell **AST 0 错误**；diff 恰为 **1 行代码 + 5 行注释**。**⑥ 门禁**：全量 **PASS（绿灯 15 / 黄灯 1 / 红灯 0）**，`evidence` 覆盖率随夹具升为 A1 5/69 · A2 6/69。**落档**：ADR-0007 缺口表 6 → ✅ + 新增"批 B 首项落地与实测"节（含两路径不对称表 + join 掩盖机制）；调研 09-16 §10 缺口 6 行转 ✅；TRACKER v3.1。**批 B 剩余**：缺口 4 diff + 阶段 1 manifest + 缺口 10 围栏（同批）→ 缺口 5 attach 哈希 → 缺口 8 遥测 |
| 2026-09-18 | **阶段 0.5「改动验证闭环」建成 —— 批 B 的解锁（v3.0）** | 用户令「先提交 然后做阶段0.5」；先提交 `f1ce0a7`（批 A，8 文件 +591/−28，见 v2.9），再建 [ADR-0007](../../adr/ADR-0007-证据流阶段推进路线与改动验证闭环.md) D3 的**改动验证夹具**。**① 夹具（均入库，可复跑）**：轻卡 [`spec/d6-agent-standard/test-cards/smoke-dispatch.md`](../../spec/d6-agent-standard/test-cards/smoke-dispatch.md)（`proj=paper` / `model=gpt-oss-20b`→B 站 / `accept: [true]` / `accept-golden` → golden；任务=只回 token **不改任何文件**）+ golden [`ops/station-bin/golden/smoke_dispatch_golden.sh`](../../ops/station-bin/golden/smoke_dispatch_golden.sh)（**最小但真断**：断言 `./.golden` 存在＝clean-inject 生效 + 本脚本落在 `.golden/`＝注入的就是这一份 + 打印 cwd＝证 cwd 为工作区根；**刻意不验模型能力**）。**② 为什么入库（实证教训）**：ADR-0005 的等价夹具卡放 `tmp/e2e-evidence-card.md`，而 `tmp/` 被 `.gitignore` ⇒ **卡已丢失**（只剩 golden 残件在磁盘）⇒ **夹具必须入库才可复跑**（"夹具本身也会腐坏"）。**③ 执行（完整真实链路，非 mock）**：`cluster.py load gpt-oss-20b` → B 站 `READY ✓ :8080 (unsloth gpt-oss-20b)` → `agent-cli.ps1 task paper -Card …/smoke-dispatch.md` → `cluster.py agent chain` / `agent verify` → `cluster.py unload`。**④ 正向验收全绿**：`TASK_DONE … exit=0`；`ACCEPT_OK=1` / `ACCEPT_GOLDEN_OK=1` / `TASK_RC=0` / `RUN_S=19`；`agent-output.txt` 含 **`SMOKE-DISPATCH-OK`**；`accept-golden-output.txt`=`SMOKE_GOLDEN_OK pwd=/home/scott-lau/agent-workspaces/paper`（**同时证 clean-inject 与 cwd**）；runDir **9 件**（含入链 6 件）；`chain` **67→68**；`verify` PASS；**A1 3/67→4/68、A2 4/67→5/68**（夹具顺带**扩了复验器可判样本**，0 gap 0 note）。**⑤ 负向验收**：改该 run `judgment-record.txt`（`ACCEPT_GOLDEN_OK=1`→`0`）⇒ `digest_mismatch` + **`verdict_mismatch` 定位到 `[67]` 与字段**；门禁 `evidence` **FAIL + 红灯 1**；恢复后回 PASS（绿灯 10）⇒ **正负双向达成**（正是 D3 定的判据）。**⑥ 资源纪律**：跑完 `unload` ⇒ 三站「在服务 0 站 · 总占用 0G」。**⑦ 顺带当场复现缺口 6**：台账行 `ledger+=202609180952112524,paper,local/gpt-oss-20b,local-only,0,0,0`，而 `.meta`/run.json 是 `QUEUE_S=2 RUN_S=19` ⇒ **台账两列确为硬编码 `0,0`**（[agent-cli.ps1:1207](../../ops/station-bin/agent-cli.ps1#L1207)），**一行接线可修**（批 B 首项）。**⑧ LF 校验**：新建 `.sh` 验 **0 个 CR** + `sh -n` 通过（CRLF 会让远端 bash 直接挂，夹具经典坑）。**⑨ 定位**：阶段 0.5 **不产出功能**，但使批 B 的全部 `.ps1` 改动从"代码走查级"升为"**可实测级**" —— 这是必须先做它的唯一理由。门禁全量 **PASS（绿灯 15 / 黄灯 1 / 红灯 0）**。**落档**：ADR-0007 阶段表 0.5 转 ✅ + 新增"阶段 0.5 建成与验收"节（含跑法）；TRACKER v3.0。**下一批**：批 B（缺口 6 一行接线 → 缺口 4 diff + 阶段 1 manifest + 缺口 10 围栏 → 缺口 5 attach 哈希 → 缺口 8 遥测）**已解锁** |
| 2026-09-17 | **批 A 执行完毕 —— 复验器加语义判据（v2.9）** | 依 [ADR-0007](../../adr/ADR-0007-证据流阶段推进路线与改动验证闭环.md) D5 执行批 A（用户令「执行 A 批次 严格测试校验」）。**① 判据先行（不做代码想象）**：先做**数据侦察**再写码，得三条实测事实——(a) 仅 **3/67** run 有 `judgment-record.txt`（ADR-0005 之后才回收 ⇒ A1 覆盖率天然薄，且**会随时间增长**）；(b) `TASK_ID` 陈旧 **0** 例、queue/run 不一致 **0** 例；(c) **`TASK_RC=9`（验收/金标准失败）在 run.json 里被映射成 `exit_code=1`**（[agent-cli.ps1:1200-1201](../../ops/station-bin/agent-cli.ps1#L1200-L1201)），**原始 9 只留在 `.meta`**（`$codeReal` 赋值后全仓无引用 = 死变量）——**这正是 ADR-0005 归档 `.meta` 的价值实证**。**② A1 verdict-chain**（[`_verdict_check`](../../ops/cluster.py#L3338)）：比对 `TASK_RC`（按映射允许集 `{0:{0},6:{6},9:{1,9},24:{24}}`，9 取双值以兼容 claude 备路未定映射 —— **宁少报不误报**）/`ACCEPT_OK`↔`accept.passed`/`ACCEPT_GOLDEN_OK`↔`accept_golden.passed`/`QUEUE_S,RUN_S`↔`queue_s,run_s`；`TASK_ID≠ts`（O-22 残留）⇒ **gap 不 FAIL**（否则必假阳性）；缺 meta ⇒ **gap 且不计覆盖率**；`accept.passed is None` ⇒ **跳过该字段**；一项可比字段都没有 ⇒ **判为"不可判"而非"通过"**。**③ A2 golden-identity**（[`_golden_identity_check`](../../ops/cluster.py#L3415)）：按 `accept_golden.base` 全仓索引（含 gitignore 的 `tmp/`）比哈希；**永不 FAIL** —— golden 会**合法演进**（改判据脚本是正常开发行为），"哈希不同"与"被篡改"在此**不可区分** ⇒ 一律 note(info)。**④ 判据三层严重度落地**（ADR-0007 D4）：`issues`⇒FAIL / `gaps`⇒WARN / `notes`⇒info；门禁 note 增**覆盖率**（`A1 verdict-chain 3/67 · A2 golden-identity 4/67`）。**⑤ 严格测试 17 项全绿**：**函数级 13 项隔离测试**（synthetic runDir，覆盖 stale 容忍、`TASK_RC=9→exit1` 允许、`9+exit0` 拒、无 meta 不适用、不可比不虚报覆盖率、A2 三态、非 golden 卡不适用）+ **端到端 5 项**（篡改真实判据记录 ⇒ `digest_mismatch`+**`verdict_mismatch` 定位到字段**+rc=1；golden 源改名 ⇒ note 且**仍 PASS**；golden 内容改动 ⇒ note 且**仍 PASS**；门禁双向自证；恢复后 PASS）。**⑥ ⚠ 测试抓到并修掉一处真缺陷（记账）**：首版把**显示标签** `proj/ts` 当成 `TASK_ID` 的比对对手 ⇒ `meta.TASK_ID != label` **恒真** ⇒ A1 把 3 个有 meta 的 run 全误判"上一轮残留"、**覆盖率假报 0/67**，且**字段比对一次都没执行**（判据**静默降级为"永远跳过"**）。修法：`ts`（裸时间戳）与 `label`（显示）**参数分离** + 加 T3 专测守回归。**教训：判据的"通过"可能是因为它什么都没判 ⇒ 新增判据必须同时报覆盖率**（已写进门禁 note）。**⑦ A3 缺口 9 闭环**：`CLOSED-LOOP-ANALYSIS:91` 由"回写 ledger/review.json（run.json 平行键 review）"订正为"回写 `<runDir>/review.json`（**独立文件**，非 run.json 内的键）"（实现在 [agent-cli.ps1:1987](../../ops/station-bin/agent-cli.ps1#L1987) 确为独立文件）。**⑧ A4 缺口 6 诊断落档**：形态改写为"台账两列硬编码" ⇒ 归批 B（需 `.ps1` + 阶段 0.5 夹具）。**落档**：ADR-0007 阶段表 2-b/2-c/缺口 9 转 ✅ + 新增"批 A 落地与实测"节；调研 09-16 §10 缺口 9 行转 ✅；TRACKER v2.9。**下一批**：批 B 需先做**阶段 0.5（轻卡+小模型夹具）**才可动 `agent-cli.ps1`。**⑨ 顺带实测（纠正一处过时信念）**：逐 check 计时发现 **quick 门禁实测 ≈16.4s**（记忆/文档旧记"1.1s"**已过时**），其中 **`syntax` 独占 15.8s（96%）**，其余合计 <1s（`secrets` 123ms / `aliases` 214ms / `doclinks` 85ms / **本批新增的 `evidence` 仅 40ms**，全仓 golden 索引 walk 仅 5.5ms ⇒ **本批改动非 quick 变慢之因，仅 +0.24%**）。已登记 OPEN-ISSUES 新行「`syntax` 占 quick 96%」（属独立性能项，非证据流范围），并更正 project_memory 中的过时值 |
| 2026-09-17 | **证据流阶段推进路线定案 —— 新建 ADR-0007（v2.8）** | 用户令「将这项证据重发及其关联工作完整落档，记录阶段推进步骤，写成 ADR」。**① 新建 [ADR-0007](../../adr/ADR-0007-证据流阶段推进路线与改动验证闭环.md)**（`status: accepted`，depends ADR-0004/0005）：把「任务卡证据流可重放性」的**全貌**收敛为一张承接图（调研 09-16 → ADR-0005 阶段 0 → 协同篡改补充调研 → evidence-chain 复验器 → 三项遗留 → 本 ADR）+ **阶段推进路线表**（逐阶段：内容 /**可判定判据** / 状态 / 落点）+ **调研 §10 缺口表逐条对账** + 关联工作五件（复验器 / 门禁断言 / 钩子自动入链 / 外部锚 / 协同篡改分层）。**② 排计划前先钉正三处"文档状态 ≠ 代码状态"**（用户给的清单已过时）：`门禁对 agent 证据零覆盖` → ❌ **今日已闭环**（第 15 项断言 `evidence`）；`阶段 2 未做` → ◐ **判据已达成一半**（复验器**形态**已落地并实测"指到第一条断裂处"，剩的是**内容** verdict-chain/golden-identity）；`台账 queue_s/run_s 恒 0` → ◐ **形态被改写**（实测 65 run 中 `run.json` 的 queue/run **61/57 非零**；真正恒 0 的是**台账 `agent-runs.log` 两列** [agent-cli.ps1:1207](../../ops/station-bin/agent-cli.ps1#L1207) 硬编码 `0,0`，而变量在 [:1165-1183](../../ops/station-bin/agent-cli.ps1#L1165-L1183) 就已解析 ⇒ 从"补计量"降级为**一行接线**）。**③ 核心决策 D1：阶段顺序修正** —— 原调研「1→2→3 不可反」**被实测推翻**：复验器**不依赖 manifest**（需验件集已由 ADR-0005 固定为 6 件）⇒ 修正为 `0 → 0.5 → 2(内容) → 1 → 3`；manifest 边际价值从"复验器前置条件"降为"**扩展性** + SLSA『未识别参数即失败』语义"。**④ D3 新增阶段 0.5「改动验证闭环」**（轻卡 `echo.md` + 小模型 `gpt-oss-20b`，**不产出功能**）：因阶段 1 与缺口 4/5/6/8/10 **全都要改 `agent-cli.ps1` 派发路径**，而无活模型/活站则只能停在"代码走查级" ⇒ **夹具是后续所有 `.ps1` 改动的证据基础设施**，不建它整条路线卡在"不敢改"。**⑤ D2 件集单一真值 + D4 判据三层严重度**（篡改⇒FAIL / 覆盖缺口⇒WARN / 合法演进⇒info；新增判据必须先归类再落地）。**⑥ D5 批次排序**：批 A（`verdict-chain`+`golden-identity` 复验、缺口 9 文档漂移、缺口 6 诊断落档；纯主控侧可离线自证，且**直击本议题原始命题**）→ 批 B（阶段 0.5 先行，再动 `.ps1`）→ 批 C（**阶段 3 异基座审计挂起**：证据面仅 6+1 件、审计员可判项过少，等批 B 扩面后重启，避免造出"因无人能应对而失败"的判据）。**⑦ 否决 6 案**（坚持原顺序 / 不建 0.5 直接改 .ps1 / 让复验器自动"修复"不一致 / gap 表升级为评分 / 双轨 recipe 兼容 / 跨实例而非跨家族审计）。**⑧ 同步落档**：[OPEN-ISSUES](../../spec/d6-agent-standard/OPEN-ISSUES.md) 的「证据流可重放性」一行改为路线定案 + 待决策=批 A；调研 09-16 §7.1 加「**现状注记（09-17）**」（阶段顺序修正 + 新增 0.5 + 阶段 3 挂起 + 两处缺口状态更正）并把 §10 缺口表 **6/7 两行就地更正**。**未做**：批 A/B/C 与阶段 0.5 均只定路线，执行前逐批按 D3/D4 判据收杆 |
| 2026-09-17 | **证据链三项遗留闭环（v2.7）—— 门禁接入 + 钩子自动入链 + 外部锚** | 承接 v2.6，用户令「解决三项遗留问题」。**① 遗留#1 门禁接入**：`ops/rpc_check.py` 新增第 **15** 项断言 `evidence`（`quick=True` ⇒ 进 pre-commit 与 pre-push，**只读**）。**严重度刻意分开**：verify 的 issues（`digest_mismatch`/`chain_break`/`cold_mismatch`/`anchor_mismatch`/`run_dir_missing`/`recipe_mismatch`）= **证据被改或链被重写 ⇒ FAIL**；"有 run 未入链 / 缺锚" = **覆盖缺口非篡改 ⇒ WARN**（否则每次派发后提交都被阻断，判据会因噪声被整体忽略——同 doclinks "判不准的不进门禁"纪律）。**② 遗留#2 自动触发**：关键发现 **`.git/hooks/*` 是 `ops/rpc.ps1 install-hooks` 的生成物（"请勿手改"）⇒ 必须改生成器**。在 `Install-HookEntry` 生成的门禁调用**前**插一行 best-effort `"$PY" ops/cluster.py agent chain >/dev/null 2>&1 \|\| true`（失败不阻断提交；失败后果由紧随其后的 `evidence` 断言以 WARN 报出 ⇒ 不静默降级）。改后**重跑 install-hooks** 并实测钩子端到端。**③ 遗留#3 跨信任域锚**：关键发现 **`d:\RPC` 有 GitHub remote（`github.com:StormstoutLau/RPC.git`）⇒ 可用它当第 3 个信任域见证**。新增**外部锚** `archive/evidence-chain/ANCHOR.txt`（一页纯文本，钉链文件**字节** `chain_sha256` + 冷路径字节 + head + 条数 + recipe）⇒ 形成 **锚→链→证据** 三级；`.gitignore` **只排除链 JSON（体积线性长且可由归档重算），刻意保留 ANCHOR.txt 入库**（唯一**不可重算**者：它记录历史状态）；锚已 `git add`（**未提交**，待用户决定）。**④ 关键设计点（防"修复即抹痕"）**：`agent chain` **不静默重锚** —— 锚与链不符时**刻意不动锚**，须显式 `--reanchor`；否则攻击者重写链后跑一次 chain 就把痕迹抹平。**⑤ 实测 12 项**（六项主判据 + 六项追加）：外部锚 4 类齐报；**#8 篡改后跑 chain 锚哈希前后相同**（信号保住）；**#9 门禁 FAIL 路径自证**（篡改归档件 ⇒ `[FAIL] ✕ evidence` + 明细 `[66] paper/… digest 不符, 变了: agent-output.txt` + rc=1；恢复回 PASS）；**#10 钩子自动入链端到端**（造 1 个假 run ⇒ 钩子前 `▲ 覆盖缺口: 1 个 run 尚未入链`，钩子后 **67→68、未入链 0、锚 entries=68**）；**#11 清理后重建自证**（删假 run+删链 ⇒ 回 67 条，head digest **复现 `83486e3b767c15ad`**，零残留）；**#12 `rpc.ps1` 编辑安全**（该文件有"剥 BOM → PS5 崩"前科 ⇒ 改前备份、改后验 **`EF BB BF` 保住** + PowerShell **AST 0 错误**）。**⑥ 门禁**：全量 **PASS，绿灯 15 / 黄灯 1 / 红灯 0**（黄灯仍是已登记的 A 站插件漂移）。**⑦ 明确不宣称/不做**：外部锚**需 `git push` 到 origin 才成立**（只提交≈仍在本地），持推送凭据者可强推 ⇒ 仅"**多副本见证**"强度，**非密码学不可否认**；本机冷路径仍与链同盘（防"顺手改一处"，防不了"知道要改三处的人"）；**派发后即时入链**需改 `agent-cli.ps1` 关键路径（无活模型/活站无法端到端验证）⇒ 明确不做，窗口由"钩子入链+门禁报缺口"兜住（**存在但可见**）；真 T2 级锚（外部介质/RFC3161/独立见证者）未做。**⑧ 顺带观察（未修，非本次范围）**：`Install-HookEntry` 的"仅在非 rpc 钩子时备份"判断用了 `Get-Content -Raw` + 中文匹配，而钩子文件是无 BOM UTF-8 ⇒ PS5.1 按 ANSI 读成乱码 ⇒ 判据恒真、每次都覆盖 `.bak`（无害但非本意）。**落档**：`evidence-chain/DESIGN.md`（§3 三级锚+强度诚实 / §4 文件表 / §5 追加 6 项实测 / §7 遗留表已闭环）+ `.gitignore` + TRACKER v2.7。**⑨ 落地（用户令「执行吧」）**：提交 `5008e92`（feat(d6)+docs(rocm)，11 文件 +1080 行）并 **push 到 `origin/main`（GitHub）** ⇒ **外部锚首次真正生效**（此前仅本地单点）；pre-commit 钩子自动入链（未入链 0）+ pre-push 全量门禁 **PASS（绿灯 15 / 黄灯 1 / 红灯 0）**。注意事项：`main` 是**唯一分支**（仓库既有工作流即直接提交 main），推送同时发布了此前未推的 `67f6b1d` |
| 2026-09-17 | **agent 证据链落地（v2.6）——「归档证据事后被改」可断链定位** | 承接《协同篡改威胁补充调研》§6.1，用户令「基于 6.1 做代码层面落档」→「执行」。**① 定位**：证据链实现落在**统一管理入口** `ops/cluster.py` 的 `agent` 子命令（符合 ADR-0004「唯一管理面」）；**`agent-cli.ps1` 与站上行为零改动**（亦避开该 .ps1 的 BOM 陷阱）。**② 设计纠正（取证推翻初稿）**：DESIGN 初稿把「`goldenSha` 只在 fused 脚本内存、须补落盘」列为**关键实现坑**；实现期 grep 发现 **ADR-0005 已把它落进 `.agent-run.json` 的 `accept_golden.sha256`**（agent-cli.ps1 L1263）⇒ 该坑**已关闭**，故**只哈希 `.agent-run.json` 字节**即覆盖之，不另抽字段（同一事实不设两个定义点）。教训：**开工前先 grep 目标字段是否已存在**。**③ 实现**：`recipe v1 = sha256("v1\n" + 逐件 "name:hex|-\n")`，入链 6 件（`.agent-run.json` / `judgment-record.txt` / `agent-output.txt` / `accept-output.txt` / `accept-golden-output.txt` / `prompt.txt`）；`prev` 链接**入链次序**（append-only 日志序，非 ts 序 ⇒ 并发派发不乱链）；缺件记 `-` 且**不得当告警**（老 run 属正常）。新增命令 `cluster.py agent chain`（幂等补录，**唯一写动作** = 链文件 + 冷路径镜像）与 `agent verify`（重算 digest + 验 prev 链 + 比冷路径，输出 gap 表不打分，有 gap 则 rc=1）。**④ 实测六项 + 一项边界自证**：链长 **67**；`verify` PASS；幂等（重跑无新增）；**T1 负向** —— head run 的 `agent-output.txt` 追加 1 字节（46→47）⇒ `✗ [66] digest_mismatch 变了: agent-output.txt`（**精确到条与件**）；**T2 负向** —— 改链本体第 1 条 digest ⇒ `[0] digest_mismatch` + `[1] chain_break` + `cold_mismatch` 三项齐报；**恢复自证** —— 删链后由还原字节重建，head digest **精确复现原值 `83486e3b767c15ad`**（证明 digest 确定性 + 备份恢复字节无误）；**边界实证** —— 完整协同篡改（改证据 + 重算 digest + 同步改链与冷路径）⇒ `verify` **PASS**，即 §3/§6 声明的物理上限**被实测出来而非写在纸上**。**⑤ 门禁**：`ops\rpc.ps1 check` → **PASS，绿灯 14 / 黄灯 1 / 红灯 0**（唯一黄灯 = 已登记的 A 站 `claude-plugins-official` 插件漂移，与本改动无关）；同时修掉一处自己早前文档的坏链接（`docs/research/2026-09-17_…协同篡改…md:64` 前缀写重 `../docs/research`）。**⑥ 明确不宣称**：不防"证据+链+冷路径"完全协同篡改、不防 T3、不防"提前不记录"、不证 judgement 级真伪（判据真的跑过且结果为 0）。**遗留（未做，已登记）**：①`agent verify` 未接入门禁；②`chain` 无自动触发点（手动/脚本调用；窗口内现网无第三方写入者）；③冷路径仍在同机同盘 ⇒ **非跨信任域锚**，T2 级防护需把链头导出到主控之外。**落档**：`spec/d6-agent-standard/evidence-chain/DESIGN.md`（§2.2 纠正 / §2.3 以代码为准 / §4 实际改动 / §5 实测表 / §7 用法+后续）+ TRACKER v2.6 |
| 2026-09-17 | **C 站统一 7.2.4 —— 三站 ROCm 全部闭环（v2.5）** | 用户令「执行清理并统一」，承接 v2.3-B/v2.4-A，**对 C 站统一 7.2.4（C 无系统 ROCm，无需清理）**。**① 源现状**：C 站 rocm/amdgpu/graphics 源全在 `backup-radeon-20260908/` 备份子目录（**未激活**，主 sources.list.d 无 rocm 源 → apt-cache 空）；keyring `/etc/apt/keyrings/rocm.gpg` 在、`rocm/apt/7.2.4` 仓库 200 可达。**② 执行**（略异 A/B）：**直接新建** `/etc/apt/sources.list.d/rocm.list` 指向 `rocm/apt/7.2.4`（不动 backup 历史目录）→ `apt update` → dry-run 确认无内核/mesa/drm 变更 → `apt-get install rocm-hip-runtime` → rocm-core 7.2.4.70204、rocm-llvm 22.0.0、hsa-rocr 1.18.0、`gfx1151` 枚举命中。**③ 验证**：遮蔽自证 PASS（`$ORIGIN` + 三 HIP lib ldd 全 bundled 未被系统抢占）；**双路径冒烟**——qwen3.8-27b-mtp（C 站既有 conf 指定 Vulkan）→ READY ✓ :18080 + `--device Vulkan0`（证明 **Vulkan 现役推理未受影响**）；**gpt-oss-120b（unsloth/HIP）→ READY ✓ :8080 + `--device ROCm0` + 端口 401**（证明 **HIP/ROCm 路径端到端正常**）。**④ ldconfig**：`so.7` 未全局注册 = 三站共性（runtime 安装，A/B 一致）。**⑤ 达成**：**三站（B/C/A）系统 ROCm 全部统一 7.2.4**；amdgpu-install 安装器版本仍三站漂移（A 6.4.6/B 6.3.6/C 30.3.1）但**非目标**（目标 = ROCm 软件栈统一，已实现；§7 判定 #3 更正为「不适用」）。**⑥ 验收（用户令「执行验收」）**：门禁全量 → **PASS，绿灯 14 / 黄灯 1 / 红灯 0**——backend 断言确认单站=HIP/分布式=Vulkan、bundled `so.7.16.26332` 不受影响（系统 7.2.4 未遮蔽）；唯一黄灯 stations（A 站 claude-plugins-official）系 inventory 已登记漂移，不阻断。**§7 验收清单 1/2/4/5/6/7 全绿**，仅 #3 判定「不适用」、#8 可选回滚演练未做。**⑦ 收尾（用户令「清理 C 站 backup 旧源目录」）**：`/etc/apt/sources.list.d/backup-radeon-20260908/`（含 amdgpu.list/amdgpu-proprietary.list/rocm.list 旧源）已删除——先核查**无外部引用**（apt 只读 sources.list.d 顶层 .list/.sources，子目录不读取）→ tar 备份到 `~/backups/backup-radeon-20260908-20260917-143139.tar.gz`（469B）→ `rm -rf` → 验证目录消失 + `apt update` 正常 + 候选仍 7.2.4。**遗留（已全部闭环）**。**落档**：DESIGN §3.3 C 站 + §7 验收清单全部更新 + §8 边界去除「不卸载 A 6.4.1」（已执行）；TRACKER v2.5。 |
| 2026-09-17 | **A 站 ROCm 6.4.1 清理 + 与 B 统一 7.2.4（v2.4）** | 用户令「落档结论，然后 A 站先清理再与 B 统一」。**① A 站 6.4.1 真清理**：实证 **`amdgpu-uninstall --rocmrelease=6.4.1` 是"假卸载"**——它软链 amdgpu-install（图形驱动卸载器），只 purge 驱动 BASE + 源清理，**不卸 ROCm 软件栈**（两次运行 END rc=0 但 55+ ROCm 包全在、`/opt/rocm-6.4.1` 仍在；判据坑：END rc=0 ≠ 成功）。改走 **`apt-get purge` 63 包**（含 rocm-core/hip-runtime-amd/hsa-rocr/rocblas/comgr/miopen/rccl 等；dry-run 干净无系统关键包），END rc=0 → 判据落实：`/opt/rocm*` 全消失、`so.6` 无、残留目录 `rm -rf` 清理。**② A 站统一 7.2.4**（按 B 站 §3.3 已验证方法）：修源自 amdgpu/6.4.1 + rocm/6.4.1 → amdgpu `.disabled` + rocm `.list` 改指 7.2.4（备份 `.bak-rocm72-*`）→ `apt-get install rocm-hip-runtime` → **rocm-core 7.2.4.70204 / hip-runtime-amd 7.2.53211 / hsa-rocr 1.18.0 / rocm-llvm 22.0.0**，`gfx1151` 枚举命中（Agent2 = gfx1151, Radeon 8060S），遮蔽自证 PASS（`$ORIGIN` RUNPATH + ldd 三 HIP lib 全 bundled），端到端冒烟 `infer-load gpt-oss-120b`（A 本地）→ READY ✓ :8080 + 进程 `--device ROCm0` + 端口 200。**③ ldconfig 注记**：A/B 两站 `libamdhip64.so.7` 均**未全局注册**（A 仅系统级 libhsa 引用）—— 属 runtime 安装共性（A/B 一致），ROCm 工具走自身 RUNPATH，遮蔽自证已证 bundled 不受影响，故非异常。**④ 达成**：A 站从"仅 A 有系统 ROCm 6.4.1"→"A 与 B 同 7.2.4"；`amdgpu-install` 漂移面收窄（A/B 现均装 rocm-hip-runtime；安装器版本 A `6.4.60401` 未变但 ROCm 栈已统一 7.2.4）。**落档**：DESIGN §3.3/§4 标注 A 站实测；TRACKER v2.4。**待办**：C 站源 `rocm/apt/7.2.1` 仍偏移 7.2.4，未动（本次范围 B→A） |
| 2026-09-17 | **三站 ROCm：B 站试点装 7.2.4 全绿（v2.3）** | 按「仅 B 站装 7.2.4」执行。**① 安装器版本判据修正**：B 站 amdgpu-install 6.3.60303 的 `--rocmrelease` 只认本站源版本段，**不能跨站统一到 7.2.4**；官方 `amdgpu-install/7.2.4` 目录也不存在（安装器 30.x/31.x 与 ROCm 7.2.x 非同编号）⇒ 单命令无法跨站统一（决策分歧，经 AskUserQuestion 定为「仅 B 站装 7.2.4」）。**② 方法发现**：无需升级安装器，**直连 apt 装 rocm-hip-runtime**。前置=修复 B 站 apt 源体制：rocm/7.2.4 与 amdgpu/6.3.3 并存 + `.distUpgrade` 后缀导致 apt 偶发忽略 rocm 源（apt-cache 空但 curl 有）→ 备份→禁用 amdgpu(.disabled，仅 dkms 与 in-tree/--no-dkms 冲突)→rocm 改名 `.list`→单源 update 稳定收录。**③ 装后验证全绿**：rocm-core 7.2.4.70204、hip-runtime-amd 7.2.53211、hsa-rocr 1.18.0、**gfx1151 枚举命中**、遮蔽自证 PASS（ldd 三 HIP lib 全 bundled 未被系统抢占）、`infer-load gpt-oss-20b`→READY✓+`--device ROCm0`、冒烟后环境回收干净。**④ rocm-hip-sdk 为待评估项**（hipcc 编译能力，未装）。**落档**：DESIGN §3.3 B 站试点 + §4 标注实测；TRACKER v2.3。**下一步**：A 站先清理 6.4.1 再与 B 统一 |
| 2026-09-17 | **三站系统 ROCm 统一：前置探测实证（v2.2）** | DESIGN §3.0 前置探测（零风险，三站 curl/`--help`/`apt-cache` 实查）**把设计假设落实证**：**① `--rocmrelease=` 三站 amdgpu-install 均支持**（D4 pin 方式成立）；**② 7.2.4 rocm/apt 源三站可解析（HTTP 200）**（「7.2.4 仓库可用」成立）；**③ `amdgpu/7.2.4/ubuntu`=404 但 `rocm/apt/7.2.4`=200** ⇒ 7.x amdgpu(内核)源不同命名 → **`--no-dkms` 第二重实证**；**④ `rocm-hip-sdk`/`hip-dev` 存在于 7.2.4 源**（Packages.gz 检索，D3 成立）；**⑤ `--usecase` help 样例仅 graphics/workstation** 但 `rocm` 实装成立（§3.2 注记要求装前 `--help` 核定已满足）。**网络**：A/B/C 均 200；实装 A 6.4.1 / B、C 无。C 站一次 ssh 抖动（empty）重连正常，非配置问题。**结论：设计可执行性成立，无阻塞**。**落档**：DESIGN §3.0 标记 ✅ 已实证 + §3.1 备份名 `pre714`→`pre72` 校正；**下一步**达 B 站试装（备份→装→遮蔽自证→冒烟），属不可逆破坏性操作待裁决 |
| 2026-09-17 | **三站系统 ROCm 统一：版本纠偏 + 执行设计落档（v2.1）** | **① 版本号重大纠偏（判据教训）**：v2.0 统一目标「7.14.0」**在 apt 仓库不存在** —— `curl repo.radeon.com/rocm/apt/` 实测 7.x 最高 = **7.2.4**（`latest` 即 7.2.4）；v2.0 的「7.14.0 兼容矩阵列 gfx1151」基于**官方产品/release 号**，与 apt 仓库版本段不同表。官方 docs-7.2 native_linux 矩阵（本轮查证）正式列 gfx1151（Ryzen AI Max+ 395/Radeon 8060S/RDNA3.5/U24.04）⇒ **统一目标更正为 7.2.4**。教训：版本号必须以 apt 仓库真实存在为准，pin 前必须 `curl` 仓库根目录清单确认目录存在。**② 前置探测实测**：A 实装 rocm-core 6.4.1（`so.6`）；B/C 无系统 ROCm；三站 source 段 A `6.4.1`/B `6.3.3`/C `7.2.1`（C 需 pin）；网络可达 repo.radeon.com 200；遮蔽判据（§9.7-③）三站成立不因版本号改变。**③ 执行设计落档**：`spec/rocm-migration/DESIGN.md`（装 rocm-hip-sdk + `--no-dkms` + `--rocmrelease 7.2.4` pin + 备份/回滚 + 装后遮蔽自证判据 + 验收清单 8 项 + 审计注记 4 条）；执行顺序 B→C→A。**④ 遗留**：3.0 前置 `apt-cache policy rocm-core` 确认真实候选后，待裁决进入实际安装。**落档**：DESIGN.md + TRACKER v2.1 |
| 2026-09-17 | **三站系统 ROCm 统一：遮蔽判据探测（v2.0）** | 用户三选一「统一安装/移除 B 站 ROCm/维持现状」倾向统一，先做版本选型 + 装前零风险遮蔽判据。**① 本体定位修正**：实测 **B/C 无系统 ROCm**（`/opt/rocm` 不存在、dpkg 零包），「移除 B 站 ROCm」不可执行 —— 唯一系统 ROCm 在 **A 站（6.4.1，`so.6`）**，且**进程级引用审计零命中**（全 `/proc/*/maps` 扫 `/opt/rocm` 零、现役推理进程未跑、infer-load/systemd 仅注释文本）＝闲置可安全移除，但对统一方向无冲突。**② 官方版本矩阵**：**ROCm 7.14.0（7/16）兼容矩阵正式列 gfx1151（Ryzen AI Max+ 395/Radeon 8060S/RDNA3.5/U24.04.4 HWE 6.17）**，TheRock 模块化 + AI inference 重点；**6.4.4 仅「初始/Preview」**（PyTorch 2.8 单档）⇒ **统一目标 = 7.14.0**（正式 + 模块化 + 与 bundled `so.7` 同代）。**③ 遮蔽判据探测（三站一致，零风险）**：bundled `llama-server` `RUNPATH=[$ORIGIN]`，`ldd` 对 `libamdhip64.so.7`/`libggml-hip.so.0`/`libhipblas.so.3` 全命中 bundled ⇒ `$ORIGIN` 先于 `ldconfig` 缓存 ⇒ 装 7.14 注册 `so.7` 不遮蔽 bundled，**零风险**；A 站 `so.6` 与 bundled `so.7` 不同 SONAME 天然不冲突。**判定：装 7.14.0 对现役推理零影响**。**遗留**：完整执行设计未写（装 rocm-hip-sdk + `--no-dkms` + amdgpu-install 版本 pin + 备份/回滚 + 装后自证判据），待裁决后动。**落档**：调研文档新增 **§9.7**；顺带更新 §9.4 表（gfx1151 已删、A 6.4.1 零引用） |
| 2026-09-17 | **非 Qwen 单机线统一 + C 站 gfx1151 移除 + flash-next 引擎修正（v1.9）** | 沿用 §2.8 完整落档。**① 非 Qwen 单机线统一**：C 站 `gpt-oss-120b` 由 `BACKEND=llama-single` + `LLAMA_SERVER_BIN=~/Applications/llama-gfx1151/llama-server`（唯一非 Qwen 单机线偏离）→ 改为 `BACKEND=unsloth`（HIP/ROCm，与 A/B 三站同构）；`.bak-unify-20260917` 备份原样。**② 单一结论先行**：因 Qwen 系引擎**内部分化**（`qwen3` 架构 mtp/davidau 由 `~/llama.cpp/build` 载、`qwen3.8-flash-next` 为 `qwen4exp` 架构由 `/opt/llama.cpp` b1533 载），**单机线无法一刀切统一** ⇒ 只统一非 Qwen 线。**③ C 站 gfx1151（`~/Applications/llama-gfx1151`，1.7G/381 文件，HIP ROCm 7.14.60850）移除**：判据先行——引用面**穷举扫描**（A/B 无该目录；systemd/opt/bin 零引用）唯一活引用 = **C 站 flash-next env**；引擎架构实测 `libllama`：gfx1151 `qwen4exp=0`（本就不支持 flash-next）⇒ 删除无损。备份 `tar czf /home/scott-lau/backups/llama-gfx1151-20260917.tar.gz`（466MB/388 条目）+ conf 双备份，`rm -rf` 后站上零活引用 `GFX_GONE`。**④ flash-next 引擎修正（三次翻转定案）**：flash-next 原本也 pin 到 gfx1151（载不动）；先 pin `~/llama.cpp/build`（0d18aaa）→ 试载**失败**`unknown model architecture: 'qwen4exp'`（0d18aaa 不支持 qwen4exp，libllama `qwen4exp=0`）→ 最终改为 **`/opt/llama.cpp/llama-server`**（b1533，libllama `qwen4exp=93` 支持）→ 试载 **`READY ✓ :8080` / health `http_200`**，进程确认 `/opt/llama.cpp/llama-server`。**⑤ 判据教训**：旧硬约束「Qwen3.8 只能 0d18aaa」**对 flash-next 不成立**（我两次 pin 0d18aaa 基于错误认知）；引擎架构支持**不能用模型名经验映射**（`qwen35`），必须对**目标 GGUF 真 arch 串**（`qwen4exp`）检测，且 arch 表在 **`libllama.so` 动态库**（不在 launcher/`strings`），**最可靠判据 = 实际试载**（journal 的 `unknown model architecture` 为铁证）。**⑥ 备份铁律补充**：`/etc` 下 root 所有文件 `cp` 无 sudo **静默失败** ⇒ `/etc` 备份必须 `sudo`（或 base64+sudo tee）+ size 校验。**遗留（非本次）**：跨站 `nodes.env` / `rpc-server@` 单工位+端口写死 50052 僵化、flash-next B 站 RPC(Vulkan/opt) vs C 站单机(/opt) 语义差异待处置 |
| 2026-09-17 | **`backend` 断言进门禁（v1.8）+ 后端清减 + 补 C 站回滚基线** | **① 门禁 14 → 15 项**：`ops/rpc_check.py` 新增 `backend`（"引擎后端与回滚基线"，`quick=False`）。**为什么**：原 14 项**没有任何一项判定"HIP 还是 Vulkan"**（`engine` 只查进程残留/内存，`stations` 只对账 conf/端口/凭据/插件），以致台账 §1 曾整片按"本集群 = Vulkan"写错而门禁全绿。**四条判据全落二进制**：单站引擎 `ldd` 含 `libggml-hip.so` / `/opt` 两件含 `libggml-vulkan.so` / 三站 studio 自带 ROCm 版本串一致（取不到时**不判**只告警）/ 回滚基线 `9859` 在位。**自证**：正 → `PASS · 单站=HIP / 分布式=Vulkan · ROCm libamdhip64.so.7.16.26332`；**真实负向** → 加入当天即抓到 **C 站基线真缺失**（FAIL）；**合成负向 4/4** → 逐条注入故障，**各自专属文案**均命中（`tmp/_negtest_backend.py`）。**② 补 C 站 `/opt/llama.cpp-9859`**：A 站打包（31,215,143 B / md5 `a0c20e05b2aacf805ea5ba4d964ef4d7`）→ 主控中转 → C；**包 md5 + 文件清单 hash `ce4d55174452` + MANIFEST 61/61 三重校验通过**，所有权正确，`RUNPATH=$ORIGIN` ⇒ 无需 patchelf。**③ 清减**（判据先行：路径白名单守卫 + 非现役 symlink + MANIFEST 全过才删 + 备份可解性自证）：三站删 `/opt/llama.cpp-master-d2e206c4`（110 文件 / 79 全过；tar 备份 27M/站）；A 删 `~/build/llama.cpp.old`（1.5G，git `5d3a4a7`，其 `llama-server` 已损坏）；B 删 v0.2.0 三项（1.3G+168M+33M，上游 tag 可得）；B 两个引用已删路径的 `.bak` conf **移出活动配置目录**（不删）。**验收**：三站 `/opt` 均恰为规则要求的 **2 个目录**；删后引用扫描三站零活引用；`cluster.py versions` 三站 `79 OK`；B 站 `infer-load gpt-oss-20b` 端到端冒烟 PASS。**④ ⚠ 一处不可逆损失（如实留档）**：A 站 `~/Applications/llama-gfx1151` 残件按「零可执行件 + 零引用」删除后，与 C 站交叉印证发现它**并非 C 站的同构子集**（A 382 文件 vs C 381 = 380 + `llama-server`；清单 hash 也不同）⇒ 那 2 个文件身份未知且**未留副本**。**判据强度不足**（"判据本身要先自证"**第五次复发，本次造成实际损失**）；影响面复验：A 站对该路径**零引用** ⇒ **零功能影响**，核心构建件 md5 与 C 站一致，C 站完整件保留；证据文件已追加"事后审计修正"注记（`/data/backup/llama-cleanup-20260917/`）。**⑤ 顺带登记既有站间漂移**：内核 A `6.17.0-40` vs B/C `6.17.0-23`（与"HIP 6.x-vs-7.x 对照实验"耦合，实验前须锁死）；LM Studio C `0.4.14+4` 落后于 A/B 的 `0.4.19+2`。**同步回写**：`UPGRADE_SOP §6`（回滚基线由门禁守护 + 收尾必跑）+ 手册门禁块（14→15 项）+ `ops/rpc.ps1` 用法行。**⑥ 过程中我自己踩到并当场修复的一处工具链坑（如实记录）**：编辑 `ops/rpc.ps1` 时**剥掉了它的 UTF-8 BOM** ⇒ PS5 按 ANSI 解码其中文致解析崩（`The string is missing the terminator: '@`，行号还错位 3）。**已恢复 BOM 并用 PowerShell AST 解析器独立复核「0 错误」**；全仓 **12 个 `.ps1` 普查零同类隐患**。**并做了负向自证**：临时再次剥掉 BOM → `syntax` 断言 **FAIL（exit 1，`.ps1:12文件/2失败`）** ⇒ **门禁本就有覆盖**（`git hook` 直接调 `rpc_check.py`、不经 `rpc.ps1`），本次只是**手动走包装器**时先崩，**不是门禁漏洞**——但这条坑会反复复发（见项目记忆已记的同类条目），故留档。**⑦ 本轮取证结论回写（用户令）**：**TRACKER §2.6** 增补 ①「**ROCm 版本矩阵**」—— `~/.unsloth/llama.cpp/build/bin/libamdhip64.so.7.16.26332`（**现役主路径**）/ studio python 栈 `rocm_sdk_core`+torch+triton **7.13.0** / C 站 gfx1151 **7.14.60850** / A 站系统 **6.4.1**（**零推理依赖**：`/opt/rocm` 引用反查 = 0、gfx1151 构建 ldd 依赖 = 0）；②「**系统 ROCm 安装面事实**」—— `amdgpu` in-tree + DKMS 目录空（未装 `amdgpu-dkms`，属**必须保持**的状态）/ 仅 `10-rocm-opencl.conf` + `LD_LIBRARY_PATH` 空（隔离干净）/ **soname：6.4.x = `so.6` vs 7.x = `so.7`，而 bundled 恰是 `so.7`** / `amdgpu-install` 三站漂移（A `6.4.60401`、B `6.3.60303`、**C `30.30.1.0.30300100`** = ROCm 7.x 时代遗留）；③ 定性「**没有任何现役推理路径跑在 6.x 上**：系统 ROCm 只是构建工具链 + 诊断」。**调研文档升 v3**：新增 **§9「ROCm 6.x vs 7.x on gfx1151 专项调研」**（§9.1 结论先行 / §9.2 **已证实 A–L 与未证实 M–Q 分列**、含官方出处 / §9.3 安装风险 / §9.4 现状对照 / §9.5 受控对照实验设计（含关键陷阱：studio b10715 vs 自建 b1533 会把**引擎版本与 ROCm 版本混在一起**）/ §9.6 建议 5 条），§8 补 ROCm 一手来源 13 条 + 二手 4 条，§0 / §6 / §7 加交叉指引 |
| 2026-09-17 | **遗留 1+2 闭环（v1.7）：悬空 `LLAMA_SERVER_PATH` + `infer-load` 注释/注入** | **背景**：该 `export` 原为 2026-06 前后"把 studio 顶到 Vulkan 引擎（不再走自身 ROCm 后端）"的**故意手段**（`spec/d6-agent-standard/unsloth-a-station.md` §509/531 明记"写入 .bashrc+.profile"）；b10715 目录 9/8 清除后意图已废 ⇒ 变成"**pin 到不存在路径**"。studio 对该变量是 **pinned 语义**（源码注释：`a deliberate pin… must not silently fall through`），但 `os.path.exists=False` 时会静默落到自带引擎 ⇒ **当时无任何可见故障**；真风险是"哪天有人重建该目录"⇒ A/B（有 profile export）与 C（无）**静默分叉到不同引擎**，且无门禁可见。**改动**：① 删 A/B 站 `.bashrc` / `.profile` 的 export（A `.bashrc` 156→155 / B 152→151，`.profile` 35→34 / 34→33；备份 `*.bak-20260917-llamaserverpath`）；② `ops/station-bin/infer-load` 头部注释改为实测口径 + **移除原 `:212` 注入**（三站部署 md5 统一 `e474b761198dde596b86ab0edfad19be`；旧件备份 md5 `d57a9021…`，14498B）。**判据（自证，非纸面）**：profile 用 `diff` 断言"恰好删 1 行且被删行 == 备份内容"（负向：命中数 ≠1 即中止不改，C 站天然为 0 命中对照）；`infer-load` 语法自证通过后才 install；"残留"判据为 `^[^#]*LLAMA_SERVER_PATH`（**注释里的历史说明不算残留** —— 首版判据在此踩了假阳性）；端到端 B 站实跑 → rc=0 / 6s / `READY ✓ :8080`，派生进程 exe = `~/.unsloth/llama.cpp/build/bin/llama-server`、maps `libggml-hip.so.0`、`--device ROCm0`、**`/proc/<pid>/environ` 已无该变量**、`/v1/models` 200、unload 后占用 0.0G。**执行脚本首跑暴露我自己的 3 处判据缺陷**（`grep -c … \|\| echo 0` 产出 `"0\n0"`；残留假阳性；备份名固定致重跑覆盖最初备份）—— **"判据本身要先自证"第四次复发**，已修并记入 §2.6。**同步回写**：`docs/三机推理集群使用手册.md` 引擎真值表（原写 `LLAMA_SERVER_PATH → ~/llama.cpp-vulkan-b10715`，属真漂移）、`ops/station-bin/README.md`（引擎说明 + 三站一致性 md5 由 `fb7df75c…` 更新为 `e474b761…`） |
| 2026-09-17 | **后端口径订正（v1.6）+ 新建 §2.6「现役 GPU 后端矩阵」** | 用户指出两处：① §1 的「对本集群影响」**未按现役框架更新**；② 「C 站 unsloth 用 HIP/ROCm、A/B 似亦有、**单站加载疑似默认走 HIP/ROCm**」—— 要求**从统一管理入口分析确认**。**取证**（统一入口 + 落到二进制级）：`cluster.py versions\|models meta`、`station-bin/{infer-load,llama-serve-instance,rpc-nodes}` 决策链、三站 `ldd`/`--list-devices`/`/proc/*/maps`/`systemctl cat rpc-server@*`/`*.env`/`~/.unsloth/run-*.log`。**结论：用户判断正确，此前"纯 Vulkan / 无 ROCm"写法错误** —— 集群是**双后端并存**：**① 默认单站加载 = HIP/ROCm**（`infer-load` 缺省 `BACKEND=unsloth` → studio 自带 `~/.unsloth/llama.cpp/build/bin/llama-server`，0.3.0-dev b10715/92cedc867，`ROCm0: AMD Radeon 8060S`，`libggml-hip.so.0`）**三站同构**；**② 分布式/RPC = Vulkan**（master `/opt/llama.cpp/llama-server` + worker `/opt/llama.cpp/ggml-rpc-server`，三站 ldd 均 `libggml-vulkan.so.0`+`libvulkan.so.1`；`nodes.env` 仅 master 站 B 有 = `10.10.10.1:50052 10.10.11.3:50052`）；**③ 系统 ROCm 仅 A 站 6.4.1**，B/C 无系统 ROCm、靠 studio 自带 bundled ROCm（23 库）；**④ C 站 conf 现役引用 `~/Applications/llama-gfx1151`（HIP）** ⇒ **非"残留/清理候选"**。**改动**：新建 **§2.6**（后端矩阵 + 判定 + 3 条顺带实测）；订正 **§1.1 #28282 / §1.2b #25452 / §1.4 #27825 / §1.4 判定 / EV-2 / EV-3** 共 6 处「对本集群影响」；修正 **§2.0**（`--device Vulkan0` → `ROCm0`）、**§2.3**（`~/llama.cpp-vulkan-b10715` 已删；`~/App/llama-gfx1151` → `~/Applications/…` 且为**现役**）、**§2.5**（散置构建处置）三处目录漂移；§2.3 结尾陈旧 P0 标 **✅ 已闭环**。**遗留（未动，待裁决）**：`LLAMA_SERVER_PATH` 悬空 export（A/B `.bashrc`+`.profile`；悬空但当前无影响）+ `infer-load:3` 注释与实测不符 + 门禁是否加后端断言 |
| 2026-09-16 | **CIRU StrixLink 取证修正（v1.5，v2）—— 用户给出出处后推翻 v1 的"未确认"** | 用户提供 `jcbtc/GLM5.3-Flash-CIRU-STRIX-IU4`（"获取 CIRU StrixLink 及相关资源的首要入口…含执行就绪的模型权重、定制 ROCm 运行时、编译好的内核与服务安装程序"）⇒ 经 **`hf-mirror.com`** 实取原页（`api/models` + `tree?recursive` + `raw/README.md` + `raw/runtime/packages/README.md`）。**核实结果**：① **它就是"两台 Strix Halo 跑 GLM-5.3-Flash 320B"的 TP=2 部署包**（README 首句"320 billion parameters. Two Strix Halo PCs. Entirely local inference."）；② **103 文件 / 166.72 GiB**，权重**已按 `rank-0`/`rank-1` 两机切好**（各 ~83.25 GiB）；③ 含 **vLLM/ROCm 10 wheel（cp314）+ AITER + gfx1151 专用内核（`iu4-m1`/`dense-kda`/`m4-residual`/`top8-epilogue`/`m8-align`/`resident-g128`）+ `INSTALL-RUNTIME.sh` + `runtime/generation-frontend/`（:8083）**；④ **`ciru-strixlink` 工具 5 个版本（0.2.0…0.3.3）**，职责="配置/检查/准备两机连接"（**EV-6 闭环**）；⑤ **实测**：decode **23.686 t/s**、402.455 prompt t/s、TTFT 5.089 s、draft acceptance 56.6%（64K+NHI+DFlash2 k7）；量化质量 PPL **2.1486→2.2612（+5.24%）**、top-token 一致率 **91.92%**；⑥ **USB4 直连 NHI all-reduce 组件门 110 µs vs RCCL socket 341 µs（低 67.5–67.9%，bit-exact）**；⑦ 要求 **ROCm 10.0.0 + PyTorch 2.13 + Python 3.14.3 + TileLang 0.1.10**，wheel 为 **Ubuntu 24.04 构建**（非 manylinux），**Direct-NHI 需 USB4STREAM/NHI 内核（实测 NixOS+Linux 7.2.2）**。**方法论修正**：**v1 判"三轮检索未命中 ⇒ 未确认"是假阴性** —— 真因是**主控/B 站直连 `huggingface.co` 超时**（HF 在本地网络不可达），而**站上 `hf-mirror.com` 可达** ⇒ 新增 **EV-7** 并把"判不存在前先证通道可达"写入纪律。**台账改动**：**EV-3 由"单站试点"改为"双机部署评估"**（该方案不可单机跑）、EV-4 补"性价比低于 EV-3"、EV-5 补"USB4 TP=2 旁路"、EV-6 闭环、新增 EV-7 |
| 2026-09-16 | **建 §1.5「待评估事项」动作队列（v1.4）+ 分布式路线可行性调研落档** | 用户要求把上轮两个待定动作"列入待评估事项"，并评估 **CIRU StrixLink 部署 GLM-5.3-Flash** 与 **引入 Skulk 框架** 的可行性（动机：**分布式 llama 对 V4-Flash / GLM-5.3-Flash 支持力度不够**）。**新建 §1.5 队列表 EV-1…EV-6**：EV-1 #28360 前置自测 / EV-2 #27825 纳入下次升级 / EV-3 **CIRU 单站试点** / EV-4 **Skulk 引入（先答"谁管生命周期"）** / EV-5 **100GbE RDMA 硬件** / EV-6 **"StrixLink" 取证（未确认）**。**调研结论**（[文档](../../docs/research/2026-09-16_分布式推理路线可行性调研_CIRU-Skulk-多机TP.md)）：**Skulk 已确认**（官方文档原页：AMD Linux = `skulk-llama-server-vulkan` wheel、引擎可注入 `SKULK_LLAMA_SERVER_BIN`、vLLM 仅 NVIDIA；但**后端就是 llama.cpp** ⇒ 不改上游事实，且其**常驻 supervised service** 与"零自加载 / load-gate / 看门狗禁用 / ADR-0004 唯一管理面"冲突）；**CIRU 部分确认**（gfx1151 原生 vLLM/ROCm 运行时发行；Ling-3.0-Flash 实测 0.269→**26.23 t/s（97.5×）**，关键收益来自"禁用 ROCm skinny-GEMM Wave32 病态分发"28.16×；**但 GLM-5.3 构建 `GLM5.3-Flash-CIRU-STRIX-IU4` 仅一处第三方索引页出现、0 runs ⇒ 未证实**，且**无任何多机声明**）；**"StrixLink" 三轮定向检索 + HF API 全未命中 ⇒ 不予评估（需用户提供出处）**；**检索另带出两条更该进队列的**：**vLLM+Ray+定制 RCCL+RoCE v2 多机 TP**（唯一改能力上限的路线，合并显存 ~248GB、RDMA 5µs 级，但**需 100GbE NIC（Intel E810）**，我们只有 USB4 ~9.4Gbps）与 **AMD 官方 playbook**（llama.cpp RPC 跑 GLM-4.7 358B ＝ 我们现路线，官方背书；瓶颈不在路线而在上游 PR 与物理链路） |
| 2026-09-16 | **新增 §1.4「分布式推理相关 PR 集中索引」（v1.3）** | 用户指出"V4-Flash 与 GLM-5.3-Flash 需要关注**分布式推理相关 PR**" ⇒ 核查发现：**V4 侧此前已有标注**（§1.2b 的「层级」列 + §1.2 行），**GLM 侧未标注**（散落在 §1.2 的 #28047 与 §1.1 结论文字里），且**两模型均缺 4 条关键条目**。集中补录 **#26490**（**V4 `-sm tensor` 主体，Merged 8/24** ⇒ 现役引擎已含；澄清"非 RPC 路径的 `-sm tensor` 早已可用，缺的只是 RPC 形态"）、**#27825**（**HIP AllReduce**，Merged 9/15，**现役不含** —— C 站 ROCm 张量并行的通信原语前置）、**#28360**（**GLM-5.3 的 RPC issue**，报告者第二机为 **GFX1151 同架构**，Open）、**#27311**（调度器 **UMA ring buffer**，49 评论、9/16 活跃）；并把 #28047/#26500（GLM 分布式前置已解除）、#26610（RPC `-sm tensor` 中枢）、#26384（**"RPC has no error recovery"**）、#27964（`-sm tensor` 在 Qwen 侧 `SPLIT_AXIS_UNKNOWN` abort）一并纳入；#26906 标低相关存档。**判定：两个真闸门 = #26610 与 #28360**。同时在 §1.1 结论、§1.2b 层级界定处加"分布式相关性"指针 |
| 2026-09-16 | **GLM-5.3-Flash + DS V4-Flash 上游状态核查（API fetch 原页，v1.2）** | **GLM**：#27754（unsloth 首选线）**43→45 commits（42 files）且 `mergeable_state=unstable` + 9/16 当日有活动** = **9/13 以来首个「接近可合并」信号**（仍未合入）；#27773 46c 未动 / #27752 11c / #27917 Draft 31c（三者均 `dirty`）；#27922 issue 仍 Open（实标题 "Feature Request: Support GLM5.3 (flash)"）。**新补录 3 条**：**#28282**（在 #27754 HEAD 上长 prefill `-ub 2048` 出现 CUDA 非法访存，`bug-unconfirmed`，更新 9/15 ⇒ 提示该线**长 prefill 侧未验证**）、**#28727 + #28729**（GLM indexer `soft_max gridDim.y` 溢出修复，**双双 closed 未合并**、无人承接）、**#28106**（`resolve_fused_ops` 只校验 device placement 不校验 tensor，8/31 创建、本次补录）。**判定**：维持等待 + **新增预警触发「#27754 转 `clean` 且 reviewer 批准 → 启动评估环境准备」**（不必等合并）+ 评估时**必须压测长 prefill**。**DS V4**：**#26610**（RPC `-sm tensor`）**9/16 仍有活动**（3 commits / 2 files；`mergeable_state` 本次惰性读作 `unknown`）⇒ 仍为**最可合并候选**；#25860 Draft 停滞（7/30）/ #28696 Draft（9/13 有触碰）/ #23122（8/1）/ #28569（9/8）均无实质变化；**新补录 #25452**（V4-Flash **SWA KV-cache 轮换复用耗尽 → crash/stall**，12 评论，创建 7/8、更新 9/15；**报告者环境为 CUDA 5 卡 ⇒ 后端相关性待自测甄别**）。**架构级无新 PR**（`deepseek4 in:title updated:>=2026-09-13` = 0） |
| 2026-09-13 | **后端 GLM-5.3-Flash + density gate PR 状态核查（GitHub API fetch 原页）** | **glm5next 仍未合入 master**，但三线推进有实质变化：#27773（timkhronos 文本+视觉）**41→46 commits（9/12，三线最活跃）**；#27754（unsloth 首选线）**41→43 commits（9/11）**；#27332（vulkan density gate）**1→2 commits（9/9）**；#27752 11 commits 未变（仅被触碰 9/11）；#27917 仍 Draft 31 commits（head 5b8593b）；#27922 issue 仍 Open。**DSV4 算子链全 merged 不变**（#26578 9/7 / #27970 / #28133 9/2 / #28047 9/5 由 #26500 修复）。**结论**：无关键合并信号 → GLM-5.3-Flash 维持等待，事件驱动前置「三变二」不变 |
| 2026-09-10 | **B 站 Q3.8F 两档清理：删 UD-Q4_K_XL（111.3G）+ GLM-5.3-Flash PR 状态核查** | **①B 站删除 `Qwen3.8-Flash-Next-UD-Q4_K_XL.gguf`（111,334,654,400 B / 104G 磁盘）**——该档超出单站（121G avail）安全余量（111.3G 权重 + KV 无富余，此前判定「单站无可部署」）；删除后磁盘 817G→923G 可用；保留 UD-IQ4_XS（93.7G，现役档，C→B 经 USB4 1.11GB/s 传输 + md5 三片一致）。**②GLM-5.3-Flash 五 PR fetch 原页核查（9/10）**：#27754 Open 41c 未变（前置 flag 更新：`NVIDIA_TF32_OVERRIDE=0`+`-fa off`）；#27752 Open **10→11c**（唯一变化）；#27773 Open 41c 未变；#27917 Draft 31c 未变（`--spec-type draft-mtp`）；#27922 Open（mmproj+文本双证 `unknown architecture 'glm5next'`）。**无合并信号 → GLM-5.3-Flash 维持等待** |
| 2026-09-10 | **C 站 unsloth(HIP) 双模型实测：M2.7 + Q3.8F 均 ✅** | C 站 `~/.unsloth/llama.cpp` b10715（HIP/ROCm0，无 Vulkan）实测：**M2.7 UD-IQ4_XS（108.4G）** 加载 40s，quality 正常，**tg 24.4-24.8**（load-gate need=107 恰好过）；**Q3.8F-Next UD-IQ4_XS（93.7G）** 加载 25s，中/英/多轮无乱码，稳态 **22.6-23.2 t/s**（need=94）。**D.5 结论修正**：Q3.8F 的 HIP 乱码限 **LM Studio 内置 ROCm 引擎**（疑含 #27621 回归），**unsloth b10715 HIP 实测无 #28113 MoE 数值 bug** → C 站 Q3.8F HIP 非全坏，unsloth b10715 即回避方案。思考治理：`--reasoning-effort medium --reasoning-budget 2000` 生效（reasoning 仅 20-245 tok）。⚠️ M2.7 是 CoT 模型，max_tokens 须 > 思考长度否则 content 空。详见 [MODEL-SOURCING D.6](../model-eval/MODEL-SOURCING-2026-09.md) |
| 2026-09-10 | **三站引擎升级 master-91f6a6cf + #26578 实测落地 ✅** | UPGRADE_SOP 六步：B worktree 构建 v0.4.0-dev build 1533（含 #26578 DSV4_HC + #27970 sparse-fa）→ MANIFEST 79 条 → tar 分发 A/C → **patchelf `$ORIGIN` 修正 RUNPATH**（cmake 产物硬编码 B 站 build 路径，不改 A/C 无法加载）→ 原子切换 symlink → `check_llama_version.py --deep` 三站指纹+79 文件全等 ✅。A/B 两机 V4-Flash（-sm layer 无 -ngl 99, 单文件 156G）decode **9.56→14.02 t/s（+46.7%）**，`resolve_fused_ops` HC 警告清零 → **#26578 升级闭环**。详见 metrics-log Phase 6。另修 `check_llama_version.py` C 站 IP（192.168.1.24→.37）。**load-gate 三站对齐 ✅**（A/B 补装 station-bin 同源，md5 6acec519 三站一致，60G 冒烟 OK）|
| 2026-09-10 | **算子 PR 动态复核（fetch 原页）** | **#26578 DSV4_HC 融合 → Merged 9/7**（decode 1.50×/prefill 1.12×，V4-Flash decode 提速最大单点入主线）；**#28047 RPC crash → Closed 9/5（#26500 修复）**，V4-Pro/GLM-5.3 分布式禁用前置解除；新增 #27970（sparse-fa 9/2 merged）；现役 /opt（d2e206c4 8/31）缺 sparse-fa+HC → **引擎升级 P0 强化**（升级即得 decode 1.50×）；GLM-5.3-Flash 三 PR 仍 Open（#27754 9/9 更新）未合入 |
| 2026-09-08 | 创建 | 首版：收拢 GLM5next 4PR + DSV4 算子 6 项 + gfx1151 波 2 项；引擎基线三套目录实测（现役=0d18aaa 系，/opt=v0.3.0 未用，b10715=文档误载）；发现 `~/App/llama-gfx1151` HIP 残留 |
| 2026-09-08 | 归位完成 | 三站分布式链路切回 `/opt/llama.cpp`（v0.3.0, rpc **v6.0.0**）；重建三站 MANIFEST（I3 首次成立，79 条全过）；C 站补装 /opt master；巡检脚本重建（`ops/check_llama_version.py`，paramiko 三站版）首跑全绿；回归后职责边界 §2.5 落地；绕 /opt 成因入档 |
| 2026-09-08 | V4-Flash 崩溃定案 | 两次 kernel panic（三站分片 + `-c 512` 最小验证均崩）；根因=**单文件 146G MXFP4 > 120G UMA 池 + `-ngl 99` 全 offload**，与 mmap/ctx/RPC 无关；完整分析见 [V4-Flash-0731加载崩溃根因分析](../../docs/V4-Flash-0731加载崩溃根因分析_20260908.md)；P0 修正=验证 MXFP4 Vulkan 支持→分片→加载监控 |
| 2026-09-08 | V4-Flash 四次崩溃定案 | 第 3/4 次（`-fit on` / `-dio`+分片）仍崩；社区调研证实 **Vulkan 后端在 Strix Halo UMA 不可行**（beowulf「只能用 ~43G」；成功案例全走 ROCm/HIP 或 CPU-only）→ **弃 Vulkan 路线，转 ROCm/HIP**（Lucebox 32t/s 同构）；文档 §3.3/3.4/4.1b/6/7 更新；恢复 B 18081 为 P0 |
| 2026-09-08 | 环网恢复（MAC 绑定根治） | B 站 thunderbolt-net 接口枚举漂移（thunderbolt0/1 名与物理口互换）致 10.10.x 全盲、RPC 退走 WiFi、无 rpccache；根因=netplan 按接口名绑定 + 重启枚举漂移。修法=netplan 改 `match: macaddress`（段 id 须改非接口名防 netplan5 反导出 interface-name）；A/C worker 补 `--cache` 启用 rpccache。重启验证：接口名又互换但 IP 正确跟随 MAC。详见 [V4复现-无rpccache与环网异常落档](../../docs/V4复现-无rpccache与环网异常落档_20260908.md) |
| 2026-09-08 | **V4-Flash 三站分布式成功复现** | 分片 4×45G + `-sm layer` 层分布 + master(v0.3.0, rpc v6.0.0) + 环网 RPC（10.10.10.1/10.10.11.3），**不用 `-ngl 99`** → 加载成功（5m21s），三站内存 B=55/A=60/C=72G 均 < 110G 阈值，无 OOM。生成冒烟：中文问候输出 ✅，**decode 7.93 t/s**（9/1 基线 ~6.3 t/s，+26%）。日志含 HC 融合警告（#26578 未合入，已知瓶颈）。**推翻「弃 Vulkan 转 ROCm」** — Vulkan 路线在正确形态（层分布、非全量持有、分片）下可行 |
| 2026-09-08 | llama 后端盘点 + ds4 部署方案 | 盘点 A/B/C 残留 llama 后端；/opt 已 MANIFEST 控制 ✅；ds4 方案落档。**（subagent 审计修正：C 站非完全同构仅 3 目录；补遗 llama-distributed/~/App/llama-gfx1151(1.7G ROCm)/zip/tar.gz 等；C 有活跃 Qwen3 多模态 llama-server 勿动）** 见 [FRAMEWORK-SURVEY 附录 H.2](../model-eval/FRAMEWORK-SURVEY-2026-09.md)（原 llama后端盘点并入） |
| 2026-09-08 | **llama 残留后端清除完成** | 已清除（先备份+md5 验证后删）：A/B 站 `/opt/llama.cpp-v0.2.0`、`~/llama.cpp-vulkan-b10715`；B 站 `~/App/llama-gfx1151`（1.7G，2024 老 ROCm CI 残留，已 tar 备份 445M）、`llama-b1292-*.zip`、`llama.cpp-v0.2.0.tar.gz`。备份：B 站 `/data/backup/llama-cleanup-20260908/`（5 项 1.2G）、A 站同路径（2 项，vulkan 由 B 同 commit 补齐，删除前 md5 与备份一致）。现役链路（/opt master 软链 + A/C rpc-server）无受损 |
| 2026-09-08 | gpt-oss-120b 后训练生态 + Astra 调研 | 盘点 AutoTrust/cloudyu 蒸馏家族（Fable-5 本地已有，HumanEval +15pp；Sonnet-Reasoning/Hertetic 等）；**GPT-6 Astra（9/3 发布）闭源 + 不支持微调 + latent thinking 阻碍蒸馏 → 不存在基于 Astra 的开源模型**；gpt-oss-120b 仍是集群单机可后训练唯一现实基座。见 [MODEL-SOURCING 附录 H.3](../model-eval/MODEL-SOURCING-2026-09.md)（原 gpt-oss 调研并入） |
| 2026-09-08 | **Fable-5/Sonnet-Reasoning 幻觉核查** | ①**Fable-5-Distilled**：AutoTrust 模型卡官方警告 **severe hallucination**（捏造 API 签名/路径/URL/版本号），黄金法则=配 anysearch 锚定；根因=40-step+无RLHF+352 turns。②**Sonnet-Reasoning-Distilled**：无专项警告，但 gpt-oss 基线 SimpleQA 高幻觉率 → 事实内容仍须验证；1000-step checkpoint 数值精度弱。**结论：两模型仅可用于代码/推理链，事实性内容一律检索验证门**。见调研文档 §4.3/§4.4 |
| 2026-09-08 | **C 站 unsloth 引擎补齐（与 A/B 同构）** | C 站原无 `~/.unsloth/llama.cpp`（studio UI-only）→ 经 B 站 tar-over-ssh 全量同步（2,011,722,283 B 字节级一致），**三站 key 文件 md5 完全一致**（llama-server=31d8787b、libggml-hip=87ea1504）。C 站 KFD + ROCm 7.2.1 环境验证可用（HIP 引擎枚举 ROCm0 120G）。C 站活跃 qwen3.8 服务未受影响。⚠️ 遗留：C 站内核为 **7.0.0-31**（A/B 为 6.17.x），与项目内核锁定规则不符，待确认 |
| 2026-09-08 | **C 站内核切换 6.17.0-23 + 环网 MAC 绑定根治** | C 站已装 6.17.0-23（与 B 同版本）但 GRUB_DEFAULT=0 跑 7.0.0-31 → 改 `GRUB_DEFAULT="1>4"`（id 方式失效，子菜单索引生效）→ 重启后运行 **6.17.0-23** ✅。**继发发现并修复两处环网问题**：①C 站 thunderbolt 接口名重启漂移（与 B 同款）→ netplan 改 MAC 绑定（02:c0:c6:93:d4:eb→10.10.11.3、02:8e:c8:89:54:ef→10.10.12.3）②A 站 `thunderbolt1` 连接 **autoconnect=false** 致 10.10.12.1 重启不恢复（这是用户此前需手动开 A thb1 开关的底层原因）→ 手动 `nmcli con up` 激活。**环网三站两两 0% 丢包全通**；C 站 qwen3.8 服务 18080 自恢复 |
| 2026-09-08 | **A 站 autoconnect 固化（根治 10.10.12 重启丢失）** | A 站 `thunderbolt1` 连接（3bdf2925，10.10.12.1）原 `autoconnect=false` → netplan yaml passthrough 改为 `"true"`（先备份 .bak-autoconnect-20260908）→ NM 运行时确认 true + netplan generate 后 yaml 仍 true（重启持久化验证）。**三站环网全通稳定** |
| 2026-09-08 | **C 站管理网迁移（WiFi→eno1, IP .24→.37）** | C 站网络接入改为网线 eno1，DHCP 获 `192.168.1.37`（原 WiFi `192.168.1.24` 弃用）；用户手动关闭 WiFi → **单网卡单默认路由**（与 A/B 同构，治理 IP 漂移）。文档同步：DEPLOYMENT.md（SSH/Cockpit URL）、tmp 工具脚本（v4f_start_monitor.sh/rpccache_deep.sh/deploy_key.py）改 .37；V4 落档加历史注记 |
| 2026-09-08 | **C 站 HIP 定案翻转：UMA=4G 档全链路打通 🎉** | 事件链：①B 站同参对照成功→定位 C 站差异=UMA carveout；②用户 BIOS 确认最小档仅 1G、Auto=64G；③社区调研（EVO-X2 同 2014:801d 公板转 Vulkan；jstormes/StrixHalo UMA 建议 512M-4G；ROCm #6146/#6582）佐证 carveout 档位是真实变量；④软件层穷尽（ROCm 屏蔽/aqlprofile/hipBLASLt/XNACK/CPU-only/amd_iommu=off+pages_limit=32M）均不解决 → **BIOS 设 4G 档**；⑤实测 gpt-oss-120b HIP：**加载 23s、零 fault、prefill 152 t/s、decode 48.8 t/s**（A/B 站 49.8-52.7 同级）→ **三站 unsloth HIP 单站同构达成**。根因=carveout 档位：1G→SVM page fault；Auto(64G)→HIP 池仅 61G 装不下 60G；**4G 两全**。**教训：C 站"HIP 不可用"早期定案错误，被用户"统一内存为何跑不了"质疑纠偏** |
| 2026-09-08 | **C 站 llama-systemd 看门狗关闭 + 显存占用根治** | `llama-server@.service` 模板 `Restart=on-failure`→`Restart=no`（备份 `.bak-watchdog-20260908`）；qwen 实例 `systemctl disable`（移除 multi-user.target.wants symlink）→ 不再开机自启/失败复活。显存占用溯源：qwen `-ngl 999` 全 offload → 权重进 GPU（carveout 4.2G + GTT 30G）；服务停止后 vram/gtt 回归基线。**C 站当前无 llama 进程、端口全释放；需手动起 qwen: `systemctl start llama-server@qwen3.8-27b-mtp`** |
| 2026-09-08 | **C 站 HIP vs Vulkan 后端对照（gpt-oss-120b, UMA=4G）** | 同模型同参数双后端实测：**HIP**（unsloth b10715, ROCm0）= prefill **152 t/s** / decode **48.8 t/s**；**Vulkan**（/opt v0.3.0, Vulkan0）= prefill **88.6 t/s** / decode **53.3-53.7 t/s**。两者均 23s 加载、零 fault。结论与 A/B 站 nemotron 规律一致：**Vulkan decode 略优（+9%）、HIP prefill 明显更快（+72%）**。C 站三后端（HIP/Vulkan/CPU）全部可用，HIP 定为主引擎（三站同构），Vulkan 互补。注意：`~/llama.cpp/build` 旧引擎不支持 `--load-mode`，Vulkan 测试须用 `/opt`。详见 [spec/infer-load/research/C站unsloth推理验证](../infer-load/research/C站unsloth推理验证_20260908.md) §8.5 |
| 2026-09-08 | **上游状态核对（fetch 原页）** | 全部 Open 无 merged：**#27754** 40→**41 commits**（8/26 更新，含 mHC 宽残差/KDA；MTP 性能 16K ctx 77.2 t/s 实证）；**#27917** 31 commits Draft（8/28）；**#27752/#27773** 未变（#27773 附 avar6 quantized GGUF 发布）；**#27922** enrichment 未合（mmproj 失败佐证）；**#26578** Open 1 commit（force-push ccbc178，性能 1.12×/1.50× 与台账一致）；**#28047** Open 未修（新增：deepseek4 命中 `dsv4_csa_state_kv_l16`、glm-dsa 命中 `top_k-40`，无 assignee/PR，master f1793c1 仍复现 → 维持 Flash 不受影响、Pro/GLM-5.3 分布式禁用的判据）；**#27332** Open（并入 §1.2 详列）。**结论保持：GLM-5.3 未合入、V4-Flash 引擎升级 P0 不变** |
| 2026-09-09 | **三站模型目录统一（方案 A）+ 迁移后冒烟 ✅** | 散落实体全部搬入 `~/.lmstudio/models/<publisher>/<repo>`，`/data/models/gguf` 反向软链（架构与 §5.7 一致，加载脚本零改动）。B 4 repo / A 2 / C 2（C 由空库变实体库）；misc 归位 lmstudio-community 后删除。C 站真实加载冒烟 gpt-oss-120b（走软链路径）**decode 56.5 t/s** 与基准一致。详见 [spec/operator-optimization/research/模型路径统一方案A](../operator-optimization/research/模型路径统一方案A_20260909.md) |
| 2026-09-09 | **V4-Flash 三站 Vulkan RPC 加载复跑成功 ✅** | 按 9/8 成功形态：B head `/opt/llama.cpp`（v0.3.0, rpc v6.0.0）+ A/C worker（`ggml-rpc-server --device Vulkan0 -c` 50052，**环网 10.10.10.1 / 10.10.11.3**，非 WiFi），模型 split3 分片，`-sm layer` 无 `-ngl 99`。加载 ~4.5min（含 A/C rpccache 命中），三站内存均衡（B52/A54/C53G，无单机全量持有 146G）。冒烟推理 `12*13=156` ✅ decode **7.79 t/s**（=历史 7.9 t/s 基线）。worker 参数修正：`ggml-rpc-server` 缓存为 `-c` 布尔开关（无路径参数） |
| 2026-09-09 | **V4-Flash 整片单文件两机 RPC 验证（A-C / B-C）✅ + split3 删除** | 删 split3 分片（146G）仅留单文件 156G 主资产。单文件经环网 B→A 同步（3m56s @632MB/s，md5 一致）。**两两组合均成功**：A-C（head=A worker=C）decode **9.20 t/s**；B-C（head=B worker=C）**9.11 t/s**，均 `-sm layer` 无 `-ngl 99`，`layer 23→RPC0` 层分布生效，两机内存均衡（head~76G / worker~76G）。**结论：整片单文件两机分布可行且比三站分片更快（-1.4 t/s，少一站 RPC）**；历史"单文件崩溃"根因实为「单机全量持有 + `-ngl 99`」非单文件本身。注：B→C ssh 复杂命令易超时，用脚本内 `setsid nohup ... </dev/null &` 规避；C 管理网 ssh 偶发失败可经 B 跳板 |