# Cpp_Hub — D6 第二试点（轻量 C++ 量化工具）

> **试点目标**：以最轻形态验证 D6 全链路——建 git 工程 → 跨站中文路径 scp（O-06）→ 远端 venv 预置（O-13）→ 远端任务 → golden 独立验收（O-12）→ 产物回联。
> 用户裁定：轻量工程（编译非硬性）；G8 预置面 = 仅编译链 + venv（R/sympy 拆除单列）。

## 结构

| 路径 | 说明 |
|---|---|
| `src/因子计算_核心.cpp` | 核心库（中文命名文件 = O-06 样本） |
| `src/因子计算_run.cpp` | 可运行入口（中文命名） |
| `CMakeLists.txt` | cmake 构建（编译非硬性） |
| `tests/` | pytest 脚手架（B 站 venv） |

## 中文路径验证样本（O-06）

本工程刻意使用**中文文件名**（`因子计算_核心.cpp` / `因子计算_run.cpp`）与中文标题 README，
作为跨 `scp` / tar 传输的 UTF-8 路径/文件名验证样本（S6 兼容性路径级未测项）。

验证判据：经 scp 传输到 B 站后，远端能按原中文名落地、`ls` 可读、可入 git，无乱码/改名。

## 构建（可选，轻量模式非硬性）

```bash
cmake -S . -B build
cmake --build build
./build/cpphub_run 3 4   # → 因子求和(3,4) = 7
```