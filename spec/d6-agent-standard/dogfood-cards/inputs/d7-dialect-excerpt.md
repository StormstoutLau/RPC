# D6/D7 方言摘录（**public 摘要**，供 U-2 方言映射卡作输入）

> **本文件是"人工摘录 + 脱敏"版**，不是任何原文档的拷贝。
> **脱敏判据**（Scott 2026-09-24 裁定：B1 = `public` + 摘要）：
> 只保留 **符号 → 该处含义** 的抽象对照，**已剔除**：主机名 / 内网 IP / 任何用户目录与绝对路径 /
> 源码标识符与行号 / 键名与配置项 / 凭据形态 / 具体实现细节。
> **保留**：符号族 · 所在项目名 · 该处的**阶段/档位含义**（用通用词表述）。
> ⚠ 它**不等于**原文档的完整性 —— 缺失项**就是缺失**，不要从本摘要推断原文还有什么。

## 族 1：`L1` / `L2` / `L3`

| 出处 | L1 | L2 | L3 |
|---|---|---|---|
| Paper | 元数据抽取阶段 | 正文提取阶段 | 质检阶段 |
| Textbook | 结构层（节点与边） | 元数据层 | 边层 |
| Open_Data | 官方来源级 | 社区来源级 | 实测取得级 |
| Macro_Data | 缓存 TTL 档位（L1–**L5**） | — | 另有**独立一套** L1–**L7**：反爬栈层级（与缓存档**不同义**） |
| Fin_Agent | 标签粒度（宽泛概念 / 细分行业 / 精细标的） | — | 另有**独立一套** L1–**L4**：架构分层（与标签粒度**不同义**） |

## 族 2：`A` / `B` / `C`

| 出处 | A / B / C 的含义 |
|---|---|
| Cpp_Hub · Spec_Workflow | **断言证据分级**：A=事实 / B=推断 / C=判断 |
| Macro_Data | **数据源可访问性档位**：A=无 key / B / C / D |
| Cpp_Hub | 另有**平台支持档位**（Tier 1 / Tier 2） |

> ⚠ 前两者**同符号、同在"分级"语境，含义完全不同**。

## 族 3：`R1` – `R5`

| 出处 | 含义 |
|---|---|
| Open_Data | **幻觉修正分级**：R1 硬伤 / R2 降级 / R3 补注 / R4 失效 / R5 过期 |
| Macro_Data | **多源冲突裁决规则码**：R1 基准优先 / R2 单位不一致 / R3 中位数兜底 / … |

> ⚠ 两者**都是"分级"，但一个管"文档怎么改"，一个管"两个数据源不一致时信谁"**。

## 族 4：`E1` – `E5`

| 出处 | 含义 |
|---|---|
| 本仓（RPC） | **证据档位族**（含 `[官方]` / `[社区]` / `[实测]` 这类来源强度） |
| Spec_Workflow | **其自己的 E1–E5 族** —— 与上者**同名不同义**（本仓已为此改名加 `RPC-` 前缀以示区分） |

## 族 5：其它已出现的编号族（摘要只知"存在"，**不含含义**）

| 出处 | 族 | 已知 |
|---|---|---|
| Paper | `B1` / `B2` · `D1` – `D8` | 存在两套内部编号族（**具体含义不在本摘要内**） |
| factor_pipeline | `CRITICAL` / `MAJOR` / `MINOR` · `P0` – `P3` · `Tier 1` – `Tier 3` | 三套并存（**具体含义不在本摘要内**） |
| 本仓 inbox | `Reproduced` / `Replicated` | 交付分级：内部同基座复验 / 异基座独立审计 |

## 本摘要**明确不含**的东西（防你补）

- 任何项目的**源码 / 文件名 / 行号 / 函数名 / 配置键**
- 任何**主机名 / IP / 端口 / 路径 / 凭据**
- 族 5 里那些族的**具体含义**（摘要里就是"不知道"）
