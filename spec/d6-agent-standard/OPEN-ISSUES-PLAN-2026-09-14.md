# D6 剩余 Open 项推进计划（2026-09-14）

> **目的**: 落实 OPEN-ISSUES §5「剩余 open 全景快照」的推进序。D6 主干已闭环（无 P1 阻塞级 open），本计划覆盖**剩余低优先/递延项**的排序、验收判据与批次归属，避免台账悬空。
> **依据**: [OPEN-ISSUES.md](./OPEN-ISSUES.md) 台账 + 代码核验（agent-cli.ps1 / CHECKLIST）。
> **原则**: 台账单一真值；任何项闭环必须实机证据回填，拒绝口头关闭（OPEN-ISSUES §4.4）。

---

## 0. 结论先行

D6 主干闭环完整，剩余无 P1 阻塞项。剩余 open 共 6 类：**O-13 是唯一有实质未完成工作的项**（R/sympy 批次 + 真编译判据决策点）；O-02 是小功能二期；O-05/O-03/O-04 为口径/纪律微调；O-07 事件驱动不可计划；O-16 `--peer` 明确递延 D7。

**本次推进范围**: 只做 O-13 + O-05 两项（可闭环可验收），其余登记留档不动作。

---

## 1. 剩余项清单（复核自代码，非仅台账）

| ID | 类别 | 状态 | 代码/现状核验 | 闭合性质 |
|----|------|------|--------------|---------|
| O-13 | G8 环境预置 | ⚠ 半收口 | B 站已装 g++/cmake/pytest；但 Cpp_Hub-001 golden 走纯源码静态断言未走 `cmake` 真编译；R/sympy 重资产单列 | **可推进** |
| O-05 | sync 性能/口径 | ⚪ 登记 | sync 62.3s 微超 60s 预算 4%（CHECKLIST §5）；IMPL §5 sync/task 口径重叠 | **可推进** |
| O-02 | workspace --archive | 🔴 open | agent-cli.ps1 L349-351 仍是 `echo` stub | 二期小功能 |
| O-07 | zen 限额 | ⏳ 待触发 | 退出码 7 已定义置位，未真实触发 | 事件驱动不可计划 |
| O-03 | probe 证据腐化 | ⚪ 登记 | 纪律项；验收产物统一入 agent-out（A14 已如此） | 流程固化 |
| O-04 | ledger 沙箱写被拒 | ⚪ 登记 | 纪律项；wrapper 从非沙箱宿主运行 | 流程固化 |
| O-16(子) | `--peer` 站间互审 | 留 D7 | review 评审环主体已 closed（2026-09-12）；站间互审协议留 D7 | **明确递延** |

---

## 2. 推进批次与排序

按「可闭环可验收 > 纪律固化 > 事件/递延」排序：

### 批次 P（本次）— 实际推进

#### 2.1 O-13 G8 收口（含两个既有子项）

**现状**: B 站编译链物质前提已满足，但 golden 未强制走真编译；R/sympy 未预置。

**子项 A — 真编译判据决策（已定案 2026-09-14 选 A1）**:
- 决策: **后续 C++ 任务卡 golden 走真 `cmake` 编译**（D-19，已入 DECISIONS.md）
- 落地形态: 代理层无需改动——golden cmd 本就在目标站工作区执行（agent-cli.ps1 L933 `cd "$W" && eval ...`），真编译只需保证派发到有编译链的站（B）且 golden cmd 内调 `cmake` + 断言；主控侧保留静态断言为无编译链场景的 fallback。
- 验收: 首个真编译型 C++ 卡落地时，golden 输出含 cmake 构建成功 + 断言通过证据。

**子项 B — R/sympy 批次**:
- 目标: B 站预置 R（noble-cran40）+ sympy，满足依赖就绪门（CROSS-PROJECT §5.2）
- 验收: `python3 -c "import sympy"` 通过 + `R --version` 可执行 + Rscript 冒烟
- 关闭判据: 一个 R/sympy 依赖的真实任务卡在 B 站跑通 accept

**推进动作**:
1. 在 B 站执行 R(CRAN) + sympy 预置（脚本落 `ops/station-bin/_r_sympy_provision.sh` 对齐 `_cpp_venv_provision.sh` 惯例）
2. `agent-cli workspace --sync` 验证预置后被排除规则不混入工作区
3. 关闭判据达成后回填 O-13 详情节

#### 2.2 O-05 sync 口径收口

**现状**: 口径重叠（sync 单测 <60s vs task 含 sync <30s 互斥）。

**推进动作**:
1. IMPLEMENTATION §5 预算表口径单一化 —— sync 改「已同步增量口径」，去除与 task 的互斥描述
2. 实测 sync 增量达标复核
3. 关闭判据: 预算表无互斥口径 + sync 增量 ≤ 预算

---

### 批次 D — 只登记不动作（纪律固化）

#### 2.3 O-03 / O-04 纪律
- 均无需代码改动，为流程纪律。
- **动作**: CROSS-PROJECT-WORK-STANDARD §6 + 手册 §2 agent-cli 加两条操作纪律（probe/验证产物一律入 `agent-out`；wrapper 从非沙箱宿主运行、或台账移可写区）。
- 关闭判据（已有）: O-03 后续验证产物均落 agent-out；O-04 台账行数与 run.json 计数一致。

---

### 批次 E — 挂起/递延（不推进）

| ID | 处理 |
|----|------|
| O-02 `--archive` | 标注「二期」，挂起（archive 前不动站上记忆的 R7 语义已保守满足，非阻塞） |
| O-07 zen | 事件驱动，保持 ⏳，首次触发即回填实测路径 |
| O-16 `--peer` 站间互审 | 明确登记「D7 递延」，D6 一期不做 |

---

## 3. 依赖关系

- O-13 子项 B（R/sympy）依赖 B 站可达 + 预置脚本落盘；
- O-13 子项 A 为纯决策点，不依赖任何前置；
- O-05、O-03/O-04 均无前置。

无任何剩余项依赖未闭环的硬前置（O-25 P0 / O-26 / O-16 均已闭环）。

---

## 4. 验收汇总表（落地时回填）

| 批次 | 项 | 关闭判据 | 状态 |
|------|----|---------|------|
| P | O-13 子项 B | ✅ R/sympy 已预置 + `o13-sympy` 卡全链 pass（2026-09-14） | ✅ |
| P | O-13 子项 A | ✅ 已定案（2026-09-14 选 A1 真 cmake 编译，D-19） | ✅ |
| P | O-05 | 预算表口径单一化 + sync 达预算 | ⬜ |
| D | O-03/O-04 | 纪律入册 | ⬜ |
| E | O-02/O-07/O-16--peer | 挂起/递延，不动作 | ⬜ |

> 本文档不替代 OPEN-ISSUES.md 单一真值；落地时以本文档为执行线索，但仍回填 OPEN-ISSUES 详情节（台账铁律）。