# AMD Ryzen AI Max+ 395 多机分布式推理完整技术文档

> **版本**: v3.1  
> **适用硬件**: AMD Ryzen AI Max+ 395 迷你主机（128GB 统一内存）  
> **适用系统**: Ubuntu 24.04  
> **模型格式**: GGUF  
> **推理后端**: Vulkan  
> **分布式框架**: llama.cpp RPC 模式  
> **最后更新**: 2026-08-27 (新增 API 服务模式)

---

## 一、概述

本文档基于实际部署经验，完整记录了在多台 AMD Ryzen AI Max+ 395 机器上使用 `llama.cpp` 的 RPC 模式进行分布式推理的全过程。内容涵盖硬件连接、系统优化、编译部署、模型处理、参数配置、性能调优及问题排查。

### 1.1 硬件配置

| 组件 | 规格 |
|:---|:---|
| 处理器 | AMD Ryzen AI Max+ 395 (16 核 Zen5, 40CU RDNA 3.5) |
| 内存 | 128GB LPDDR5X 统一内存 |
| 可用显存 | 96GB (BIOS 默认) → 120GB (GRUB 优化后) |
| 网络接口 | USB4 (40Gbps) / 2.5GbE / 5GbE / 10GbE |
| 操作系统 | Ubuntu 24.04 LTS |

### 1.2 软件栈

| 组件 | 版本/说明 |
|:---|:---|
| llama.cpp | commit b9859+ (Vulkan + RPC 支持) |
| Vulkan 驱动 | mesa-vulkan-drivers (RADV for GFX1151) |
| 内核 | 6.17.0-23-generic+ |
| 推理工具 | llama-cli / llama-server / ggml-rpc-server |

---

## 二、硬件连接与网络配置

### 2.1 网络拓扑选择

根据节点数量选择合适的拓扑结构：

| 节点数 | 推荐拓扑 | 说明 |
|:---|:---|:---|
| 2台 | 点对点直连 | 使用 USB4 线缆直接连接 |
| 3台 | 菊花链 (Daisy Chain) | 每台机器用两个 USB4 端口首尾相连 |
| 4台+ | 环形 (Ring) 或 全网状 | 环形成熟可靠，全网状需每台3+个USB4口 |

**重要提示**：当前没有 USB4 交换机或 1 转多扩展坞可用于多机点对点通信。只能用点对点直连或菊花链。

### 2.2 USB4 直连配置

连接后，为每个 USB4 网络接口配置静态 IP（替换 `enx...` 为实际网卡名）：

```bash
# 机器 A（Master）
sudo ip addr add 10.10.10.1/24 dev enx...
sudo ip link set enx... up

# 机器 B（Worker）
sudo ip addr add 10.10.10.2/24 dev enx...
sudo ip link set enx... up
```

验证连通性：
```bash
ping 10.10.10.2   # 在 A 上执行
ping 10.10.10.1   # 在 B 上执行
```

### 2.3 主机名解析与 SSH 免密登录

编辑 `/etc/hosts`（所有节点）：
```
10.10.10.1  node1
10.10.10.2  node2
10.10.10.3  node3
```

配置 SSH 免密登录（Master 到所有 Worker）：
```bash
ssh-keygen -t rsa
ssh-copy-id node2
ssh-copy-id node3
```

---

## 三、系统优化：扩展 GPU 显存

突破 BIOS 96GB 限制，将每台机器的可用显存扩展至 **120GB**。

### 3.1 修改 GRUB 参数

```bash
sudo nano /etc/default/grub
```

在 `GRUB_CMDLINE_LINUX_DEFAULT` 中追加：
```
ttm.pages_limit=30720000 amdgpu.gttsize=120000
```

更新并重启：
```bash
sudo update-grub
sudo reboot
```

### 3.2 验证显存扩展

```bash
sudo dmesg | grep "amdgpu.*memory"
```

期望输出：`120000M of GTT memory ready.`

### 3.3 BIOS 设置

重启进入 BIOS，将 `iGPU Memory Size` 设为 **512MB**。

---

## 四、llama.cpp 编译部署

### 4.1 安装依赖

```bash
sudo apt update
sudo apt install build-essential cmake git ccache curl
sudo apt install mesa-vulkan-drivers vulkan-tools
```

### 4.2 获取源码

方式一（从 GitHub 克隆）：
```bash
git clone https://github.com/ggerganov/llama.cpp /opt/llama.cpp
```

方式二（使用离线包，推荐）：
```bash
sudo tar -xzf llama.cpp-*.tar.gz -C /opt/
sudo mv /opt/llama.cpp-* /opt/llama.cpp
sudo chown -R $USER:$USER /opt/llama.cpp
```

### 4.3 编译（Vulkan + RPC）

```bash
cd /opt/llama.cpp
mkdir -p build && cd build
cmake .. -DGGML_VULKAN=1 -DGGML_RPC=ON
cmake --build . --config Release -j $(nproc)
```

**注意**：不同环境编译产物可能出现的位置：
- `/opt/llama.cpp/build/bin/` （标准）
- `/opt/llama.cpp/` （部分环境）
- `/opt/llama.cpp/build/` （某些配置）

### 4.4 验证编译

```bash
ls -l /opt/llama.cpp/llama-cli
ls -l /opt/llama.cpp/ggml-rpc-server
```

如找不到，检查 `/opt/llama.cpp/build/bin/`。

---

## 五、模型处理：合并 GGUF 分片

### 5.1 背景

从 Hugging Face 等平台下载的模型常被拆分为多个分片：
```
model-00001-of-00005.gguf
model-00002-of-00005.gguf
model-00003-of-00005.gguf
model-00004-of-00005.gguf
model-00005-of-00005.gguf
```

`llama.cpp` 需要完整的单一 GGUF 文件，因此必须合并。

### 5.2 合并命令

```bash
cd /path/to/model/dir
/opt/llama.cpp/llama-gguf-split --merge model-00001-of-00005.gguf model-merged.gguf
```

### 5.3 验证合并结果

```bash
file model-merged.gguf
```

如果输出 `data` 而非 `GGUF model data`，**这不一定表示失败**。`file` 命令依赖魔数数据库，GGUF 新格式可能未被识别。合并成功的标志是日志中无错误，并显示 `merged from N split with X tensors`。

进一步验证（推荐的权威方法）：
```bash
/opt/llama.cpp/llama-cli -m model-merged.gguf -p " " -n 0
```

### 5.4 关于 config.json

对于 GGUF 模型，**`config.json` 无需修改**。`llama.cpp` 直接读取 GGUF 内部的元数据，不会使用外部配置文件。

---

## 六、分布式推理配置与脚本

### 6.1 目录结构

```bash
mkdir -p ~/llama-distributed/logs
cd ~/llama-distributed
```

### 6.2 配置文件 `inference.conf`

```ini
# inference.conf - 推理参数配置

# ---------- 模型路径 ----------
MODEL_PATH="/home/user/.lmstudio/models/.../model-merged.gguf"

# ---------- RPC Worker 地址 ----------
RPC_ADDR="10.10.10.2:50052"

# ---------- 提示词与生成长度 ----------
PROMPT=""                      # 留空进入交互模式
MAX_TOKENS=4096

# ---------- 采样参数 ----------
TEMPERATURE=0.3                # 学术/编程任务推荐 0.3
TOP_P=0.95
TOP_K=40
REPEAT_PENALTY=1.1
REPEAT_LAST_N=64

# ---------- 上下文与性能 ----------
CTX_SIZE=32768                 # 可逐步提升至 131072
GPU_LAYERS=999
THREADS=16
BATCH_SIZE=512

# ---------- MoE 专用参数 ----------
N_CPU_MOE=8                    # CPU 运行的 MoE 专家数

# ---------- Flash Attention ----------
FLASH_ATTN="on"

# ---------- MTP 投机解码 ----------
# 需要模型保留 MTP 权重（文件名含 MTP 字样）
# SPEC_TYPE="draft-mtp"
# SPEC_DRAFT_N_MAX=2

# ---------- 其他 ----------
VERBOSE=false
```

### 6.3 RPC 启动脚本 `start_rpc.sh`

在 Worker 节点执行：

```bash
#!/bin/bash
RPC_BIN="/opt/llama.cpp/ggml-rpc-server"
# 如果产物在 build/bin/ 下，改为 /opt/llama.cpp/build/bin/ggml-rpc-server
RPC_HOST="0.0.0.0"
RPC_PORT="50052"
LOG_DIR="$HOME/llama-distributed/logs"
LOG_FILE="$LOG_DIR/rpc_$(date +%Y%m%d_%H%M%S).log"

mkdir -p "$LOG_DIR"

if pgrep -f "ggml-rpc-server.*$RPC_PORT" > /dev/null; then
    echo "⚠️ RPC 服务已在端口 $RPC_PORT 运行"
    exit 1
fi

echo "🚀 启动 RPC 服务，监听 $RPC_HOST:$RPC_PORT"
nohup "$RPC_BIN" -H "$RPC_HOST" -p "$RPC_PORT" > "$LOG_FILE" 2>&1 &

sleep 2
if pgrep -f "ggml-rpc-server.*$RPC_PORT" > /dev/null; then
    echo "✅ RPC 服务启动成功，PID: $(pgrep -f "ggml-rpc-server.*$RPC_PORT")"
else
    echo "❌ 启动失败，请检查日志: $LOG_FILE"
    exit 1
fi
```

### 6.4 推理执行脚本 `run_inference.sh`

在 Master 节点执行：

```bash
#!/bin/bash
CONFIG_FILE="$(dirname "$0")/inference.conf"
if [ ! -f "$CONFIG_FILE" ]; then
    echo "❌ 配置文件不存在: $CONFIG_FILE"
    exit 1
fi
source "$CONFIG_FILE"

if [ -z "$MODEL_PATH" ] || [ ! -f "$MODEL_PATH" ]; then
    echo "❌ 模型文件不存在或未指定: $MODEL_PATH"
    exit 1
fi

if [ -z "$RPC_ADDR" ]; then
    echo "❌ 未指定 RPC_ADDR"
    exit 1
fi

# 检测 RPC 服务
RPC_HOST=$(echo $RPC_ADDR | cut -d':' -f1)
RPC_PORT=$(echo $RPC_ADDR | cut -d':' -f2)
if ! nc -z "$RPC_HOST" "$RPC_PORT" 2>/dev/null; then
    echo "⚠️ 无法连接到 RPC 服务 $RPC_ADDR"
    exit 1
fi

LLAMA_CLI="/opt/llama.cpp/llama-cli"
# 如产物在 build/bin/，改为 /opt/llama.cpp/build/bin/llama-cli

CMD="$LLAMA_CLI -m \"$MODEL_PATH\""
CMD="$CMD --rpc $RPC_ADDR"
CMD="$CMD -ngl $GPU_LAYERS"
CMD="$CMD -n $MAX_TOKENS"
CMD="$CMD --temp $TEMPERATURE"
CMD="$CMD --top-p $TOP_P"
CMD="$CMD --top-k $TOP_K"
CMD="$CMD --repeat-penalty $REPEAT_PENALTY"
CMD="$CMD --repeat-last-n $REPEAT_LAST_N"
CMD="$CMD -c $CTX_SIZE"
CMD="$CMD -t $THREADS"
CMD="$CMD -b $BATCH_SIZE"

# MoE
if [ -n "$N_CPU_MOE" ]; then
    CMD="$CMD --n-cpu-moe $N_CPU_MOE"
fi

# Flash Attention
if [ -n "$FLASH_ATTN" ]; then
    CMD="$CMD --flash-attn $FLASH_ATTN"
fi

# MTP
if [ -n "$SPEC_TYPE" ] && [ -n "$SPEC_DRAFT_N_MAX" ]; then
    CMD="$CMD --spec-type $SPEC_TYPE --spec-draft-n-max $SPEC_DRAFT_N_MAX"
fi

# 提示词
if [ -n "$PROMPT" ]; then
    CMD="$CMD -p \"$PROMPT\""
fi

if [ "$VERBOSE" = "true" ]; then
    CMD="$CMD --verbose-prompt"
fi

echo "🚀 执行推理命令："
echo "$CMD"
echo ""
echo "---------- 开始推理 ----------"
eval "$CMD"
```

### 6.5 赋予执行权限

```bash
chmod +x ~/llama-distributed/start_rpc.sh
chmod +x ~/llama-distributed/run_inference.sh
```

---

## 七、运行分布式推理

### 7.1 标准流程

1. **Worker 节点启动 RPC**：
   ```bash
   cd ~/llama-distributed
   ./start_rpc.sh
   ```

2. **Master 节点执行推理**：
   ```bash
   cd ~/llama-distributed
   ./run_inference.sh
   ```

### 7.2 多节点扩展

3台及以上时，在 Master 节点的 `run_inference.sh` 中修改：
```bash
CMD="$CMD --rpc 10.10.10.2:50052"
CMD="$CMD --rpc 10.10.10.3:50052"
```

### 7.3 API 服务模式 (OpenAI 兼容, 2026-08-27 新增)

`run_inference.sh`（llama-cli）只支持交互式对话。要实现程序化调用，用 **`llama-server`** 替代 —— 同样支持 `--rpc` 分布式推理，且提供 OpenAI 兼容 HTTP API。

**部署位置**: Master 节点（B 站）`~/llama-distributed/run_server.sh`

**启动脚本 `run_server.sh`** 特性：
- 复用 `/llama-distributed/inference.conf` 全部参数（模型路径、RPC 地址、采样参数、MoE 调度等），与 `run_inference.sh` 同源，仅将 `llama-cli` 换成 `llama-server`
- 启动前自动探测 RPC Worker 可达性
- 防重复启动（按端口检测）
- 自动轮询 `/health`（每 10 秒，最长 15 分钟）等待模型加载完成
- 日志保存到 `~/llama-distributed/logs/server_YYYYMMDD_HHMMSS.log`

**标准启动流程**:
```bash
# 1. A 站 (Worker): 启动 RPC
bash /llama-distributed/start_rpc.sh

# 2. B 站 (Master): 启动 API 服务 (默认 8080, 可指定端口)
bash ~/llama-distributed/run_server.sh 8080
```

**停止服务**:
```bash
# B 站停止 llama-server (A 站 RPC 可常驻, 不占内存)
pkill -f llama-server
```

**可用端点**:

| 端点 | 说明 |
|:---|:---|
| `POST /v1/chat/completions` | OpenAI 兼容对话（支持 `"stream": true` SSE 流式） |
| `POST /v1/completions` | OpenAI 兼容续写 |
| `GET /v1/models` | 模型列表 |
| `GET /health` | 健康检查（加载中返回 503） |

**调用示例** (任意 OpenAI 客户端, 主控站/局域网任意机器):
```python
from openai import OpenAI
client = OpenAI(base_url="http://192.168.1.15:8080/v1", api_key="none")
r = client.chat.completions.create(
    model="minimax",   # 任意名，服务端单模型忽略此字段
    messages=[{"role": "user", "content": "你好"}],
)
print(r.choices[0].message.content)
```

**curl 示例**:
```bash
curl http://192.168.1.15:8080/v1/chat/completions \
  -H 'Content-Type: application/json' \
  -d '{"model": "minimax", "messages": [{"role": "user", "content": "Reply with exactly: PONG"}], "max_tokens": 1024}'
```

**实测性能** (MiniMax-M2.7 Q4_K_S, 双机 RPC):

| 指标 | 值 |
|:---|:---|
| 模型加载 | ~4.4 分钟 |
| 生成速度 | 17.7 t/s (prompt 22.7 t/s) |
| 并发 | 4 slots × 32K ctx |
| prompt cache | 生效 (cached_tokens 计数正常) |

**注意**:
1. **推理模型的 reasoning 字段**: MiniMax-M2.7 等思考模型，思考过程在 `message.reasoning_content`，正式回答在 `message.content`。`max_tokens` 过小（如 64）会全部被思考消耗导致 content 为空，建议 ≥1024。
2. **安全加固**: 默认 `--host 0.0.0.0` 对局域网无认证开放。如需认证，在 `run_server.sh` 的 CMD 中追加 `--api-key YOUR_KEY`。
3. **并发**: `n_slots=4` 由 llama-server 根据 `-c` 自动划分（总 ctx 32K / 4 槽），如需更高并发或更大单槽 ctx，调整 `inference.conf` 的 `CTX_SIZE` 或启动参数 `--parallel N`。

### 7.4 交互模式

当 `PROMPT=""` 时，推理进入交互模式：
```
> 
```
直接输入问题即可进行多轮对话。输入 `/exit` 或 `Ctrl+C` 退出。

---

## 八、模型特定配置

### 8.1 MiniMax-M2.7

- **架构**: MoE (230B 总参数, 10B 激活)
- **推荐 `CTX_SIZE`**: 32768~65536
- **推荐 `--n-cpu-moe`**: 8~16
- **推荐 `TEMPERATURE`**: 0.3 (学术/编程)

### 8.2 DeepSeek-V4-Flash

- **架构**: MoE (支持 MTP 投机解码)
- **需要 MTP 版本模型**: 文件名含 `MTP`
- **启用 MTP**:
  ```bash
  --spec-type draft-mtp --spec-draft-n-max 2
  ```
- **验证加载**:
  ```bash
  /opt/llama.cpp/llama-cli -m model.gguf -p " " -n 0
  ```

### 8.3 模型文件合并

| 模型 | 分片数 | 合并命令 |
|:---|:---|:---|
| Qwen3-Coder-Next | 2 | `llama-gguf-split --merge ...-00001-of-00002.gguf merged.gguf` |
| MiniMax-M2.7 | 4 | `llama-gguf-split --merge ...-00001-of-00004.gguf merged.gguf` |
| DeepSeek-V4-Flash | 5 | `llama-gguf-split --merge ...-00001-of-00005.gguf merged.gguf` |

---

## 九、常见问题排查

### 9.1 编译问题

| 问题 | 解决方案 |
|:---|:---|
| `CMakeLists.txt` 不存在 | 确认源码解压路径，检查是否多了一层目录 |
| `GnuTLS recv error (-110)` | 网络不稳定，使用 `--depth 1` 浅克隆或离线包 |
| `failed to connect to docker API` | 使用 Docker Desktop 时设置 `DOCKER_HOST` 环境变量 |

### 9.2 运行时问题

| 问题 | 解决方案 |
|:---|:---|
| `Failed to connect to ...:50052` | 检查 Worker RPC 是否运行，使用 IP 而非主机名 |
| `invalid split file name` | 模型未合并，使用 `llama-gguf-split` 合并 |
| `error: invalid argument: --ub` | 移除不支持的参数（`--ub`, `--ctk`, `--ctv` 等） |
| `error: expected value for argument` | 新版参数需显式指定值，如 `-fa on` |
| `ggml_cuda_init: failed to initialize` | 安装 ROCm 用户态库，或切换 Vulkan 后端 |
| 终端输出被截断 | 增大 `MAX_TOKENS`，或重定向到文件 |
| 显存不足 (OOM) | 降低 `-c`, 增大 `--n-cpu-moe`, 检查 TTM 参数 |

### 9.3 RPC 服务管理

查看 RPC 状态：
```bash
ps aux | grep rpc-server
```

停止 RPC 服务：
```bash
pkill -f "ggml-rpc-server.*50052"
```

### 9.4 部署勘误 (2026-08-27 实测)

本节记录与文档早期描述不一致的实际部署情况，以实测为准：

| 项 | 文档原描述 | 实际部署 |
|:---|:---|:---|
| 脚本位置 | `~/llama-distributed/` | **根目录 `/llama-distributed/`**（root 所有）：A 站 `start_rpc.sh`，B 站 `run_inference.sh` + `inference.conf`；B 站 API 脚本 `run_server.sh` 在 `~/llama-distributed/`（用户所有） |
| llama.cpp 版本 | "commit b9859+" | **version: 9859 (commit 4fc4ec554)**，RPC 协议 v4.0.1，2026-07-02 构建 |
| 角色分配 | A=Master (10.10.10.1), B=Worker (10.10.10.2) | **A=Worker (10.10.10.1)，B=Master (10.10.10.2)**（`inference.conf` 的 `RPC_ADDR="10.10.10.1:50052"` 指向 A） |
| USB4 网络接口 | `enx...` | `thunderbolt0` |
| 二进制位置 | 可能在 `build/bin/` | 直接在 `/opt/llama.cpp/` 根目录（非 git 目录，离线部署） |
| Vulkan 驱动 | — | Mesa RADV 25.2.8, Vulkan API 1.4.318, GFX1151 (deviceID 0x1586) |

**版本一致性关键**: 两站 llama.cpp 为 MD5 级一致的相同构建（`llama-cli`、`libggml-vulkan.so`、`libggml-rpc.so`、`libggml-cpu-zen4.so` 逐一比对）。RPC 模式要求 Master/Worker 协议 ABI 兼容，**任何一站单独升级 `/opt/llama.cpp` 都会导致 RPC 失败，需两站同步更新**。

---

## 十、性能调优参考

### 10.1 推理速度参考

| 模型 | 量化 | 节点数 | 生成速度 |
|:---|:---|:---|:---|
| Qwen3-Coder-Next Q6_K | 2 | ~40 t/s |
| MiniMax-M2.7 Q4_K_M | 2 | ~10-15 t/s |
| MiniMax-M2.7 Q4_K_S (API 模式, 2026-08-27 实测) | 2 | 17.7 t/s (prompt 22.7 t/s) |
| DeepSeek-V4 Flash Q4_K_XL | 4 | 视上下文而定 |

### 10.2 参数调优建议

| 场景 | TEMPERATURE | CTX_SIZE | N_CPU_MOE |
|:---|:---|:---|:---|
| 学术分析 | 0.3 | 32768-65536 | 8-12 |
| 编程任务 | 0.3-0.5 | 32768 | 8-16 |
| 创意写作 | 0.7-1.0 | 16384 | 4-8 |
| 长文档处理 | 0.5 | 65536-131072 | 16-24 |

---

## 十一、总结

本技术文档基于数月的实际部署经验，完整记录了在多台 AMD Ryzen AI Max+ 395 机器上使用 `llama.cpp` RPC 模式进行分布式推理的全过程。核心要点：

1. **网络互联**：USB4 菊花链是当前最可靠的多机连接方案。
2. **显存优化**：通过 GRUB 参数将显存扩展至 120GB，是运行大模型的必要条件。
3. **模型处理**：GGUF 分片必须使用 `llama-gguf-split` 合并。
4. **参数适配**：不同模型（MoE、MTP 等）需要针对性配置。
5. **脚本化部署**：通过配置文件 + 执行脚本，大幅简化日常操作。

---

**文档版本**: v3.1 (新增 7.3 API 服务模式 / 9.4 部署勘误 / 10.1 API 实测速度)  
**适用范围**: AMD Ryzen AI Max+ 395 双机及以上集群  
**版权声明**: 基于实际部署经验整理，可自由参考使用