# V4 复现：无 rpccache 与环网异常落档（2026-09-08）

> **状态**: 已落档（现象+归因+临时对策；环网根因待排查）
> **关联**: [V4-Flash-0731加载崩溃根因分析_20260908.md](V4-Flash-0731加载崩溃根因分析_20260908.md) ｜ [upstream-tracker/TRACKER.md](../spec/upstream-tracker/TRACKER.md)
> **落档**: Scott ｜ 2026-09-08

---

## 1. 背景

复现 V4-Flash 分布式推理（B head + A/C worker，master 引擎）时，加载监控日志发现两个异常：

1. **无 rpccache**：权重在每次加载时全量推送（9/1 同形态约 3min/次），监听轮询期间 `load_model` 日志停在本地阶段不变，推测权重传输走 RPC 直发而非本地缓存命中。
2. **走 WiFi 而非环网**：实际启动命令 `RPC="192.168.1.33:50052,192.168.1.24:50052"`（WiFi 网段 192.168.1.x），而环网地址段为 A=10.10.10.1、C=10.10.11.3（见 `tmp/nodes.env.c`）。之前（9/1 及崩溃复现）RPC 均走环网闪电链路，本次却退到 WiFi。（注：C 站管理 IP 已于 2026-09-08 迁移至 eno1 `192.168.1.37`，此处的 .24 为当时 WiFi 地址，属历史记录）

用户明确说明：**环网物理连线没有变动，与之前一样**。

---

## 2. 现象证据

### 2.1 加载监控日志（`v4f_start_monitor.sh` → job-1da7ea…output.log）

```
[1] B used=4G head;  log_tail: load_model: loading model '.../v4f-00001-of-00004.gguf'
[2] B used=56G head; log_tail: load_model: loading model '.../v4f-00001-of-00004.gguf'   ← B 本地 56G 已入内存
...
[23] B used=57G head                                         ← 后续全程 56-57G，无 A/C 分载迹象
```

- B 内存冲到 56-57G 后**长时间不动**：与 9/1「rpccache 不命中 → 全量推权重 ~3min」形态一致；
- 无 `RPC tensor` 流式日志证明权重从 B 逐 tensor 推送（未走本地缓存命中路径）。

### 2.2 启动命令（`tmp/v4f_start_monitor.sh` L8）

```bash
RPC="192.168.1.33:50052,192.168.1.24:50052"   # A, C via WiFi
```

- 明确走 WiFi 网段（A=192.168.1.33, C=192.168.1.24）；
- 环网地址（10.10.10.1 / 10.10.11.3）未出现在本次启动命令中。

---

## 3. 归因分析

### 3.1 无 rpccache：A/C worker 未带 `--cache` 启动

依据（`tmp/rpccache_analysis.sh` / `rpccache_deep.sh`）：

| 检查项 | 结果 |
|---|---|
| master rpc-server 是否支持 cache | `/opt/llama.cpp/ggml-rpc-server -h` 含 cache 选项（v6.0.0） |
| 9859 rpc-server 是否支持 cache | 需比对（`rpccache_deep.sh` §2，待回填） |
| A/C worker 当前进程启动参数 | **未带 `-c/--cache`**（`ps aux` 待回填） |
| 缓存目录 `~/.cache/llama.cpp/rpc/` | 待回填（`rpccache_analysis.sh` §3） |

**结论方向**：llama.cpp 的 RPC 权重缓存在 **worker 侧**——`ggml-rpc-server` 需带 `--cache` 启动才会把收到的权重落盘复用。本次 A/C worker 按旧命令（无 `--cache`）启动 → 每次加载全量推权重，表现为"无 rpccache"。

### 3.2 走 WiFi：环网 ARP 异常 → 10.10 段物理不通

依据（`tmp/rpccache_analysis.sh` §1/§4）：

| 检查项 | 结果 |
|---|---|
| B 环网接口（thunderbolt0/1）ARP 记录 | **A/C 的 10.10.x IP 出现在错误接口 / STALE / FAILED**（跨接口漂移） |
| `ping 10.10.10.1`（A） | **100% 丢包**（此前实测） |
| `ping 10.10.11.3`（C） | **100% 丢包**（此前实测） |
| WiFi 段 ping | 通（因此回退走 WiFi） |

**物理连线已由用户确认未变动**（A↔B、B↔C 环网线仍插在原有口）。故 ARP 异常不是"拔线/换口"导致，方向指向：

1. **接口枚举漂移**：重启后 thunderbolt 网卡接口编号/链路层地址（thunderbolt0/1）与旧配置错位，静态 IP 仍配在旧接口上；
2. **历史 ARP 残留**：ARP 表跨接口残留（A 的 10.10.10.1 被记录在 thunderbolt1，实际在 thunderbolt0），路由/邻居表错乱未刷新；
3. **链路层协议未协商**：雷电/USB4 链路协商失败但物理线正常（对端接口名/驱动加载顺序变化）。

**注意**：9/1 崩溃复现（§3.1 崩溃 1）时 `--rpc 10.10.10.1:50052,10.10.11.3:50052` 尚能启动且 A/C 有等待连接迹象——说明**当时环网至少握手可用**；本次可能是在多次 kernel panic 强制重启后，B 的 thunderbolt 接口状态漂移导致 10.10 段不通。

---

## 4. 临时对策（已用 / 建议）

| 优先级 | 动作 | 说明 | 状态 |
|---|---|---|---|
| ✅ 已用 | **RPC 暂走 WiFi 网段** | `192.168.1.33 / .24 :50052` 连通正常，V4 加载流程可推进；仅带宽吞吐低于环网 | 环网恢复后已回归 |
| P0 | **A/C worker 重启时加 `--cache`** | 启用 RPC 权重缓存，避免每次全量推权重（9/1 ~3min/次的痛点） | ⏳ 待办 |
| ✅ 完成 | **环网按 MAC 绑定根治** | 见 §5，B 站 netplan 改为 MAC 绑定，环网三站互通 | **2026-09-08 完成** |

---

## 5. 环网根治：按 MAC 绑定（2026-09-08 已执行）

### 5.1 根因确认（接口枚举漂移，非物理接线）

用户明确环网物理连线未变动。排查发现 B 站 thunderbolt-net **接口枚举漂移**：thunderbolt0/1 接口名与物理口错位，而 netplan 按接口名（`match: name:` / 危险的 `match: {}`）绑定 IP → A 段 IP 落在连 C 的口、C 段 IP 落在连 A 的口 → 10.10.x 全盲。A-C 直连段（10.10.12.x）此前因 A 站 thunderbolt1 开关未自动开启（用户手动开启）而 down。

### 5.2 物理口锚定（ARP 交叉验证 + 临时 IP 实验）

| 物理口（MAC 锚定） | 对端 | 正确网段 | 证据 |
|---|---|---|---|
| B `02:66:bd:12:dc:35`（现名 thunderbolt1） | A thb0 | 10.10.10.2/24 | 临时加地址 ping A 10.10.10.1 ✅ 0.13ms |
| B `02:00:d2:46:c4:6b`（现名 thunderbolt0） | C thb0 | 10.10.11.1/24 | 临时加地址 ping C 10.10.11.3 ✅ 0.13ms |

MAC 稳定性由历史文档印证：9/1 记录 `10.10.10.2 ⇔ MAC 02:66:bd:12:dc:35`（A站挂死根因分析 L137）与本次一致。

### 5.3 修改内容（B 站 /etc/netplan）

| yaml | 修改前 | 修改后 |
|---|---|---|
| `90-NM-27fcd87a…yaml`（netplan-thunderbolt0） | `match: {}` + 段 id `thunderbolt0` | `match: {macaddress: "02:66:BD:12:DC:35"}` + 段 id `ring-a`（消除 interface-name 约束） |
| `90-NM-73aad587…yaml`（有线连接 4） | `match: {name: "thunderbolt1"}` | `match: {macaddress: "02:00:d2:46:c4:6b"}` |
| 幽灵连接「有线连接 2」（0b59bfcb） | match name thunderbolt0、无地址 | **已删除**（sudo nmcli con delete） |

执行要点：netplan 段 id 若保留 `thunderbolt0` 会被 netplan5 反向导出为 `interface-name` 约束再次失效，故段 id 必须改为非接口名（`ring-a`）。

### 5.4 恢复验证（全链路 0% 丢包）

```
A thb0(10.10.10.1) ↔ B thb1(10.10.10.2):  min/avg = 0.091/0.099 ms  ✅
C thb0(10.10.11.3) ↔ B thb0(10.10.11.1):  min/avg = 0.273/0.285 ms  ✅
A thb1(10.10.12.1) ↔ C thb1(10.10.12.3):  min/avg = 0.214-0.702 ms  ✅（A 站手动开启 thb1 后）
B dev: thunderbolt0=有线连接4(10.10.11.1) / thunderbolt1=netplan-thunderbolt0(10.10.10.2)
```

### 5.5 后续回归路径

环网已通 → 下一步 RPC 从小模型（Qwen2.5-7B）三站冒烟（走环网 10.10.x 而非 WiFi）→ 再切 V4-Flash 完整流程。

### 5.6 重启持久性验证（2026-09-08 14:29 执行）✅

B 站 `sudo shutdown -r now` 重启，20s 恢复 SSH，等 thunderbolt 稳定后验证：

| 验证项 | 结果 |
|---|---|
| 接口名重启后是否漂移 | **是——thunderbolt0/1 名与 MAC 互换**（thunderbolt0 变 02:66:bd:12:dc:35、thunderbolt1 变 02:00:d2:46:c4:6b） |
| IP 是否跟随 MAC 而非接口名 | ✅ 10.10.10.2→thunderbolt0(MAC 02:66=A口)、10.10.11.1→thunderbolt1(MAC 02:00=C口) |
| B→A 环网 | ✅ 0% loss, 0.151ms avg |
| B→C 环网 | ✅ 0% loss, 0.635ms avg |
| 邻居表归属 | ✅ A(10.10.10.1) dev thunderbolt0、C(10.10.11.3) dev thunderbolt1（与接口名一致） |

**结论**：MAC 绑定根治方案经重启验证有效——接口名每次重启都漂移（本次实测互换），但 IP 正确归属物理口，环网稳定连通。**若无 MAC 绑定，重启后环网将再次全盲**。

---

## 6. 遗留待回填项（下次操作时补）

- A/C 当前 rpc-server 进程 cmdline 快照（`ps aux` 证据落档）；
- `~/.cache/llama.cpp/rpc/` 是否存在（缓存目录证据）；
- 9859 版 rpc-server 是否支持 `--cache`；
- 重启持久性验证：B 站重启后确认 10.10.x 仍正确落在 MAC 对应接口（验证 netplan 按 MAC 绑定在重启后生效）；
- A 站 thunderbolt1 开关未自动开启根因（boltctl 授权 / retimer 协商，需 `boltctl list` 排查）。