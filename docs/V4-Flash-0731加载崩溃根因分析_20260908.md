# V4-Flash-0731 加载崩溃根因分析（2026-09-08）

> **状态**: 已定案（两次 kernel panic 复现，根因=单文件 146G 超 UMA 池上限 + `-ngl 99` 全 offload）
> **关联**: [upstream-tracker/TRACKER.md](../spec/upstream-tracker/TRACKER.md) §1.2 / §2.4 ｜ [results-ledger.md](../spec/model-eval/results-ledger.md) §9/1 冒烟
> **落档**: Scott ｜ 2026-09-08

---

## 1. 背景与目标

V4-Flash-0731（146G MXFP4）是三站分布式推理的**首个真实负载验证目标**——它必须分布式（单机 124G < 146G），且是引擎归位 `/opt` v0.3.0（含 lightning indexer + transpose + `-sm tensor`）后的首次大模型实测，目的：对比 9/1 双机 RPC 基线（tg ~6.3 t/s）的加速幅度。

执行过程中 B 站**两次 kernel panic 强制重启**。本文档记录完整过程与定案根因。

---

## 2. 环境快照（崩溃前实测）

| 项 | 值 |
|---|---|
| 引擎 | `/opt/llama.cpp` → `llama.cpp-master-d2e206c4`（v0.3.0-dev build 0, commit d2e206c4, rpc v6.0.0） |
| 模型 | `/data/models/gguf/lmstudio-community/DeepSeek-V4-Flash-0731-GGUF/DeepSeek-V4-Flash-0731-MXFP4.gguf` |
| 模型格式 | **单文件 146G**（GGUF v3, 1328 tensors, `split.tensors.count`=1）——**非分片** |
| B 站内存 | 124G 总（UMA 统一内存，GRUB `amdgpu.gttsize=120000 ttm.pages_limit=30720000`） |
| UMA GPU 池 | Vulkan0 RADV GFX1151，~120G（gttsize 上限） |
| A/C worker | `/opt/llama.cpp/ggml-rpc-server`（v6.0.0），50052 监听正常，各 ~120G 可用 |
| 9/1 成功基线 | results-ledger L428：**分片模型** 145G Q4，双机 RPC，rpccache 不命中每次全量推权重 ~3min |

**关键差异（两次崩溃的核心背景）**：9/1 成功跑的是**分片文件**；本次用的是**未分片单文件 146G**。

---

## 3. 崩溃时间线（完整过程）

### 3.1 第一次崩溃：三站 `-sm tensor` 分片启动（约 02:50）

**启动命令**（B 站 head）：
```bash
/opt/llama.cpp/llama-server \
  -m DeepSeek-V4-Flash-0731-MXFP4.gguf \
  --rpc 10.10.10.1:50052,10.10.11.3:50052 \
  -sm tensor -c 32768 --no-context-shift --device Vulkan0 -ngl 99 -t 16 \
  --host 127.0.0.1 --port 18081
```

| 时间 | 观测 | 当时判断 | 事后回看 |
|---|---|---|---|
| t+0 | `launched pid=628682`，日志无输出 | 正常（加载中） | — |
| t+20s | 轮询日志为空，进程存活 | 加载慢 | **加载期内存已打满** |
| t+60s | 主控→B SSH **banner 超时**（TCP 通） | "加载 146G 慢，等" | **B 系统濒死信号①** |
| t+90s | A→B 内网 SSH 也超时 | 继续等 | 濒死信号②（内网独立路径也挂） |
| t+120s | C→B（10.10.11 段）SSH 超时 + 18081 health 无响应 | 仍等 | 濒死信号③（多路径全挂） |
| t+150s | A/C worker 日志：**零 tensor 推送、零连接**，内存空闲 | "tensor 还没分发" | **B 在本地加载阶段就崩，根本没到 RPC 分发** |
| t+180s+ | 主控重试 SSH 仍超时 ×N | — | B 已无响应 |
| — | 用户发现 B 无响应 → **手动强制重启** | — | **第一次 kernel panic** |

**关键判断失误**：SSH banner 超时（TCP 通）+ 多路径全挂 + worker 零连接三者齐备时，应判定 **OOM/系统濒死** 并立即请求人工介入，却按"加载慢"连续等待 3+ 分钟。

### 3.2 第二次崩溃：`-c 512` 最小验证（约 03:05）

**启动命令**（B 站单机，吸取教训的"最小验证"）：
```bash
/opt/llama.cpp/llama-cli \
  -m DeepSeek-V4-Flash-0731-MXFP4.gguf \
  -c 512 -n 1 --no-context-shift --device Vulkan0 -ngl 99 -t 16 -p "Hello"
```

| 时间 | 观测 | 当时判断 | 事后回看 |
|---|---|---|---|
| t+0 | `llama-cli pid=4714` | 正常 | — |
| t+几s | 守护脚本报 `[: : 需要整数表达式`（`free -g` 输出**为空**） | 脚本 bug（未深究） | **free 读不到内存 = 内核内存子系统已异常（濒死信号）** |
| t+几s | `Connection closed by remote host` | SSH 会话被关 | **B kernel panic，强制断连** |
| — | 用户报告 **B kernel panic** | — | **第二次崩溃** |

**关键点**：这次即使 `-c 512`（最小上下文）也崩溃——证明问题**不在上下文/KV 大小**，而是 **`-ngl 99` 全 offload + 146G 权重本身**就触发 UMA 池超限。`free -g` 空输出是崩溃前兆，被误当脚本 bug。

### 3.3 第三次崩溃：`-fit on` 自适应加载（约 03:20）

**启动命令**（B 站单机，吸取教训改用 `-fit on` 让引擎自适应设备内存）：
```bash
/opt/llama.cpp/llama-cli \
  -m DeepSeek-V4-Flash-0731-MXFP4.gguf \
  -c 512 -n 1 --no-context-shift --device Vulkan0 -fit on -t 16 -p "Hello"
```

| 时间 | 观测 | 事后回看 |
|---|---|---|
| t+0 | `llama-cli pid=…`，守护脚本（修正版）启动 | — |
| t+几s | 守护轮询显示内存攀升 | `-fit on` 未能阻止 146G 初始化分配 |
| t+… | B 再次 kernel panic（用户报告） | **第三次崩溃**——`-fit on` 无效 |

**关键点**：`-fit on`（引擎自适应设备内存）也未能规避——引擎仍试图把 146G 权重装入 Vulkan0 设备内存。

### 3.4 第四次崩溃：`-dio` 直读 + 分片文件（约 03:50）

**前置动作**：社区调研确认 [llama.cpp #19745](https://github.com/ggml-org/llama.cpp/issues/19745)（2× Strix Halo, 128G×2, USB4, 与本站同构）报告「>100GiB 模型 RPC 加载挂死，加 `-dio` 完全解决」；遂拆片（146G→4 片，`--split-max-size 49G`）+ 单机 `-dio` 最小验证。

**启动命令**（B 站单机，`-dio` 直读 I/O 绕页缓存 + 分片文件）：
```bash
/opt/llama.cpp/llama-cli \
  -m split3/v4f-00001-of-00004.gguf \
  -c 512 -n 1 -dio --no-context-shift --device Vulkan0 -fit on -t 16 -p "Hello"
```

| 时间 | 观测 | 事后回看 |
|---|---|---|
| t+0 | 拆片完成（45/45/45/12G），启动单机验证 | — |
| t+150s | SSH banner 超时（TCP 通） | 崩前信号再现 |
| t+270s | mDNS 解析失败（`.local` 消失） | **B 已再次异常（第 4 次）** |
| — | 用户确认 **B 崩溃** | **第四次崩溃** |

**关键点**：`-dio`（社区验证的 ROCm 场景 workaround）在**本集群 Vulkan 后端无效**——因为 #19745 的 `-dio` workaround 针对的是 **ROCm/HIP 后端的 HIP 缓冲问题**，而本集群是 **Vulkan 后端**，其 UMA 分配缺陷是另一个问题。这排除「缺 `-dio`」假设。

---

## 4. 根因分析

### 4.1 直接根因：UMA 架构下 `-ngl 99` 全 offload 146G 权重超池上限

```
llama-cli/llama-server 加载 146G 单文件
      ↓
--load-mode 默认 mmap（按需分页，物理内存不爆——mmap 本身无害）
      ↓
-ngl 99 = 把 1328 个 tensor 全部 offload 到 Vulkan0（GPU）
      ↓
Strix Halo UMA：GPU "显存" = 同一块系统内存池（gttsize=120000 → ~120G）
      ↓
Vulkan 要为全部 146G 权重分配 buffer → 超 120G 池上限
      ↓
Vulkan 分配失败 → 驱动层内存压力 → 内核 OOM/panic → 整机强制重启
```

**146G > 120G（UMA 池上限）** 是最底层算术：任何分片配置下，若 head（B）被要求 `-ngl 99` 全量持有或 mmap 全量 + offload 全量，都会超限。**四次崩溃（三站分片 / `-c 512` 最小 / `-fit on` / `-dio` 分片）全部在加载阶段 panic**，与后续推理无关。

### 4.1b 归因修正：根因 = 单机/本机持有 146G 超限（2026-09-08 用户反例确认）

> **⚠️ 原结论「Vulkan 后端不可行」错误**——由本集群 9/1 成功记录推翻（用户指出）。正确归因如下。

**9/1 成功记录（本集群自证，比外部案例优先）**：

| 模型 | 部署形态 | 引擎 | 结果 |
|---|---|---|---|
| MiniMax-M2.7 Q4_K_S **121G** | **双机 RPC 层分布** | `/opt/llama.cpp-9859`（4fc4ec554, 7/2, RPC v4.0.1） | ✅ tg 19.7 t/s |
| deepseek-v4-flash **145G Q4** | **双机 RPC 层分布** | `/opt/llama.cpp-9859`（同） | ✅ tg ~6.3 t/s |

**引擎版本三方交叉验证（2026-09-08 补充）**：

| 来源 | 记录 |
|---|---|
| 文档 [分布式推理.md L562](file:///d:/RPC/docs/分布式推理.md) | 「llama.cpp 版本: version 9859 (commit 4fc4ec554)，RPC 协议 v4.0.1，2026-07-02 构建」 |
| 实物（B 站 `/opt/llama.cpp-9859/` 已核实） | `libllama.so.0.0.9859` / `libllama-common.so.0.0.9859` 版本号硬编码；llama-server/ggml-rpc-server/libggml-vulkan.so+MANIFEST 全套齐 |
| 用户记忆 | 「/opt 老版本 = 最早版本」 |

**9859 = /opt 最早构建**（7/2）→ v0.2.0（8/28）→ master-d2e206c4（8/31）= 时间线完整。
**⚠️ 现役陷阱**：`/opt/llama.cpp` symlink **当前指向 master-d2e206c4（v0.3.0）而非 9859**——若复现 9/1 时仍走文档 L273/L330 的 symlink 路径，实跑引擎是 master 不是 9859。

**4 次崩溃的共同错误（与成功记录对照）**：

| 尝试 | 形态 | 本机持有量 |
|---|---|---|
| `-sm tensor -ngl 99` | head B 全量持有 + `-ngl 99` | **146G**（超限） |
| `-c 512 -ngl 99` | 单机加载 | **146G**（超限） |
| `-c 512 -fit on` | 单机加载 | **146G**（超限） |
| `-dio` + 分片 | 单机加载 | **146G**（超限） |

**正确根因**：4 次全部是「**单机/本机试图持有 146G > 124G**」——算术必崩，**与 Vulkan 无关、与引擎版本无关**。9/1 成功 = **双机 RPC 层分布，每机仅持 ~73G** << 120G → 不超限。

**两个并存差异（此前混淆）**：
1. **部署形态**：9/1=双机层分布（每机 ~73G）；4 次崩溃=单机/本机全量（146G）→ **这是崩溃的决定性变量**
2. **模型档位**：9/1=Q4 145G（9859 支持）；崩溃尝试=MXFP4 146G → 引擎对 MXFP4 的加载路径差异是**次要因素**（需在正确形态下再验证）

### 4.2 为什么 mmap 不是根因（排除）

| 假设 | 判定 | 依据 |
|---|---|---|
| mmap 导致内存爆 | ❌ 排除 | mmap 按需分页，物理内存只装访问页；146G 文件 mmap 不占 146G 物理内存 |
| 上下文/KV 导致 | ❌ 排除 | `-c 512` 极小仍崩溃 → 与 ctx 无关 |
| RPC 分发导致 | ❌ 排除 | A/C worker 零连接、零推送；B 在本地加载阶段就崩 |
| **MXFP4 格式不被引擎支持** | ✅ **排除（2026-09-08 实证）** | `/opt` v0.3.0 二进制含完整 MXFP4 支持：`libggml-base`（dequantize_row_mxfp4/quantize_mxfp4）、**`libggml-vulkan`（arr_dmmv_mxfp4_* 反量化内核）**、`libggml-cpu`（gemm/gemv_mxfp4）；上游 2026-05 官方公告 gpt-oss 原生 MXFP4 全后端支持——**V4-Flash-0731-MXFP4 的格式完全受支持，非崩溃诱因** |
| **缺 `-dio`** | ✅ **排除（第 4 次崩溃）** | 社区 #19745 workaround 针对 **ROCm/HIP 后端**；本集群 **Vulkan 后端**加 `-dio` 仍崩 |
| **`-fit on` 能规避** | ✅ **排除（第 3 次崩溃）** | 引擎自适应设备内存仍试图装入 146G |
| **UMA 池超限（Vulkan 后端）** | ✅ **主因** | 146G > 120G 池上限 + Vulkan 后端 UMA 分配缺陷（beowulf 实证），四次崩溃全部复现 |

### 4.3 叠加因素

1. **单文件 vs 9/1 分片**：9/1 成功的分片模型可让各站 `-ot` 只持部分层；单文件 146G 无法按站切分，B 必须全量面对
2. **无加载期监控**：未在 A/C/B 侧起 `free`/`vmstat` 轮询，崩溃前兆（free 异常/SSH 超时）无法早期识别
3. **无渐进验证阶梯**：直接从"单机最小加载→双机→三机"跳到最后生产形态（三站全参数），跳过了 `-c 512` 单机先验证格式可 load 的步骤（第二次崩溃正是补做该步骤时发生）
4. ~~MXFP4 格式支持未验证~~ → **已排除（§4.2）**：v0.3.0 原生支持 MXFP4（含 Vulkan 内核）

### 4.4 9/1 成功 vs 本次崩溃对比

| 维度 | 9/1 成功冒烟 | 本次崩溃 |
|---|---|---|
| 文件 | **分片** 145G Q4（5 片） | **单文件** 146G MXFP4 |
| 引擎 | 双机 RPC（旧链路） | /opt v0.3.0 + rpc v6.0.0 |
| 加载 | rpccache 不命中，全量推权重 ~3min | 本地加载阶段即 panic |
| tg | ~6.3 t/s（无 V4 Vulkan 算子，CPU 回退） | 未到推理阶段 |

---

## 5. 反思（判断失误清单）

| # | 失误 | 教训 |
|---|---|---|
| 1 | 146 > 120 的算术未在启动前拦截 | 加载前必须算内存账：`模型大小 vs 站内存/UMA 池` |
| 2 | 无渐进验证，直接上三站全参数 | 阶梯：`-c 512` 单机最小 load → 单机满 ctx → 双机 → 三机 |
| 3 | SSH 超时 + worker 零连接齐备仍判"加载慢" | OOM 三信号（TCP 通+SSH 挂+worker 无连接）齐备 = 立即判定濒死 |
| 4 | `free -g` 空输出被当脚本 bug | 内存读取异常 = 内核异常前兆，非脚本问题 |
| 5 | 无失败回滚预案 | 应固化"失败即 kill + 恢复 18081 常规服务"一键命令 |

---

## 6. 修正方向（2026-09-08 归因修正后定案）

> **撤销「弃 Vulkan 转 ROCm」**——那是归因漂移的错误产物。正确路线 = **回到 9/1 成功形态（双机/三机 RPC 层分布）**，先验证再扩展。

| 优先级 | 动作 | 说明 |
|---|---|---|
| P0 | **恢复 B 18081 常规 qwen3.8 服务** | B 已多次崩溃，恢复日常推理第一优先 |
| P0 | **按 9/1 成功形态验证**：双机 RPC 层分布（`llama-server --rpc A,C -sm layer`，**不用 `-ngl 99`**，引擎自动分到各机，每机 ~49-73G） | 9/1 用 9859 + 双机分布成功；先复现同形态 |
| P1 | **引擎选择**：9859（v4.0.1，9/1 实证支持 Q4）vs master-d2e206c4（v0.3.0，支持 MXFP4 + 新算子） | 9/1 档位是 Q4；MXFP4 需确认 9859 是否支持 → 若否，master 或换 Q4 档 |
| P1 | **模型档位**：Q4 145G（9/1 实证）或 MXFP4 146G（需在正确形态下验证） | 先 Q4 复现成功 → 再试 MXFP4 |
| P2 | **失败回滚** | 固化一键恢复 18081 常规服务命令 |
| ~~P0~~ | ~~弃 Vulkan 转 ROCm/HIP~~ | **已撤销（归因修正）** |

---

## 7. 结论

**V4-Flash-0731（146G）在本集群的崩溃根因是「单机/本机试图持有 146G > 124G 物理内存」**——4 次崩溃全部是这种形态（算术必崩），**与 Vulkan 无关、与引擎版本无关、与 MXFP4 无关**。本集群 9/1 已实证的可行路径是 **双机 RPC 层分布**（MiniMax-M2.7 121G / v4-flash 145G Q4 均成功，每机 ~73G，用 `/opt/llama.cpp-9859`）。正确方向 = 复现 9/1 形态（`--rpc` + `-sm layer`，不用 `-ngl 99`），再扩展三站与 MXFP4 档。

---

## 7b. 三站分布式成功复现（2026-09-08 15:00 收口）✅

> **「弃 Vulkan 转 ROCm」已撤销（© §4.1b 归因修正）——Vulkan + 层分布 + 分片在三站环网下完全可行**。崩溃不是 Vulkan 的错，是「单机全量持有」的错。

### 成功形态（与 4 次崩溃的差异）

| 维度 | 4 次崩溃 | ✅ 本次成功 |
|---|---|---|
| 文件 | 单文件 146G / 单机加载 | **分片 4×45G**（split3/） |
| 部署 | 单机/本机全量持有 | **三站 `-sm layer` 层分布** |
| offload | `-ngl 99` | **不用 `-ngl 99`**（引擎按层自动分配） |
| 触发动作 | 无 RPC 实际分发 | 环网 RPC（10.10.10.1:50052, 10.10.11.3:50052） |

### 启动命令（B 站 head）

```bash
/opt/llama.cpp/llama-server \
  -m split3/v4f-00001-of-00004.gguf \
  --rpc 10.10.10.1:50052,10.10.11.3:50052 \
  -c 4096 -sm layer --port 18099 --host 127.0.0.1 -t 16
```

### 结果

| 指标 | 值 |
|---|---|
| 加载耗时 | **5m21s**（A/C 全量接收权重；与 9/1 ~3min 基线同一量级） |
| 三站内存峰值 | B=55G / A=60G / C=72G（**均 < 110G 阈值，无 OOM**） |
| /health | `{"status":"ok"}` |
| 生成冒烟 | 中文问候输出 ✅（reasoning→content 完整链路） |
| **decode** | **7.93 t/s**（9/1 双机 Q4 基线 ~6.3 t/s，**+26%**） |
| prompt | 10.71 t/s |
| 已知警告 | `resolve_fused_ops: layer 28 ... HC pre/comb/post assigned to RPC`（#26578 未合入，Vulkan HC 融合慢路径，decode 上限瓶颈） |

### 与 9/1 基线的差异说明

- 引擎：master-d2e206c4（v0.3.0, rpc v6.0.0）vs 9/1 的 9859（v4.0.1）→ 新引擎含 lightning indexer / transpose 算子，prompt 侧更快
- 形态：三站 vs 双机；MXFP4 146G 分片 vs Q4 145G 分片

---

## 7c. FAQ：单文件 vs 分片（2026-09-08 追加）

> 用户提问：「单文件是否也是崩溃原因」「单文件存在意义」「单文件是否提高吞吐」。结论先行：**单文件不是根因（崩溃④用分片仍崩）；单文件对吞吐≈零影响**。

### 7c.1 单文件不是根因——四证据

| 尝试 | 文件形态 | 部署形态 | 结果 |
|---|---|---|---|
| 崩溃① | 单文件 146G | 三站 `-sm tensor` + `-ngl 99` | panic |
| 崩溃② | 单文件 146G | 单机 `-c 512` + `-ngl 99` | panic |
| 崩溃③ | 单文件 146G | 单机 `-fit on` | panic |
| 崩溃④ | **分片 4×45G** + `-dio` | 单机 | **仍 panic** |
| ✅ 成功 | 分片 4×45G | 三站 `-sm layer`，无 `-ngl 99` | 跑通 7.9 t/s |

崩溃④已拆片 + `-dio` + `-fit on` 仍崩 → 决定性变量是**「单机/本机全量持有 146G > 124G UMA 池」**，与文件形态无关。

### 7c.2 单文件存在意义

单文件是**「权威源」而非部署形态**：分发/归档/md5 一致校验简单；兼容需直接 `-m 单路径` 的工具；单机内存充足时最简形态；元数据读取（`llama-gguf r`）一次到位。分片只是派生部署形态。

### 7c.3 单文件加载不会提高吞吐

分片只影响 **I/O 层**（磁盘读取），不影响**计算层**（张量计算）。multi-file 加载按 tensor 顺序拼接，mmap 语义与单文件一致；RPC 层分布时 head 逐 tensor 读出推送，片边界与张量边界对齐与否对总耗时可忽略。

**吞吐决定因素 = 算子与融合，不是文件形态**：

| 因素 | 状态 | 影响 |
|---|---|---|
| lightning indexer / transpose 算子 | ✅ master 已含 | 本次 6.3→7.9 t/s (+26%) 主因 |
| #26578 HC 融合（decode 1.5× / prefill 1.12×） | ⚠️ 未合入 | decode 上限瓶颈（当前 unfused Sinkhorn 慢路径） |
| 层分布策略 `-sm layer` | ✅ 已用 | vs 崩溃形态 `-ngl 99` 全量持有 |
| 后端路线 | ROCm 社区 32t/s 对照 | 架构级差异，见 survey |
| **文件形态单/分片** | — | **≈零影响** |

---

## 8. 佐证与产物

- 崩溃命令：`d:\RPC\tmp\start_v4_flash.sh` / `v4_min_test.sh` / `v4_progressive1.sh` / `v4_progressive2.sh`（本地留档）
- 分片产物：`/data/models/gguf/lmstudio-community/DeepSeek-V4-Flash-0731-GGUF/split3/`（v4f-0000{1-4}-of-00004.gguf，45/45/45/12G）
- B 站日志：`/tmp/v4_flash_18081.log` / `v4_min_test.log` / `v4_progressive1.log` / `v4_progressive2.log`
- A/C worker 日志：`/tmp/rpc_new_50052.log`（证明 worker 未参与，B 本地加载阶段即崩）；**成功日志：`/tmp/v4f_ring.log`（B）、`/tmp/rpc-server-a-cache.log` / `rpc-server-c-cache.log`（A/C，含 set_tensor 落盘 rpccache）**
- 模型元数据：`llama-gguf r` 输出（deepseek4 架构，1328 tensors）
- 引擎：`/opt/llama.cpp` v0.3.0-dev（deepseek4 + MXFP4 Vulkan 内核；**本次成功证明 Vulkan 后端在层分布形态下可用**）
- 社区对照：[beowulf #2](https://github.com/geerlingguy/beowulf-ai-cluster/issues/2) / [Lucebox](https://www.lucebox.com/blog/deepseek-v4-strix-halo) / [HipFire](https://www.localmaxxing.com/en/hardware/UNIFIED%3Aryzen%20ai%20max%20395) / [tinycomputers](https://tinycomputers.io/posts/running-deepseek-v4-flash-on-amd-strix-halo.html) / [#19745](https://github.com/ggml-org/llama.cpp/issues/19745)
