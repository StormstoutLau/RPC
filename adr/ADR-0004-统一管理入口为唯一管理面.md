# ADR-0004: 统一管理入口为唯一管理面（禁止重复生成一次性脚本）

---------------------------------------------------------------------

id: ADR-0004
type: adr
version: 1.0
status: accepted
date: 2026-09-15
depends: \[ADR-0001, ADR-0002]
upstream: \[ADR-0001]
---------------------------------------------------------------------

> **Feature**: 把「所有管理操作从统一管理入口执行」定为硬规则；存量一次性脚本冻结（只减不增），
> 并用门禁断言 `scripts` 绑定执行。
> **创建日期**: 2026-09-15
> **状态**: accepted
> **适用**: `ops/` 下一切脚本的增删；集群管理、模型加载、凭据、出站、观测、门禁等全部管理动作

---------------------------------------------------------------------

## 元数据

| 字段   | 值 |
| ---- | ---- |
| 编号   | ADR-0004 |
| 日期   | 2026-09-15 |
| 状态   | accepted |
| 决策者 | Scott (鹏) |
| 相关文档 | [三机推理集群使用手册](../docs/三机推理集群使用手册.md)、[ADR-0001](ADR-0001-集群运维框架审计与四项改进决策.md)（框架审计）、[方案 v2 §A.6](../docs/2026-09-15_统一管理入口_深入分析与优化方案_v2.md)、`inventory/ops.yaml`、`ops/rpc_check.py` 断言 `scripts` |
| 取代   | 无（新增） |

**证据等级约定**: E1=本会话实测；E2=前会话实测留档；E3=外部文档；E4=推断。

---

## 背景（Context）

1. **存量已经失控（E1）**：`ops/` 下 git 跟踪的**脚本共 256 个**，其中 **230 个属历史一次性脚本** ——
   `ops/station-bin/_*.sh` 一片（`_aportchk.sh` / `_bportchk.sh` / `_astate.sh` / `_bocdbg.sh` /
   `_qwen27_*.sh` …），另有 `repro-*.sh` ×6、`check_llama_version.{sh,py,ps1}`、`make-dashboard.ps1` 等。
   命名本身就承认了性质：`_` 前缀 = "临时的"。
2. **它们与统一入口能力重复（E1）**：端口/状态/加载/卸载/凭据/出站/版本/观测这几类动作，
   `cluster.py` 与 `cluster_web.py` 都已有对应子命令或卡片，而且是**三站一次做完 + 有判据 + 有台账**；
   而 `_xxx.sh` 是**单站、无判据、无留痕**的一次性动作，做完没人再读。
3. **它带来的不是便利而是复利式债务**：每次新需求 → 再写一个脚本 → 下次要改同一件事时，
   得先判断"该改哪一个"；改错了没人知道（没有门禁看它们）。这正是 ADR-0001 记录的
   "40+ 手工校验脚本却没有总入口"的同类问题，只是发生在管理面上。
4. **既有纪律已作方向性表态、但缺执行**：手册早已写"多个 Python 工具应并入 cluster.py 子命令"、
   方案 v2 把观测类能力逐项收进入口（`reqlog` / `ttl` / `agent` / 各 flow）。缺的是**拦住新增**的机制 ——
   只靠自觉的规则在本仓库被证明会失效（同 2026-09-14 调研的方法论教训）。
5. **`tmp/` 通道已存在且被 `.gitignore` 覆盖（E1）**：一次性验证脚本有正确去处，
   不需要占用 `ops/` —— 本会话的验证脚本（`_verify_agent_p0.py` 等）全部落在 `tmp/`，不入库。

---

## 决策（Decision）

### D1. 唯一管理面 = `ops/cluster.py`（+ 同一入口的 web 形态 `ops/cluster_web.py`）
所有**管理操作**（集群/模型/凭据/出站/观测/门禁/流程）一律从统一入口执行：
CLI 子命令，或 web 卡片（同一份取数实现）。**不再新增并列入口**。

### D2. 存量冻结：`inventory/ops.yaml` 登记 + 门禁断言（只减不增）
- 登记表三张清单：`entry`（入口本体）· `station_runtime`（被调方）· `frozen_ops_scripts`（历史存量）。
- 新增断言 `scripts`（`ops/rpc_check.py`，quick 项，进 pre-commit/pre-push）：
  `ops/` 下任何脚本必须 ∈ 三张清单之一，否则 **FAIL**；入口件缺失亦 FAIL。
- 依据与 `plugins.yaml` 的 `known_drift`、`models.yaml` 的 `non_model_conf` **同一套纪律**：
  真实差异可登记，但登记这个动作本身要在 review 里可见。
- **断言范围 = git 跟踪文件**（与门禁其余各项一致）：新脚本在 `git add` 后即进入范围，
  故 **pre-commit 就能拦住**（实测：新增未登记脚本 → `未登记 1` → FAIL 并点名）。
  反向推论：把一次性脚本放在 `tmp/`（已 gitignore）永远不会被本断言看到 —— 这正是想要的边界。

### D3. 新增脚本只有三条合法路径（按优先级）
| 序 | 情形 | 做法 |
|---|---|---|
| ① | **默认**：这是一项管理能力 | 给 `cluster.py` 加子命令（+ 需要时在 web 加卡片）；一次覆盖三站、带判据 |
| ② | 它是**被入口/systemd 调起的站上运行时件** | 加进 `station_runtime`（如 `infer-load`、`cluster-ttl`） |
| ③ | 确需独立一次性脚本 | 加进 `frozen_ops_scripts` + **在提交信息里说明为何不能并入入口** |
| — | 临时验证/探针脚本 | **放 `tmp/`**（gitignore 覆盖；本断言看不到它，也不该看到） |

### D4. 例外边界：什么算"站上运行时件"而不是"并列入口"
判据 = **它是否自带管理语义**：
- 有子命令/参数解析/状态输出，能被人直接当命令用 → 属**入口**，必须并入 D1（否则就是并列入口）。
- 只是被调起的实现细节（读 conf 起进程、探端口、采一次指标）→ 属 `station_runtime`。

### D5. 清减顺序：**先补入口能力，再删脚本**（顺序不能反）
冻结清单里的脚本逐条判：能力已被入口覆盖 ⇒ 删；能力仍需要 ⇒ **先**给入口加子命令/卡片，
验证通过 **再**删脚本，并在同一次提交里把它从 `inventory/ops.yaml` 移除。

**已核实的清减候选（E1，可作为第一批）**

| 存量脚本 | 入口已有 | 证据 |
|---|---|---|
| `ops/station-bin/_aportchk.sh` / `_bportchk.sh` | `rpc_check.py --only ports,stations`（端口真值表自洽 + 三站占用对账） | P1-4 落地：表内自洽 + 站上占用对账，且注入故障 16/16 抓到 |
| `ops/check_llama_version.{sh,py,ps1}` | `cluster.py versions`（三站引擎版本矩阵）+ web「引擎版本矩阵」卡片 | `cmd_versions` 存在且手册已收录 |
| `ops/station-bin/make-dashboard.ps1` + `dashboard.html` | web「Agent 任务」卡片 + `cluster.py agent` | 本会话 P1 落地；快照不标新鲜度，已被取代 |

> 其余 200+ 条需逐条判（不做"看不出来就批量删"这种危险动作）。

---

## 实施记录：第一批清减（2026-09-15）

**D5 当场拦下了我自己的一处误判 —— 这正是这条顺序存在的意义**

上表把 `check_llama_version.*` 标为"可删"，理由是"`cluster.py versions` 已覆盖三站版本矩阵"。
真按 D5 逐条核对时发现**覆盖不完整**：

| 事实（E1） | 结论 |
|---|---|
| 旧脚本做 **MANIFEST 全量 `md5sum -c`** + 读 `rpc_protocol` | 入口的 `versions` 两样都没有 |
| 入口的"完整性"列三站全显示 **缺失** | 它只找 `MANIFEST.md5`，而站上文件叫 **`MANIFEST`**（md5 段在第 10 行起）⇒ **长期假阴性**：能力看着坏了，其实数据一直在 |
| 旧脚本里 C 站 IP 仍是 `192.168.1.24`（现为 `192.168.1.37`） | 旧脚本对 C 站**本就跑不通**（exit 2） |

**动作（顺序不可反）**：先把能力并入入口 → 验证 → 再删脚本。

1. `_VER_SCAN` 的完整性段改为：有 `MANIFEST.md5` 用它，否则用 `MANIFEST` 的 `[md5]` 段（`tail -n +10`）；
   并把 `rpc_protocol` 纳入采集与三站一致性比对。
2. **顺带修掉第二个真缺陷 —— locale 陷阱**：站上 `LANG=zh_CN.UTF-8`，`md5sum -c` 打印"**成功**"
   而不是 `OK` ⇒ 按英文文本解析会把**全部通过**读成"**全 FAILED**"（实测首跑就是这样）。
   修法：`LC_ALL=C md5sum -c`。**凡是解析命令输出的地方都必须锁 locale** —— 与 memory 里
   "`free` 输出随 locale 变化 ⇒ 改读 `/proc/meminfo`" 是同一条教训的复发。
3. 验证：`cluster.py versions` → 三站 `完整性 79 OK`、`rpc_protocol v6.0.0` 三站一致。
4. 删除 7 个文件：`_aportchk.sh` `_bportchk.sh` `check_llama_version.{sh,py,ps1}` `make-dashboard.ps1`
   `dashboard.html`（后两个中 `dashboard.html` 非脚本，不入登记表）；`inventory/ops.yaml` 重新生成
   （脚本 256 → 250，冻结存量 230 → 224）；**站上无残留副本**（三站 `NONE`）。
5. 引用修正：手册（`dashboard.html` → 已删）、`spec/vulkan-version-control/UPGRADE_SOP.md`
   （`check_llama_version.sh --deep` → `cluster.py versions`，并注明旧脚本 C 站 IP 过时）。
   历史记档（`docs/DEV-LOG-008`、`.trae/documents/*`、`docs/security/2026-09-13_*` 事故清单、
   `archive/*`）**有意不动** —— 它们记录的是当时的事实，不属于"指向未来的指令"。

> 第一批的经验：**"能力看起来重复" 不等于 "能力已被覆盖"**。判断必须落到"逐项功能对得上"，
> 而不是"入口有个名字差不多的子命令"。这也是本 ADR 把清减顺序写成硬步骤的原因。

---

## 实施记录：第二批清减（2026-09-15）

**判据换成更强的两条（可机械判定）**：第一批用的是"能力是否已被入口覆盖"（需逐条判断，且我判断错过一次）。
第二批改用两条**不需要判断能力**的判据 —— 满足任一条即可删，因为**脚本本来就跑不通或跑起来有害**：

| 判据 | 数量 | 说明 |
|---|---|---|
| ① 脚本自标 `DEPRECATED` / `勿执行` | 8 | 密钥脱敏后逻辑失效，且**重跑会把 `***REMOVED***` 占位符写进生产配置**（opencode/claude settings） |
| ② 含 `***REMOVED***` 占位密钥 + 认证必需 | 19 | 密钥已被脱敏成占位符 ⇒ **认证请求必然 401**，脚本已是空壳（含 4 个 `_bench*.py` unsloth 基准，能力已由 `flow bench` 承接） |
| ③ 目标子系统已退役 | 4 | `_bs2_*` 是 LiteLLM 网关（`:4000`，2026-09-13 退役）的探针 ⇒ 靶子不存在 |

**结果**：脚本 250 → **219**（冻结存量 224 → **193**）；git 跟踪文件里含占位密钥的从 36 个降到 **5 个**
（其余 5 个是：两份 security 事故记档、`spec/d2-cluster-cli/IMPLEMENTATION.md`、本 ADR 引用的 runbook、
以及 `ops/cluster.py` 里那条"占位符不命中"的检测器注释）。

**引用反查（删前必做）抓出一处真正的操作风险**：
`ops/station-bin/REPRO-RUNBOOK.md`（**活跃操作手册**）在 §1.3 与 §3 仍写着
"unsloth 重铸 key 后**必须重跑 `_bkeyupdate.sh`**" —— 而该脚本 2026-09-13 已标"勿执行"。
**照着手册操作会把占位符写进生产配置**。已改为走入口：

```powershell
python ops/cluster.py secrets status   # 看落点/引用/权限
python ops/cluster.py secrets push     # 从主控正本 secrets/stations/<站>/ 下发
python ops/cluster.py providers        # 复验 provider/默认模型无漂移
```

> 即"删脚本"顺带修掉了一处**会按旧手册踩坑**的隐患 —— 这正是 ADR-0004 想要的副作用。

**反查结论**：三站 `/usr/local/bin` 与 `/tmp` **均无副本**、systemd 单元不引用；仓库内 12 处引用
**全部属历史记档**（`DEV-LOG-011`、`docs/security/2026-09-13_*` 事故/轮换清单、`ADR-0002`、
`spec/model-eval/results-ledger.md`、`archive/*`）—— **有意保留**（记录当时事实）。
唯一"指向未来"的一处是 `docs/双端点部署与opencode混合框架调研.md` 把 `_bs2_*` 当"可复用先例"，
已改为指向 **git 历史**。

---

## 实施记录：第三批清减（2026-09-15）

**判据（比第二批更机械）：脚本显式引用已不存在的靶子** —— 由 `grep` 判定，不需要任何主观判断：

| 簇 | 数量 | 靶子状态（实测） |
|---|---|---|
| LiteLLM 网关簇 `_bs2*` ×8 + `_bcur.sh` | 9 | `:4000` 在**主控与三站均无监听**；三站 `ps -eo args` **无 litellm 进程**（网关 2026-09-13 退役） |
| claude 死端口写配置 `_acldset.sh` `_acldoverride.sh` `_acldofficial.sh` | 3 | 硬编码 `ANTHROPIC_BASE_URL=http://127.0.0.1:8087` —— 该端口**未登记且全集群无监听**；而现行生产配置是 `:8080` + 真实 token（B 站 `~/.claude/settings.json` 实测） |

**`_acldset.sh` / `_acldoverride.sh` 还带额外风险**：它们会**整体覆盖**生产 `settings.json`，写成
死端口 + `ANTHROPIC_AUTH_TOKEN='dummy'` ⇒ 今天跑一次就把 claude 打断。其作用（同步 claude/opencode
配置到后端）已由 `cluster.py secrets push`（正本在主控 `secrets/stations/<站>/`）承接。

**⚠ 本次核查中我自己踩了一次 `pgrep -f` 自匹配（值得记下）**：
第一版核实写的是 `ssh host 'pgrep -f litellm'` —— 三站**全部**报 `RUNNING`。
真因：`ssh` 那侧的包装 shell **命令行里就含 "litellm" 这个模式**，`pgrep -f` 匹配到了它自己。
**改用 `ps -eo args | awk` 实查才得到真相（三站都没有 litellm 进程）。**
教训：**跨 ssh 的 `pgrep -f` 必然自匹配**（包装层自带模式）；这与 RPC 排查时那条 `pgrep -f` 陷阱同源，
只是这次犯在我自己的核查脚本里。凡"进程是否存在"的判据，一律走 `ps args` / `pgrep -x` 并核对全文。

**结果**：脚本 219 → **207**（冻结存量 193 → **181**）。
**引用反查**：仓库内除登记表**零引用**；三站 `/usr/local/bin`、`/tmp`、`$HOME` **无副本**；systemd 不引用
⇒ 本批**无需改动任何文档**。

---

## 实施记录：第四批 = 文档漂移审计（2026-09-15）

**触发**：用户提出「三站配置实况与手册/派发表存在漂移，当前很多历史文档是否已经漂移」。前三批清减的是**脚本**，
本批清的是**文档**——同一问题的另一半：脚本删了/改名了/搬家了，引用它的文档不会自己失败，只会**继续自信地教人做错事**。

### 方法（四步，缺任一步都会得出错误结论）

1. **实况取证**：直接读三站 `/etc/llama-instances/*.env`、监听端口、`which` 站上工具链 —— 一切以站上实况为准
2. **机械扫描**：全仓 md 按可判定模式扫（已删脚本名 / 死端口 / 旧 IP / 失效相对链接）
3. **逐条核对（防误报的关键一步）**：机械命中 ≠ 漂移。必须区分三类 ——
   **① 真漂移**（文档在教今天的操作，但内容是昨天的）→ 改
   **② 历史记档**（明确带日期、写的是当时事实）→ **保留原文 + 加现状注记**，不改写历史
   **③ 假阳性**（我自己刚加的注 / 本来就对的值 / 有日期的实测记录）→ 不动
4. **分级处置**：能改的改；"改了就篡改历史"的加注记；纯记档的不动

### 真漂移与处置

| 类别 | 位置 | 实况对照 | 处置 |
|---|---|---|---|
| **派发表/路由** | 手册 §2.2「路由规则硬编码」写 "llama-rpc 类拦截并打印手动步骤 (exit 2)" | `RPC_MODELS` 已由 `_load_rpc` **自动化**（P0-3）；`qwen3.8-flash-next` 已移出 RPC 类 | 改写；并补 `STATION_ROUTES`（按站路由名）说明 |
| **退出码语义** | 手册 "2 = RPC 类需手动" | `return 2` 实际只有两处：别名解析失败 / flow 用法错 | 改写 |
| **代码即文档** | `cluster.py` 模块 docstring 同一句话 | 同上 | 改写 |
| **隧道方案已废** | d6 规范 5 处（ARCHITECTURE 图/铁律/接缝表、DECISIONS D-14/D-15、OPEN-ISSUES O-10） | 跨站派发已改 `agent-cli --RemoteHost <站>` + 站内 `_station_ready.sh` 自发现端口；`_bs2_cross.py` 已删；B 站 `18081` 现被 conf `davidau-q38-27b-q4k` 声明占用 | 逐处加现状注记（决策**意图**仍有效，只废弃链路形态） |
| **网关路径** | `docs/双端点部署与opencode混合框架调研.md`（jsonc 留档 / 层②表 / A 站指引） | LiteLLM `:4000` 2026-09-13 退役；provider 名保留但 baseURL = 站上引擎真实端口 | 顶部加现状注记 + 行内标注；历史记档保留 |
| **仓库路径前缀** | `spec/cluster-bench/DESIGN.md` 两处 `d:\RPC\scripts\`；手册真值表 `scripts/*.sh` | 该目录**已不存在**（现役件在 `ops/`，退役件在 `archive/scripts-history/`） | 改指真实落点 |
| **C 站 open issues** | `spec/station-c/DEPLOYMENT.md` O3/O7「接入路由待决策」、O5「claude 未配」 | O5 已解决（`settings.json` baseURL `127.0.0.1:8080` + 实测 `end_turn`）；C 站 station_runtime 工具链 **11/11 已装**、统一入口 `STATION_ROUTES` 已含 3 条 C 条目 | 标注收口/部分收口（`agent-cli $ROUTE_TABLE` 仍无 C 模型别名，属实） |
| **失效相对链接** | 全仓 **65 条** | 见下 | 机械修正 61 条 |

### 失效相对链接（本批最大的一块，65 条）

两个病根，都是"**前缀写重**"：

- `spec/<x>/` 里的 `../spec/y` ⇒ 解析成 `spec/spec/y`；`../docs/z` ⇒ 解析成 `spec/docs/z`
- 实体搬家（脚本进 `archive/`、文档进 `research/`）后链接没跟着走

修正方式**限定为"能唯一确定新目标"**才动手：目标当前不存在，且在候选解析（仓库根 / `docs/` / `spec/` / 逐级上升）
中**恰好一种**存在 ⇒ 改写；多种可能 ⇒ **不动作只报告**（不猜）。结果：**65 → 4**，4 条各自有正当理由
（2 条是 vendored 技能缺 `references/`、1 条是被并入他文的旧文件名、1 条是模板占位符）。

### 本批发现但**不改**的（假阳性清单，防下次重复劳动）

- 手册 `gpt-oss-120b → A 站` **本来就是对的**（与 `ROUTE = {...}` 一致）
- `spec/station-c/DEPLOYMENT.md` §1 C 站 SSH 行、`spec/upstream-tracker/TRACKER.md` 的 `192.168.1.24`/`18081` 行
  **都已正确标注**"已弃用/过期" ⇒ 不是漂移
- `2026-09-15_统一管理入口…v2.md` 里「B 站 systemd `:18080`」**实测正确**（B 站 `qwen3.8-27b-mtp.env` 的 `PORT=18080`）
- `REPRO-RUNBOOK.md` 的两处命中是**我上一批自己加的注**（在 grep 里长得像漂移）
- `spec/model-eval/results-ledger.md`、`docs/V4-Flash-*` 等带日期的实测记录 ⇒ 历史记档，不动

### 能力进统一入口（D3 路径①）

漂移**没有门禁就必然复发**，故本批不满足于"改完这次"：

- 新增断言 **`doclinks`（文档链接可达）** —— 见 `ops/rpc_check.py`，`quick=True`
- 判据：md 里的**仓库内相对链接**必须可达；外链 / 锚点 / 占位词 / 解析后落到仓库外的一律**不判**（避免噪声）
- 真实差异可登记 `DOCLINK_ALLOW`（**登记动作在 review 里可见**，同 `known_drift` 纪律）
- **自证（正+负）**：正 → 实跑 `144 md / 840 链接 / 失效 0` PASS；负 → 注入一条坏链（并同时注入
  "好链 / 库外 / 占位 / 外链"四个对照）⇒ **只报那一条坏链**、退出码 1
- 门禁总数 **13 → 14 项**（quick 8 → 9），手册门禁块已同步；全量复跑 **13 绿 / 1 黄 / 0 红**（黄灯为既有的 C 站 `claude.key` 占位串）

### 教训沉淀

1. **"机械命中"不等于"真漂移"** —— 不区分「真漂移 / 历史记档 / 假阳性」就批量改，等于**篡改历史**。
   本批 65 条里真正需要改文档内容的只有 7 处，其余是链接路径或历史注记。
2. **文档漂移方向是单向的**：代码改了会红，文档改了不会。所以**只能靠门禁**，不能靠自觉。
3. **可判定的判据才配写进门禁**：`doclinks` 特意**放弃**判"语义过时"（那需要人读），只判"链接可达"
   —— 判不准的项写成门禁只会制造噪声，最后被人整体忽略。

### 补记：`Agent跨项目调用标准与迁移复用调研.md`（第四批续，用户追问）

用户追问「C 站已加入实际工作，这篇文档是否也要更新」。答复：**要更新，但走"加现状注记"而非改写**——
它是 `status: draft（D6 前期调研）` + 带日期版本头的**史档**，正文 510 行按「两站 (A/B)」写成；
改写它等于篡改历史（本批刚沉淀的那条纪律）。

实测取证（`import` 级验证，不信文本解析）后在该文顶部加四段注记：

| 项 | 实测（2026-09-15） | 结论 |
|---|---|---|
| 三站 agent 生态 | `~/.claude/skills/` 各 12 件、`~/.config/opencode/skills/` 各 4 件、opencode 1.18.25 + claude 2.1.258 三站同版 | 同构成立，**C 站确已参与** |
| agent 工作区 | A=`Cpp_Hub,paper,v0probe-a` / B=`Cpp_Hub,paper,v0probe` / **C=`paper`** | C 站在跑真实项目 |
| §9.8 **G8 依赖** | sympy：**仅 B**（1.12）；numpy：仅 B（2.5.2）；scipy/antlr4：**三站全缺**；R：**仅 B 且是 apt 4.3.3** | **G8 定案（"sympy 装两站 + R 走 CRAN 4.6.x"）实际未落地**，降级链当前不成立 |

**这条是本轮最有价值的发现**：G8 的「sympy 快筛」是 Auto_Prover 型任务卡降级链的第一层，
而它只在 B 站可用 ⇒ 卡派到 A/C 会缺依赖；`parse_latex` 依赖的 `antlr4` 三站全缺 ⇒ 注入点 D 的
恒等式预验证根本跑不起来；B 站 R 用的是 G8 明确排除的 apt 4.3.3 档。**三项均登记为未收口项**。

> 顺带踩到并修掉自己的一个判据缺陷：第一版探针用 `pip list | grep '^sympy$'` 判"装了没"，
> 而 `pip list` 输出是 `name  version` ⇒ **正则永不匹配**，三站被"测"成全都没装。
> 改用 `importlib.import_module` 直接 import 才算真判据。**"判据本身要先自证"**又一次成立。

### G8 收口（2026-09-16，用户拍板「三项缺口补装三站」）

| 动作 | 结果 | 备注 |
|---|---|---|
| pip 补装三站（sympy/numpy/scipy/antlr4-python3-runtime） | sympy 1.14.0 / numpy 2.5.3 / scipy 1.18.1 / **antlr4 4.11.0** 三站统一 | B 站原 1.12/2.5.2 已升级对齐 |
| R 补装三站（CRAN noble-cran40） | **R 4.6.1 (2026-06-24)** 三站统一 = 主控站版本 | 原 B 4.3.3 → 4.6.1 |
| 功能级验证 | 三站 `parse_latex(r'\frac{1}{2}')` → `simplify(x-1/2)==0` 全 True | 注入点 D 恒等式预验证可用 |

**两个值得留档的坑（都是"判据自证"的续集）**：

1. **antlr4 版本钉死 4.11**：sympy 1.14 的 `_parse_latex_antlr.py` 硬校验 `antlr4.__version__ == "4.11"`。
   第一版装了最新 4.13.2 —— `import antlr4` 成功、`pip show` 有版本，**看着全对**；
   直到功能级 `parse_latex()` 才抛 `ImportError ... version 4.11`。⇒ **"包在" ≠ "功能在"**，
   import 级验证只证明"装上了"，功能级验证才证明"能用"。

2. **CRAN 源签名两连坑**（三站同踩，静默降级）：
   ① cran40 仓库换过新签名 key（指纹 `E298A3A825C0D65DFD57CBB651716619E084DAB9`），旧 marutter key 不认 ⇒ `NO_PUBKEY`；
   ② 更隐蔽的是 `signed-by=` 指向的 keyring 必须是 **gpg 二进制格式**（`gpg --dearmor`），写成 ASCII armor
   ⇒ apt 报 `NO_PUBKEY 51716619E084DAB9` 后**静默回落默认源装了 4.3.3**，`apt install` 照样"成功"。
   **第一轮"安装成功"是假象，版本验证才暴露**（`R_new: 4.3.3`）。

**仍待办（不在本次范围）**：Cpp_Hub 基准对拍的 R 依赖包（forecast/rugarch/urca/ARDL/midasr/Spillover/vars，
G8 执行列 T0 预置）；对拍时的 `sessionInfo()` 版本对照流程。

---

## 附表：单项目依赖环境隔离选型（2026-09-16 增补）

**用户假设**：其它项目调用存在环境冲突，是否应用容器化/沙盒做单项目依赖隔离？

**结论**：**不采用容器化/沙盒做常规隔离**；采用**语言级**依赖隔离（Python → venv/uv，R → renv），容器仅作极端兜底
（详见 [Agent跨项目调用标准与迁移复用调研.md §9.10](../docs/Agent跨项目调用标准与迁移复用调研.md)）。四理由：

1. **威胁模型不同**：社区 2026 主流"agent 沙箱"防的是**不可信 agent 越权泄密**（Claude/Codex/OpenAI 的沙箱叙事），
   本项目的 agent 是自家 opencode/claude + 本地模型、跑在受控 cwd 下，非安全诉求——真痛点是**依赖冲突 + bit-exact 对拍漂移**。
2. **零自加载纪律**：venv/renv 激活即用、无 daemon；容器（尤其 docker/podman 需常驻 daemon）与之冲突。
   真到必须 OS 级隔离时首选**无 daemon 的 Apptainer**。
3. **agent 外壳资产**：容器化会切断 `~/.claude/skills/`、凭据、记忆层；依赖隔离只改 Python/R 解释器指向，资产不动。
4. **对拍精度**：项目 `lockfile`（uv.lock / renv.lock）比容器镜像更细粒度、更可靠、无二进制热同步。

**待办（防冲突，不阻断）**：已统一的三站全局 sympy/numpy/scipy/antlr4/R 4.6.1 视为**系统基础层**共享；
Cpp_Hub 对拍 R 依赖包（forecast 等 7 个）**装进项目 renv，不装全局库**，避免三站全局混装版本漂移。

### 能力增强：`providers` 增补「端点形态 + 记忆层」两维（2026-09-16，D3 路径①）

**触发**：用户问两事——①"是否有 :4000 网关还在"（§2.1 定版是"直连/过网关"混合，用户记得已全直连）；②"记忆协同层三站现状待查"，且要求**从统一管理入口查**。查证发现 `providers` 只给 provider 集合/凭据形态，**不回答端点端口**，也不覆盖记忆层 ⇒ 按 D3 给入口加能力，不新增脚本：

| 改动 | 落点 |
|---|---|
| opencode provider 端点形态 | `_ep_form()`：读 `options.baseURL`，`:4000` → 标"⚠ 网关(已退役, 应直连)"；`http://` → 缩略 host:port |
| claude baseURL 显示 | `_claude_forms` 早有 `base_url`，`cmd_providers` 补打印 + `:4000` 告警 |
| 记忆协同层 | PROBE 加 `### mem` 块（memory.db 大小 + MEMORY.md / summary 行数），`cmd_providers` 打印 `memory` 行 |

**实测结论（三站）**：①**三站零 `:4000`**——opencode 全 `127.0.0.1:8080`、claude 全 `http://127.0.0.1:8080`，**已全直连**（网关 09-13 退役），用户记忆正确；②**记忆层正主 = opencode 自带 memory**（`~/.local/share/opencode/memory.db` + `memories/MEMORY.md`），**非**文档 §2 记的 `codex-memory` 独立命令（三站 `command -v` 均未命中）——A 最饱满（MEMORY.md 88 行/summary 51 行），B/C 有库但 MEMORY.md 近空（内容在 `-wal` 待合并，B wal 4.1MB 表明有活跃会话在写）。三站同款机制，无跨站共享（站内记忆、站间靠任务卡交接）。**

**纪律沉淀**：①这类"是否还过网关/依赖版本/记忆库状态"的现状断言，**都应能由统一入口一键机械回答**，而不是靠人 grep；本次给 `providers` 补两维即为此。②"包/服务"与"功能/现状"是两回事——`command -v` 找不到 `codex-memory` 不等于记忆层缺失，真实现状要从数据文件本身看。

---

### 能力增强：`syntax` 增补「.ps1 BOM」维度（2026-09-21，D3 路径①）

**触发**：同族缺陷**第二次**咬人 —— 09-18 守门夹具 `_fm_golden_test.ps1` 缺 BOM ⇒ **静默失效**（`fns count=0`，"守门人一直是死的"）；09-21 编辑工具在某些重写路径下剥掉 BOM ⇒ `agent-cli.ps1` / `_fm_golden_test.ps1` / `_probe_fallback.ps1` 三个脚本 BOM 全丢，随后夹具**当场解析崩溃**（`Unexpected token 'claude'` + 中文乱码）。两次都**没有门禁拦住**。⚠ **该行为间歇**（当日实测：同一工具同一批文件，**有的编辑保留、有的剥掉**）⇒ 既不能假设保留、也不能假设必剥，**只能按字节验**。

**判据**：`.ps1` **含非 ASCII 字节 且 无 `EF BB BF`** ⇒ FAIL（读**字节**判，与内容无关 ⇒ 确定性）。纯 ASCII 的 `.ps1` 不受约束（无 BOM 也安全，不误报）。计数**单列** `.ps1-bom`，覆盖率与 `.ps1` 解析分开报。
**刻意不覆盖 `.sh`**：`.sh` **不得**加 BOM —— 内核会把 `#!` 认成 `\xEF\xBB\xBF#!` ⇒ `bad interpreter` 直接不可执行。（原 OPEN-ISSUES 待办写"`.ps1`/`.sh` 都要 BOM"是**错的**，已更正。）

**⚠ 根因更正（推翻当日早先的推断）**：曾以为"门禁的 `[Parser]::ParseFile` 走 .NET 解码（BOM-less 按 UTF-8）而执行走 ANSI ⇒ **结构性看不见**"。**实测推翻**：把 BOM-less 的中文 `.ps1` 交给那次 `ParseFile`，它**报了错**，且 11 条错的行号**全部落在中文注释行**（30/37/209/214/231/237/257/299）—— 若按 UTF-8 读则一条都不会有 ⇒ **两者走同一条解码路径（ANSI/GBK）**。
⇒ 真正的缺陷是**间歇性 + 内容相关 + 报错不指根因**：是否"吞掉换行"取决于具体字节序列 ⇒ 同一缺陷可能报一堆错（09-21），也可能**报 0 条错却照样跑坏**（09-18 目击的后者）；且报错行号指向注释行，看不出"加 BOM"这个修法。**这才是"必须另加字节判据"的理由**（而不是"ParseFile 看不见"）。

**顺带修两处报告缺陷**（同一函数内，同属"判据输出必须可信"）：
| 缺陷 | 症状 | 修法 |
|---|---|---|
| FAIL 行**无文件名** | 13 个 `.ps1` 里只报 `Unexpected token ')' [line 37]` ⇒ **查不到人** | `PS_SNIPPET` 的 FAIL 行补文件名；明细统一回**仓库相对路径** |
| 失败数**按错误行计** | 摘要写 `.ps1:13文件/11失败`，读起来像"11 个文件坏了"（实际 1 个）—— **09-21 实地误导过一次** | 改为**按文件**去重计数 ⇒ `13文件/1失败` |

**自证（正 / 负 / 对照）**：正 = 干净仓库 `.ps1:13文件/0失败 .ps1-bom:13文件/0失败` PASS；负 = 剥掉 `_fm_golden_test.ps1` 的 BOM ⇒ `(ps1-bom) …含非 ASCII 却无 UTF-8 BOM…` + `.ps1:13文件/1失败` + **退出 1 阻断**（还原后 sha256 逐字节一致）；对照 = 暂存一个**纯 ASCII 无 BOM** 的 `.ps1` ⇒ 两计数均 `0失败`（**无误报**）。
**门禁总数不变**（属 `syntax` 的**子判据**，非新增断言）；`docs/三机推理集群使用手册.md` 的 `syntax` 说明块已同步。

**纪律沉淀**：①**"能跑通"不是判据** —— 编辑工具改了文件却不动 BOM，`syntax` 在这个时点可能报 0 失败；凡编辑过 `.ps1`，提交前**按字节**验 BOM。②**判据的输出本身也要可信** —— 少个文件名、把"11 条错"写成"11 个文件"，都会让人**误判范围**（本次实地发生）。③**推断的根因要拿实测证伪** —— 本次"ParseFile 按 UTF-8 读"是一个**听起来合理但错**的机制假设，成本极低的一次注入实验就把它推翻了；写进文档前先证。

---

## 否决/比较对象

| 方案 | 结论 | 理由 |
|------|------|------|
| 只写"以后别新增脚本"的文档、不设门禁 | ❌ 否决 | 本仓库已多次证明只靠自觉会失效（空头承诺）；规则必须绑定执行 |
| 一次性删除全部 `_*.sh` | ❌ 否决 | 其中确实有**唯一实现**（golden 测试、评测冒烟、一次性迁移），盲删会丢能力 |
| 用目录约定（把一次性脚本移进 `archive/`） | ❌ 否决 | 仍是"攒着"，且新脚本照样会长；问题不是位置而是**没有断言** |
| 把 `tmp/` 也纳入门禁 | ❌ 否决 | `tmp/` 是**刻意的**临时区（已 gitignore）；门禁看 git 跟踪文件即可 |
| 允许"随手写、事后登记" | ⚠ 保留但收紧 | 登记仍在同一提交内完成 → review 可见；不登记即 FAIL |

---

## 后果与待办

**已完成（2026-09-15）**
- `inventory/ops.yaml`（entry 4 / station_runtime 22 / frozen 230，共 256 个脚本全登记）
- `ops/rpc_check.py` 新增断言 `scripts`（quick 项）→ 门禁现在会拦住新增脚本
- 本 ADR + 手册纪律 + 项目记忆（跨会话遵守）

**待办（按 D5 顺序，逐条做）**
1. 第一批清减候选 3 组（上表），每组：确认入口覆盖 → 删脚本 → 从 `inventory/ops.yaml` 移除
2. 其余冻结项分类（"仅存一次性调查"可直接删 / "能力仍需要"须先补入口）
3. 长期：`frozen_ops_scripts` 单调递减；`ops/` 目录逐步收敛到"入口 + 运行时件 + 真值表 + 文档"

**风险**
- 冻结清单可能掩盖"某脚本其实仍被引用"——缓解：删除前用 `grep` 反查引用（站上/systemd/其他脚本）
- 例外路径 ③ 可能被滥用成"照着登记就过"——缓解：`fix` 文案明确要求**在提交信息里说明理由**，且登记条目数可见

## 实施记录：第五批（provider 命名漂移修复 + 新模型入网，2026-09-16）

**触发**：用户「已定案的几个问题执行闭环」+「minimax-m2.7 / qwen3.8-flash-next 从 C 站传给其他两站并完成配置」。

**发现的阻塞性漂移（高严重度，命中"已闭环"项自身）**

- 三站 `~/.config/opencode/opencode.jsonc` 实况**只有 `local` + `openrouter`** 两个 provider，**无 `cluster-litellm`**。
- 但 `agent-cli.ps1` 的 ROUTE_TABLE / `_station_ready.sh` 注入 / `agent-cli-smoke.sh` / `a5-nextday-verify.sh` **仍全部使用旧名 `cluster-litellm/*`**。
- `_station_ready.sh` 第 [3] 步**强制要求** config 里存在 `cluster-litellm` 块（否则 `ERR_INJECT exit 11`），而它在派发路径上（[agent-cli.ps1:750](../ops/station-bin/agent-cli.ps1#L750)）⇒ **整条 `task` 派发门会失败**。
- **教训**：这是"资产名与配置实况脱钩"的典型 —— 三站 config 改名时引用方没跟上。**判据**：任何 provider/别名/端口改名，必须 grep **全仓引用方**（不只文档）。

**修法（用户裁定"资产跟随现状"）**

- 实测决定性证据：opencode **拒绝未在 `models` 中声明的模型名**（`local/nemotron` → UnknownError；`local/main` → 正常解析）。故单纯塌成 `local/main` 会丢"风味/站点"语义 ⇒ 改为**在 `local` 下声明多模型**，资产只做纯前缀替换 `cluster-litellm/` → `local/`。

| 落点 | 改动 |
|---|---|
| 三站 `opencode.jsonc` 的 `local.models` | 新增 `gpt-oss` / `gpt-oss-20b` / `nemotron` / `qwen` / `qwen3.8-flash-next`(ctx 262144) / `minimax-m2.7`（三站同改，一致哈希 `d765c81f…`） |
| `agent-cli.ps1` | ROUTE_TABLE 全部 → `local/*`；新增 `minimax{,-a,-b}` / `flash-next{,-a,-b}` 与 full-id 形态；`$TpBench` 键改名并补 minimax(21.5)/flash-next(19)（src 指 THROUGHPUT-BASELINE，无虚构）；slot-gate / split 判据 `-like 'local/*'`；review 默认模型 → `local/nemotron` |
| `_station_ready.sh` | 注入目标 `cluster-litellm` → `local`（含 regex 与错误文案） |
| `agent-cli-smoke.sh` | 模型 id → `local/*`；补 **C 站**节；补 **G10 负向用例**（位置参数形式**应仍挂死**；若"意外成功"= 上游行为变更信号） |
| `a5-nextday-verify.sh` | `cluster-litellm/nemotron` → `local/nemotron` |
| `cluster.py` | `STATION_ROUTES` 补 `qwen3.8-flash-next-{a,c}` / `minimax-m2.7-{a,b,c}`；`ROUTE` 补 `minimax-m2.7 → C`；注释更新 |

**新模型入网（minimax-m2.7 / qwen3.8-flash-next，UD-IQ4_XS）**

- 取证：A 站两个都缺；B、C 已有 ⇒ 实际只需 **C → A**（~202GB）。
- 传输：走 A↔C USB4 直连段（rtt 0.17ms），`rsync -a` 并行两路；**逐字节一致**（MiniMax 108,413,781,312 B；Qwen 93,682,584,224 B）。
- ⚠ **PowerShell 会剥掉 ssh 命令里的内层双引号**（`nohup bash -c "…"` 被拆成 `bash -c rsync …` → rsync 收到错参、静默不传）⇒ 改写成**无嵌套引用的独立后台命令** + `setsid nohup` + 日志。
- 三站建 conf（A/B/C 的 `minimax-m2.7.env` + A/C 的 `qwen3.8-flash-next.env`），结构取各站既有同族 conf；**CTX/N_CPU_MOE 为初值，待 load-mem-gate + 实测校准**（已在 conf 头与 models.yaml 注明）。
- `inventory/models.yaml` 登记两模型；**flash-next 保持默认路由 B（不静默改既有行为）**，minimax 默认 C（源站/基线站）。

**验证（端到端，非纸面）**

| 项 | 证据 |
|---|---|
| 门禁 | 全量 **13 绿 / 1 黄 / 0 红**（黄灯仍为既有 C 站 `claude.key` 占位串）；BOM 复核 ✓ |
| station-ready 注入 | B 站实跑：`STATION_READY port=50889` → `INJECT_OK` → `INJECT_VERIFY_OK`（引擎落在**随机端口 50889**，正是注入存在的理由；旧名在此必 dead `ERR_INJECT exit 11`） |
| `local/*` 解析 | `local/nemotron` → OK；**新声明的 `local/minimax-m2.7` → OK** |
| G10 负向 | 位置参数形式无输出（仍挂死，符合预期）；stdin 管道 → OK |
| **整链** | `agent-cli task paper --model nemotron` → `SLOT-GATE…local` → `ESTIMATE bench=…#L15 命中` → `LOCK_ACQUIRED` → `PIPE_STDIN_OK` → `TASK_RC=0` → `ACCEPT_OK=1` → **`TASK_DONE exit=0`** |
| 收尾 | 三站 `infer-unload` 全 OK（内存回收） |

**遗留（已登记）**

- `_agent-cli-bom.ps1` 是 `agent-cli.ps1` 的**整份过期副本**（仍含旧名）—— 属"并列入口"，建议按 D5 清理（先确认无人引用）。**✅ 已清减（2026-09-16）：取证更正为"O-26 串行基线测量的一次性工装"（被 root 级 `_o26_serial_loader.ps1` 引用），三站零引用后与引用者一并删除 + 同步 `inventory/ops.yaml`（冻结存量 181→180）；详见 [DEVELOPMENT-LOG 2026-09-16 ⑫](../spec/d6-agent-standard/DEVELOPMENT-LOG.md)。**
- `_btest.sh` / `_opencode_bump.py` / `_aic.sh` 等冻结脚本仍引用旧名，已成死代码，下一批清减候选。
- `load gpt-oss-120b-b` 在 B 站触发**前缀歧义**（`gpt-oss-120b` vs `-fable-5-distilled`）⇒ 按站路由名到站后仍走前缀匹配，重名族需更精确别名。记入待办。
- **`_station_ready.sh` 注入会持久改写 config**（把 `local.baseURL` 写成当次引擎端口）⇒ 任务跑完后该站 config 与其余站**不再一致**（本次 B 被写成 `:50889`），`stations` 门禁的"三站一致"断言随即转红（pre-push 拦下）。属**既有设计副作用**（注入本就写文件，非本批引入），本次因引擎落在**随机端口**才首次暴露。可选处置：注入改写到 `/run` 运行态覆盖层（与 TTL 的 `/run/llama-instances/<alias>.env` 同思路），config 保持基线不被污染。**本次按基线恢复**（以 A 站 config 覆盖 B 站），待办登记。

### 第五批补记（2026-09-16 晚：两项遗留调研 + 命名回归修正）

**① 命名回归（当日引入当日修）**：MiniMax 的站上规范别名是 **`m27-q4ks`**（`infer-load` 内有归一化 `sed 's/^minimax-m2.7.*/m27-q4ks/'`；`infer-list` 第一列即真值；`tests/b5q/*` 与 review judge 别名 `m27` 早已用它）。上一批误按模型技术名自建 `minimax-m2.7` ⇒ 实测 `infer-load minimax-m2.7` 直接 **`ERROR: 无匹配`**（回归），三站 `minimax-m2.7.env` 成**孤儿 conf**。已全链改正：conf 改名 `m27-q4ks.env`（3 站）／`cluster.py` ROUTE+STATION_ROUTES／`inventory/models.yaml` alias／`agent-cli.ps1` ROUTE_TABLE+bench+JUDGE_TABLE id／opencode `local.models` flavor。
**纪律**：**别名必须以 `infer-list` 的 alias 空间为准，不得照模型技术名自造**（已写入 `cluster.py` ROUTE 注释）。

**② `infer-load` 匹配逻辑两缺陷（已修）**：原实现只做 `grep "^${PREFIX}"` ⇒ (a) **传精确别名也判歧义**（`gpt-oss-120b` 同时是 `gpt-oss-120b-fable-5-distilled` 的前缀 ⇒ `load gpt-oss-120b-b` exit 1）；(b) **PREFIX 未转义**（`.` 是正则通配，`qwen3.8` 会命中 `qwen3x8`）。改为「候选集全列 → **精确相等者胜出** → 否则才走唯一前缀」，前缀比较用 shell `case` 按字面串。验证：`infer-load gpt-oss-120b` **不再报歧义**（正常进入加载）；`zzz-nomatch` 仍正确报无匹配；三站部署 + `bash -n` OK。

**③ 调研中新增两项缺口（登记，未处置）**：
- **claude 侧 baseURL 在随机端口下无人纠正**：`_station_ready.sh` 只改写 opencode 的 `local.baseURL`，而 claude 走 `~/.claude/settings.json` 的 `ANTHROPIC_BASE_URL`（写死 `:8080`）⇒ **unsloth 后端（引擎落随机端口）下 claude 路径实际不可用**（本轮 gpt-oss-20b 实测引擎在 50889）。这直接扩大了"端口不确定性"的受害面，是遗留 1 方案选型的关键输入。
- **G10 的"确定性"存疑**：`ocrun` 头注记其 2026-09-14 复测为「位置参数 4/4 成功、管道形式 2/4 成功 —— **未能复现该铁律**，两种形式都呈间歇性慢」，与本轮我观测到的「位置参数无输出」相反 ⇒ 我上一批写入 `agent-cli-smoke.sh` 的 G10 负向用例**可能是一颗 flaky 测试**，需重评（不可把间歇现象断言为确定行为）。

### 第五批补记二（C2 引擎面统一，2026-09-16 晚）

**决策 C2 落地**：引擎面统一为 **unsloth studio 的固定 `:8080`**，废弃"运行时改写 opencode config"的注入机制。

- **前置验证（`unsloth studio run --help`）**：`--host`/`--port`/`--path`/`--api-prefix`/`--reuse-port` 属 **managed flag 被拒收** ⇒ 内层 llama-server 端口**不可指定**（实测随机 40007/50031/50889）。**C1 不可行。**
- **C2 可行性实测**：`:8080` 是**三协议网关**（`/v1/chat/completions` 200、**`/v1/messages` Anthropic 200**、`/v1/responses`），`/v1/models` 自带 `context_length/max_context_length`，`/api/health` **免 key** 200；`/slots` 404。
- **slot-gate 数据源替换（选项 3 成功）**：`/api/inference/active-generations` 空载 `count=0` → 忙态 `count=1`（+`parallel_slots=4`）。**优于 `/slots`**（直接给并发生成数+上限，无需逐槽聚合）。映射 `count→SLOT_BUSY`、`parallel_slots→SLOT_TOTAL`、`QUEUE=0` ⇒ **输出契约不变，`Invoke-SlotGate` 与 wrapper 零改动**。

| 落点 | 改动 |
|---|---|
| `infer-load` | 加载后把 studio 重铸的 `UN_KEY` **落盘** `~/.config/rpc/unsloth.key`（端口固定后唯一需刷新的东西） |
| `_station_ready.sh` | **删注入 + 删端口发现**；端口固定 8080；就绪判据 `/v1/models`(带key) + `/props` + chat |
| `_slot_gate.sh` | 数据源 → `:8080 /api/inference/active-generations` |
| `agent-cli.ps1` | `:208` 断言 `INJECT_OK` → `CHAT_OK`；函数头注释同步 |
| `cluster.py` | **无需改**（读码后确认其 `curl :8080/health` + "任意 HTTP JSON 即在线/占用" 判据**本就**覆盖 unsloth 的 `{"detail":...}` 404 情形，2026-09-15 注释已说明） |

**验证（加载态端到端）**：`_station_ready` → `STATION_READY port=8080` + `CHAT_OK choices=1` + `READY_OK`(rc=0)；`_slot_gate 8080` → `SLOT_TOTAL=4 SLOT_BUSY=0`；wrapper 全链 `SLOT-GATE(idle,allow)` → `TASK_RC=0` → **`TASK_DONE exit=0`**；**claude `rc=0 / 36s / OK`**（此前因陈旧 key 必坏）；**config md5 全程恒定** `755975db…`（前后一致，注入不再发生）；三站 infer-unload OK。

**连带修好**：① **claude 路径** —— 其 `:8080` 目标一直正确，缺的只是 key（真因是 key 陈旧，非端口）；② **C 站 key 占位串**（22B `sk-local-noauth…`，即门禁那盏黄灯）由落盘机制自动纠正。

**过程中修掉两个我自己的判据缺陷（诚实记录）**：
- chat 就绪判据原看 `content` 非空 ⇒ **reasoning 模型在 max_tokens 小时把配额全给 thinking、无 content 字段** ⇒ 假失败。改看 `"choices"`。
- 原请求**缺 `Content-Type: application/json`** ⇒ curl 默认 form-urlencoded ⇒ 引擎 Pydantic 报 `body: Input should be a valid dictionary`。**且旧实现的 `CHAT_OK` 一直是假阳性**（匹配到的 `"content":""` 来自非正常响应）——即"能报 OK"不等于"真的 OK"。

---

## 关联

- [ADR-0001](ADR-0001-集群运维框架审计与四项改进决策.md)：集群运维框架审计 —— 同类的"脚本无总入口"问题
- [三机推理集群使用手册](../docs/三机推理集群使用手册.md)：入口用法（cluster.py 全子命令 / web 卡片清单）
- [方案 v2 §A.6](../docs/2026-09-15_统一管理入口_深入分析与优化方案_v2.md)：各能力逐项收进入口的落地记录
- `inventory/ops.yaml`：本 ADR 的登记表（断言 `scripts` 的事实依据）
- `tmp/`：一次性脚本的正确去处（`.gitignore` 覆盖）
