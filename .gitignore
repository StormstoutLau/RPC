# ── 机密 (绝不入库) ──
secrets/
litellm_config_20260901.yaml
*.key
*.pem

# ── 大文件 / 二进制 / 模型 ──
downloads/
*.tar.gz
*.gguf
*.bin

# ── 备份 / 临时 ──
# results-ledger.md.bak.* 等手动快照不入库, 历史交给 git
*.bak
*.bak.*
*.bak-*
.tmp*
# tmp/ 整体不入库 (调试/探针产物)。2026-09-14 收敛: 此前只逐项排除, 导致 git status
# 长期显示 "?? tmp/" 噪声; 已确认 tmp 下无任何已跟踪文件 (git ls-files tmp/ = 0)。
# 其中含 git bundle 备份 / 密钥替换规则 / 敏感清单 —— 一律不入库, 防意外推送含历史明文。
tmp/

# ── 系统垃圾 ──
Thumbs.db
Desktop.ini
# Python 字节码缓存 (2026-09-25 收敛): 原先只有 `ops/__pycache__/` 一条**按路径**的规则 ⇒
#   `tests/` 等其它目录的缓存不被覆盖, 每次跑独立测试脚本都冒出 `?? tests/__pycache__/` 噪声,
#   且 `secrets` 断言会报"另有 1 个未跟踪文件未计入"。改为**无前缀**规则: git 的 `__pycache__/`
#   匹配**任意层级**同名目录 ⇒ 一条覆盖全部, 故删掉那条按路径的旧规则 (冗余窄规则本身就是噪声)。
__pycache__/

# D2 运行时生成物
ops/cluster_status.html
ops/agent-dashboard.html
ops/.egress_daily.json
# 2026-09-21: OpenRouter 额度借用台账 —— 运行时状态(谁在借、借自谁、起于何日), 由
# `cluster.py egress borrow --go/--restore --go` 读写; 无密钥内容, 但属环境态 ⇒ 同 .egress_daily.json 不入库。
ops/.or-borrow.json
# ADR-0007 路A: 审计增量水印 —— 主控本地状态(单机单贡献者, 入仓换来的"跨机一致"当前无收益);
# 由显式命令 `cluster.py agent audit --accept` 推进, 门禁只读不改它。
# 2026-09-23 (D7-P1-1): `.audit-baseline.json` **已入仓**（移至 inventory/）——
#   决策依据见 docs/research/2026-09-18_证据流审计常跑…md §D-b 的升级条件（多机 ⇒ 入仓）。
#   故此处**不再忽略**它；留痕即 `git log inventory/audit-baseline.json`。
# (2026-09-25) 原先此处还有一条 `ops/__pycache__/` —— 已由上方"系统垃圾"段的通用 `__pycache__/` 覆盖, 故删除。

# agent-cli / 评测 运行日志与结果快照 (不入库)
ops/station-bin/*.log
ops/station-bin/*.log.err
ops/station-bin/agent-runs.log
ops/station-bin/result_*.jsonl
ops/station-bin/b6_smoke_*.result.jsonl

# agent 证据链 (2026-09-17, spec/d6-agent-standard/evidence-chain/)
#   · live 链(工作副本)不入库: 体积随 run 数线性长, 且**可由归档重算**(digest 确定性已验证) ⇒ 放 git 是噪声。
#   · ANCHOR.txt **必须入库**(刻意不排除): 它钉住链文件字节, 是唯一**不可重算**的外部锚 ——
#     提交并 push 到 origin 后, 改写链/证据须同时改写已推送的 git 历史才可能无痕。
#   · (P2, 2026-09-21) **冷镜像 `archive/evidence-chain/agent-chain.json` 入仓** —— 本体字节==锚的
#     chain/cold_sha256, 供应 off-machine 副本; 与锚**成对提交**, 由 verify 的 `p2_pair_mismatch` 断言兜。
ops/station-bin/agent-chain.json
