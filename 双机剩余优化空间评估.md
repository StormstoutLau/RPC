# 双机部署剩余优化空间评估
# （回应：是否只剩投机解码与换线两项）

> **日期**: 2026-08-28
> **问题**: 当前双机部署（A↔B USB4 RPC, MiniMax-M2.7 Q4_K_S, tg128 ≈ 20 t/s）的剩余优化空间，是否只剩投机解码和更换线材？
> **结论**: **否**。零成本参数层已基本穷尽，但剩余空间分布在四个方向（链路延迟税基 / 并行策略 / 协议根因 / 算术密度），且投机解码与换线恰恰不是预期收益最高的两项。
> **证据基础**: [metrics-log](spec/rpc-optimization/metrics-log.md) Phase 0-3 + A1/A2 试验; [互连方案调研](AMD395分布式推理高性能互连方案调研.md); [算子层 DESIGN](spec/operator-optimization/DESIGN.md); [RPC串行跨链调研](RPC串行跨链社区优化调研.md)

---

## 1. 已实证穷尽的层（无剩余空间）

| 层 | 做了什么 | 证据 |
|----|----------|------|
| 链路层 | pm_qos=100 + MTU 9000 + systemd 持久化 | RTT 0.619→0.100ms (-84%)，但吞吐零增益 → 链路非瓶颈 |
| 服务参数 | rpc-server 收敛 10.10.10.1:50052 + per-model 缓存 + 加载线程 | 二次加载 -43s，tg 无变化 |
| 张量放置 | A1 四配置（C0 基线 / C1 专家→A / C2 专家→B / C3 注意力→B） | 全负迁移：C1 -5.3%, C2 -33.7%, C3 -64.2% → TCP 下默认层切分已最优 |
| 稳定性 | systemd Restart=always + 崩溃传导演练 | A 站 6s 自愈，端到端 MTTR ~6min |

**根因锁定**（A2 采证）: 每 token ≈ 38.7 次 RPC 命令 / 903KB，带宽占用 17.6 MB/s = 链路的 **0.02%**。瓶颈是串行跨链的**延迟 × 命令数**，不是带宽。这决定了后续所有方案的筛选判据。

---

## 2. 剩余空间全景（按攻击点排序）

| # | 方向 | 攻击点 | 预期收益 | 成本/状态 |
|---|------|--------|----------|-----------|
| 1 | **A3a USB4 低延迟 sysctl 套件** | RTT 税基本身（38.7 次往返/token × RTT） | RTT 从 100µs 再往下压，收益线性传导到 tg | **零成本，未做**。源: ayysasha `99-usb4net-lowlatency.conf`（TCP 低延迟参数 + TB 保活 + CPU EPP=performance），逐项对照增量应用 |
| 2 | **A4 vLLM TP=2 平行部署** | 并行策略（层切分→张量并行），绕开 RPC 串行税 | 同款 M2.7 AWQ INT4 实测 18.5–24.8 t/s，上限略高于现 20 | 中成本。B 站平行部署不动 llama.cpp 服务。决策门: 单流 tg ≥ 24 t/s 且 16k 上下文无崩溃 |
| 3 | **A6 llama.cpp #26610 协议升级** | 根因（async graph_compute 消灭串行等待 + peer 直连聚合） | 同构社区实测 +32% tg | 事件驱动跟踪（Open，协议 6.0.0 无向后兼容），before/after 锚点已建 |
| 4 | **投机解码（ngram）** | 算术密度（每步验证多 token） | **不确定，有结构性风险**（见 §3.2） | v0.2.0 可测 ngram，但 llama-bench 无此参数，需 server+API 实测；DSpark 需升 master |
| 5 | **换 40G 认证线** | 带宽 | **性能 ≈ 0**（A2 实证利用率 0.02%，20G→40G 无感） | 真实价值 = 消除 retimer found/disconnected 振荡（**可靠性项**，非性能项） |
| 6 | **-dio / -np 卫生项 + Phase B 参数 sweep** | 加载 hang 避坑 / 聚合吞吐 / 长上下文 KV | 卫生级小收益（-dio 防加载 hang；KV q8_0 省显存换吞吐） | 零成本，未做（[DESIGN](spec/operator-optimization/DESIGN.md) Phase A 遗留项） |
| 7 | **ROCm 后端（prefill 端）** | pp 端 | visorcraft 七后端对比: ROCm prefill 占优；对长 prompt 代理工作流有感 | 中成本（Lemonade SDK gfx1151 栈）。注意 ROCm 与 Vulkan 的 tg/pp 权衡，须实测 |

---

## 3. 三个修正认知

### 3.1 换线不是速度项
A2 采证已把"换更粗的管子"证伪：带宽利用率 0.02% 意味着 40G 线对吞吐无感。换 0.8m 内 USB-IF 认证线解决的是 [USB4-40G评估](spec/rpc-optimization/USB4-40G链路能力评估.md) §4.2 发现的 **idle 振荡可靠性隐患**，应按可靠性预算排期，不占性能优化位。

### 3.2 投机解码在双机 RPC 上是未验证的赌博
单机场景下 draft-target 验证是纯算术密度增益；但分布式场景每步验证引入额外跨链同步，**方向上与已确认的瓶颈（命令数）相反**——若 ngram 验证每步增加 k 次 get/set_tensor，则串行税放大，收益可能被吃掉甚至倒挂。值得 server+API 实测（成本仅一次实验），但不是剩余空间里最稳的项，不应作为默认期待。

### 3.3 真正的上限在软件栈
20 t/s 的结构性根因（串行跨链）只有两条路径攻击：
- **现在就能试**: vLLM TP=2（A4，换并行策略 + RCCL/Gloo 集合通信）
- **等社区**: #26610（A6，协议层异步化）

对照证据: Geerling 4×M3 Ultra 同硬件同线，llama.cpp RPC 负加速（20.4→15.2）vs TP+RDMA 正加速（19.5→31.9）——**软件栈决定分布式是加速还是减速**。

---

## 4. 建议执行顺序

```
A3a sysctl 套件（本周零成本，压 RTT 税基，收益传导最确定）
  → A4 vLLM TP=2 平行试验（决策门: tg ≥ 24 t/s 且 16k 无崩溃）
  → ngram 投机解码 server+API 实测（一次性实验定性）
  → A6 #26610 触发后走升级 SOP（预期 +32%，事件驱动）
换 40G 线: 按可靠性需求单独排期，不计入性能优化序列
C 站容量档部署: 独立轨道（ROCm + TTM 120GB），与速度档并存
```

**一句话**: 参数层已到头，天花板在"延迟税基（A3a）→ 并行策略（A4）→ 协议（A6）"这条软件栈链上；投机解码待验证，换线归可靠性。

---

## 5. 与既有规划的关系

- 本评估不推翻 [算子层 DESIGN](spec/operator-optimization/DESIGN.md) 双路径策略（容量路径双机 RPC / 速度路径单机 UD-Q3_K_XL），但用户已明确**当前需求是分布式跑大模型**，单机速度路径降级为可选试验
- A3a/A4 行动项源自 [互连方案调研](AMD395分布式推理高性能互连方案调研.md) §4，本评估将其相对优先级固化
- A6/#26610 跟踪机制沿用 [RPC串行跨链调研](RPC串行跨链社区优化调研.md) A3 行动项
