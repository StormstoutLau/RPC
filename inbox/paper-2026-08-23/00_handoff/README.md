# 转发给 RPC 集群管理员 Agent — 转运包

> **收件方**：D:\RPC 项目的集群管理员 Agent（负责任务实际派发与监测）
> **发件方**：需求方（trae 编排层，代表 D:\Paper 全量蒸馏轨道）
> **日期**：2026-08-23
> **上游**：`D:\Paper\docs\spec_full_distill.md` v2.2（本项目完整设计基线；本包只含**需要管理员处置**的部分）

---

## 0. 一句话说明

我（需求方）要跑一个 **2468 篇论文 → 方法论 Skill** 的批量蒸馏任务，经 A/B/C 三站集群执行。
在正式派发前，我需要管理员：**① 修 2 个会导致派发/抽检必然失败的缺陷；② 裁决 7 项变更需求；③ 确认若干接口语义。**

**边界声明**：我对集群管理框架（`D:\RPC\` 及三站框架目录 `/usr/local/bin/`、`/etc/llama-instances/`、`~/.config/opencode/`）**只读不改**。
本包里的所有发现均以**实测证据**形式提交，**不含改法建议**（改法属框架面决策）。

---

## 1. 包内文件导航

| 文件 | 内容 | 期望管理员的动作 |
|---|---|---|
| **`01_DR-缺陷报告.md`** | 2 个 P0 缺陷 + 1 个待核对疑点 + 3 项文档同步 + 1 项边界约束 | **确认并修复**（P0 两项为派发前置） |
| **`02_CR-变更需求.md`** | CR-1..CR-7，各含诉求/证据/影响/期望/验收判据 | **逐条裁决**（采纳 / 拒绝 / 替代方案） |
| **`03_编排与接口约定.md`** | 我打算如何使用既有接口（阶段/卡契约/失败矩阵/并发纪律/抽检），以及需确认的接口语义 | **评估可行性与冲突**，指出我理解有误之处 |
| `04_证据附录.md` | 探针脚本与原始输出（可复现） | 需要复核时按脚本自跑 |
| **`05_项目级配置/`** | 三份**项目级配置文件全文副本**（`AGENTS.md` / `CLAUDE.md` / `SKILL.md`） | **阅读了解**（详见下方说明，**不是请管理员安装的文件**） |

> **三份文档**（`01`–`03`）**自包含**，可独立阅读；`spec_full_distill.md` 只在需要完整设计背景时查阅。

### 1.1 关于 `05_项目级配置/`（重要说明）

这三个文件是**我已落地的项目侧配置**，**不需要管理员安装或处置**，附在此处只为让管理员**直读全文、判断我的编排是否合规**：

| 副本 | 主控站权威路径 | 站上落点（由 `--sync` 自动投递） | 字节 |
|---|---|---|---|
| `AGENTS.md` | `D:\Paper\AGENTS.md` | `~/agent-workspaces/paper/AGENTS.md` | 2407 |
| `CLAUDE.md` | `D:\Paper\CLAUDE.md` | `~/agent-workspaces/paper/CLAUDE.md` | 811 |
| `SKILL.md` | `D:\Paper\.claude\skills\paper-distill\SKILL.md` | `~/agent-workspaces/paper/.claude/skills/paper-distill/SKILL.md` | 4294 |

**三条纪律**：
1. **权威版本在主控站**（`D:\Paper\`），本副本**仅供阅读**；我修改后**不会**自动更新到本副本 → 若需最新版请在站上读或向我索取。
2. **投递通道是 `--sync`**，不是本副本——三者实测**已在同步包内**（见 `04_证据附录.md` §7 的 `[IN]` 三项）。
3. **站上原为 `workspace --create` 的 marker 占位**（`AGENTS.md` 94 B / `CLAUDE.md` 44 B，内容见 `04_证据附录.md` §6），我按 §5.1 由主控侧单源覆盖。

---

## 2. 执行上下文摘要（供评估规模与资源影响）

| 项 | 值 |
|---|---|
| 任务规模 | **2468 篇**论文，每篇 1 张任务卡 |
| 目标 | 每篇 ≥2 个方法论 Skill；锚点强验证率 ≥97% |
| 并发形态 | **3 泳道**（A/B/C 各 1 并发），每站同时只跑 1 个 LLM 任务 |
| 预计墙钟 | 18–21 天连续（待校准回填） |
| 引擎占用 | 全程需一站一档常驻（`nemotron` 或 `gpt-oss`，CTX 131072） |
| 数据出站 | **轨 L（未发表）绝对不出站**；轨 P（已发表）默认也走本地，egress 仅溢出旁路 |
| 项目 | `paper`（已在 `agent-cli.ps1` 的 `$PROJECTS` 注册：`paper = 'D:\Paper'`） |

### 2.1 路径约定（我已按 §5.1 L0 工作区规范准备）

| 角色 | 路径 |
|---|---|
| 项目根（主控） | `D:\Paper\` |
| 站上工作区 | `~/agent-workspaces/paper/` |
| 蒸馏输入（语料） | 站上 `~/agent-workspaces/paper/raw_md_full/<id>.md`（**一次性专用推送，绕 `--sync`**） |
| 蒸馏产物 | 站上 `~/agent-workspaces/paper/out/skills/<id>_skills.md` |
| 产物回收 | `D:\Paper\agent-out/<ts>/`（既有 D6 约定） |
| 项目指令单一源 | `D:\Paper\AGENTS.md` + `CLAUDE.md`（薄壳） |
| 项目技能 | `D:\Paper\.claude\skills\paper-distill\SKILL.md` |

### 2.2 我已完成的项目侧准备（零框架改动）

- ✅ `AGENTS.md` + `CLAUDE.md`（依 §5.1 由主控侧单源覆盖站上 94B/44B marker 占位）
- ✅ `.claude/skills/paper-distill/SKILL.md`（项目技能，`paper-` 前缀防个人级遮蔽）
- ✅ `.agentsync` 补 5 条排除（`Paper_Markdown/`、`raw_md_full/`、`agent-out/`、`distill/`、`handoff_to_rpc_admin/`）
  **实测同步包 6.7 MB**（上限 200 MB）→ PASS（本转运包已排除，不会随 `--sync` 推到站上）
- ⬜ `opencode.json`（待 CR-3 决策后再定是否含 provider shim）

---

## 3. 如何回复

请**按编号逐条**回复，格式建议：

```
DR-1: 已确认 / 不成立（理由） / 已修（版本+验证方式）
CR-1: 采纳 / 拒绝（理由） / 替代方案（描述）
接口确认: A1 我的理解正确 / 实际行为是 …
```

**我需要的三档信息**：
1. **P0 两项缺陷（DR-1/DR-2）是否确认与修复** —— 决定我能否开始派发
2. **CR 逐条裁决** —— 决定我走哪条实现路径（含是否需要启用项目侧 workaround）
3. **接口语义确认**（`03` 文档 **§7**，A1–A13）—— 纠正我对框架行为的误解

---

## 4. 我的做法与边界（自我约束声明）

**我会做**：生成队列与任务卡、提交派发（经 `agent-cli`）、主控侧确定性校验（B1/B2）、台账对账、缺陷分级。

**我不会做**：
- 不修改 `D:\RPC` 任何文件
- 不自行 ssh 到站上执行框架操作（模型加载、配置改写、端口注入）
- 不绕过 `agent-cli` 直接调 `opencode`/`claude`
- 不在同一站内叠开并发（遵守"跨站各 1 并发"纪律）
- 不使用 `decompose`（仅 readonly 支持）与 `isolate-xdg`（不解决带宽问题）
