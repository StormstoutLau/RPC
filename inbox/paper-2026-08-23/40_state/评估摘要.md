# 受理评估摘要（paper-2026-08-23）

> 本文件保留 ADR-0008 落地**之前**的实质评估结论；正式状态见 `STATE.json` + `LOG.md`。
> 机器可读状态 = `STATE.json`；本文件为上下文说明，不参与门禁判定。

## 当前待办（待管理员推进；完成前 paper 保持 `waiting`）

> 待办聚合：见 [inbox/README §8](../../README.md)。状态变更须走 `STATE.json` + `LOG.md`。

1. **`10_admin/裁决报告.md`** —— 逐条 DR/CR/接口确认（DR-1/DR-2 代码侧已修、CR-2 已消解；CR-1 A/B、CR-5 宿主、CR-7 引擎常驻需需求方裁定）。**正式裁决报告 = 解除 `waiting` 的前置。**
2. `10_admin/受理决定.md`（轨 L/P + 并发约束）
3. `20_plan/`（派发计划 + 容量预估）若受理通过 → 交需求方复核（`plan-review`）

## 既有评估摘要（ADR-0008 前，前几轮会话已做）

- DR-1（ROUTE_TABLE id 失效）/ DR-2（JUDGE_TABLE['main'] 失效）：**代码侧已修**（统一 `local/*`，`main-opencode-cli`），转运包描述过时。
- CR-1（批量派发）：框架无批量循环入口，`decompose` 仅 readonly → 待裁 A/B。
- CR-2（判官资源）：`main` 判官落主控本地，与三站引擎解耦 → 已消解。
- CR-5（Phase 2 宿主）：`Paper_Markdown/` 不存在 → 硬前置待裁（主控 RTX4060 vs 395+SMB）。
- CR-7（引擎常驻）：零自加载 + TTL 默认关 → 待确认 `TTL_ENABLED=0` + 恢复责任人。

## 待补

- `10_admin/裁决报告.md`（逐条 DR/CR/接口确认）
- `10_admin/受理决定.md`（轨 L/P + 并发约束）
- `20_plan/`（派发计划）若受理通过