# 状态变更记录（<project>-<date>）

> **纪律**：状态只由管理员改 —— 覆盖写 `STATE.json`（state/updated_at/by），
> 并在本文件**追加**一条变更记录。需求方不改。
> 每次追加一条，不覆盖历史。理由溯源到 `10_admin/`（受理决定 / 复核意见-N）。

| 时间 (updated_at) | 新状态 | 理由 | by |
|---|---|---|---|
| `2026-09-23T00:00:00+08:00` | `open` | 示例：转债包已提交，待管理员初筛 | `admin` |