# 20_plan — 派发计划（<project>-<date>）

> **纪律**：`20_plan/` 成型后交需求方复核（状态 `plan-review`）；收到意见进入 `plan-revise`，
> 修订留版本（文件名带 `vN` 或本文件 LOG 记录），循环至定稿后才 `running`。

## 派发计划

- 任务卡清单 / 批次（逐卡引用）：
- 路由（站 / 模型 / 轨 L|P）：
- 证据回收（`agent-out/<ts>/` run.json + 证据束）：

## 容量预估（受理时填；业务判断，不强制）

> 参考 assign.cloud："priority ≠ workload"。批量任务受理时预估墙钟 / 站占用窗口。

- 预估墙钟：`<如: 2468 篇 × 20 天>`
- 站占用窗口 / 并发约束：
- 估算依据：
