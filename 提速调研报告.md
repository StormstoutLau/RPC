# A/B 双机分布式推理提速调研报告

> **日期**: 2026-08-27
> **作者**: Scott (鹏)
> **适用系统**: A 站 (scott-lau-NEX, Worker, 10.10.10.1) + B 站 (scott-lau-GTR-Pro, Master, 10.10.10.2)
> **硬件**: 2 × AMD Ryzen AI Max+ 395 (Strix Halo, 128GB LPDDR5X, Radeon 8060S gfx1151)
> **当前状态**: USB4 (thunderbolt0) RPC 分布式推理已跑通, llama.cpp 9859 (4fc4ec554), Vulkan/RADV 25.2.8
> **基线性能**: MiniMax-M2.7 Q4_K_S 双机 RPC, 生成 17.7 t/s, prompt 22.7 t/s, 加载 4.4 分钟

---

## 一、执行摘要

当前 17.7 t/s 的生成速度**不是硬件带宽瓶颈, 而是 llama.cpp RPC 协议开销与运行参数组合的瓶颈**。理论带宽上限约 80 t/s (详见第二节), 实测仅达 22%。

开源社区调研结论 (2026-08 时点):

1. **升级 llama.cpp 是最高 ROI 单项** — 当前 9859 (2026-07-02 构建) 落后上游 v0.2.0 约 1700 个 build, 期间合入 MTP 投机解码、Vulkan KV-cache 反量化 2x+、RPC 多线程加载 3x 等关键优化。
2. **保持 Vulkan 后端, 不要切 ROCm/HIP** — gfx1151 上 decode (生成) 密集负载 Vulkan 全面占优。
3. **llama.cpp RPC 仍是 2×Strix Halo 的最优分布式方案** — exo 不支持 AMD Linux, hipEngine 仅支持单模型; AMD 官方 playbook 即推荐 llama.cpp RPC。
4. **投机解码 (MTP) 是最大的单项加速杠杆** (1.7~2.2x), 但受模型权重限制, 且需规避 Vulkan + 并行 + MTP 的组合坑。
5. **两站系统层调优 (内核/sysctl/tuned) 有 +7~15% 免费收益**, 当前两站均未配置。

综合预期: Tier 1 参数与升级 → **25~30 t/s**; 叠加 MTP 模型 → **35~45 t/s**; 速度敏感任务改单机小激活模型 → **40~50 t/s**。

---

## 二、瓶颈定位: 为什么是 17.7 t/s

### 2.1 带宽理论账

MiniMax-M2.7 为 230B 总参数 / 10B 激活 MoE, Q4_K_S 量化:

- 每 token 读取字节 ≈ 激活参数 × 量化位宽 ≈ 10B × 0.56 byte/param ≈ **5.5 GB/token**
- RPC 按 tensor 切分后, 每站承担约一半: ~2.75 GB/token
- 每站统一内存带宽 ~230 GB/s (LPDDR5X-8000, 256-bit)
- 理论上限 ≈ 230 / 2.75 × 2 站并行 ≈ **~80 t/s**(理想切分、零开销假设下)

实测 17.7 t/s = 理论值的 22%。差距来源如下。

### 2.2 RPC 协议层损耗(结构性)

上游 issue [#22850](https://github.com/ggml-org/llama.cpp/issues/22850) 的深度剖析(2026-05, build b9033, 情形与当前高度相似):

| 缺陷 | 机制 | 影响 |
|:---|:---|:---|
| 同步请求-响应 | 每个 op 每层一次 RPC 往返, decode 阶段 batch=1 时纯延迟主导 | 实测 RPC 比本地 PCIe 慢 28%~55% |
| 元数据风暴 | 每次图执行重新深度遍历并序列化全部张量元数据(含固定大小 name 缓冲) | prefill 损失 >33% |
| 小张量冗余传输 | `HASH_THRESHOLD=10MB` 以下更新跳过 hash 直接裸传, KV/激活更新几乎全命中 | decode 期网络利用率极低(几十 MB/s 级) |
| 无数据压缩 | 传输无 LZ4 等压缩, 全裸传 | 带宽压力放大 |

**佐证**: 即便 2.5GbE (312MB/s) 也远慢于 PCIe v1 x1 (200MB/s), 证明瓶颈在协议不在带宽 — USB4 40Gbps 的物理带宽远未吃满。

### 2.3 运行参数损耗(可调)

| 参数 | 现状 | 问题 |
|:---|:---|:---|
| `--n-cpu-moe 8` | 8 个 MoE 专家/层跑 CPU | 每层引入 CPU-GPU 跨设备同步点; 双机切分后每站仅 ~65GB, 117GB GTT 装得下全部专家, CPU offload 无必要 |
| mmap 默认开启 | 日志有 `tensor overrides to CPU are used with mmap enabled` 警告 | 权重按需换页, 生成期出现页错误; 应 `--no-mmap` |
| KV cache F16 | 未量化 | 32K ctx × 4 槽占显存且拖慢注意力路径(新版本 Vulkan KV 优化吃不到红利) |
| `--parallel`(隐含 4 槽) | n_slots=4, 总 ctx 32K ÷ 4 | 单流交互场景被 4 槽稀释; 且 RPC 下多槽共享计算图, 调度开销叠加 |
| batch 512 / ubatch 默认 | -b 512 | decode 无影响, prefill 有少量空间 |

### 2.4 系统层状态(实测, 2026-08-27)

| 项 | A/B 两站现状 | 社区基线 |
|:---|:---|:---|
| 内核 | 6.17.0-23-generic | 7.x(生成内核有增量) |
| `vm.swappiness` | 60 | 1 |
| `vm.overcommit_memory` | 0 | 1 |
| `vm.dirty_ratio / background` | 20 / 10 | 40 / 10 |
| tuned | 未安装 | `accelerator-performance` profile |
| IOMMU | 默认(pt) | `amd_iommu=off`(社区实测更优) |
| TTM pages_limit | 30720000 (117GB) | 33554432 (128GB, 全量) |
| BIOS 功率 | 未确认 | 120W 档拉满(散热允许时) |

---

## 三、开源社区调研详情

### 3.1 llama.cpp 上游动态(关键!)

当前部署 9859 落后上游 v0.2.0(2026-08-25 起语义化版本, 对应 b 序号 ~10500+)。两个月内合入的关键改动:

| 改动 | 内容 | 对本集群的意义 |
|:---|:---|:---|
| **MTP 投机解码主线化**(PR #22673) | 模型内建多 token 预测头, 单 GGUF 无需 draft 模型 | decode **1.7~2.2x**(社区实测: Qwen3.6-27B 38→65 t/s); 需模型带 MTP/NextN 权重 |
| **Vulkan KV 反量化单次化**(b10517) | q8_0 KV 每 coopmat1 块反量化一次而非每线程 | KV 路径 **2x+**; 与 `--cache-type-k/v q8_0` 配合 |
| **RPC 多线程加载**(PR 26291) | `GGML_RPC_LOAD_THREADS` 环境变量并行分发 | 300GB 模型加载 4m54s → 1m38s (**3x**); 直接治 4.4 分钟冷启动 |
| Vulkan FA MMQ fp32 量化计算修正(v0.2.0) | FA 路径精度与性能修正 | 配合 `-fa on` |
| mimo2 MTP 支持(PR #26228)等架构扩展 | 各家 MoE 的 MTP 陆续支持 | MiniMax-M2 是否原生支持需验证(见 4.2) |

**已知坑** [#27544](https://github.com/ggml-org/llama.cpp/issues/27544)(2026-08-22, 未解决): **Vulkan 后端 + `--parallel > 1` + `--spec-draft-n-max > 1` 组合下性能塌缩**(AMD 特有, NVIDIA 不受影响)。规避: MTP 配单槽, 或 draft-n-max=1。

### 3.2 后端选择: Vulkan vs ROCm/HIP(gfx1151 专项)

多方独立基准一致结论:

| 来源 | 对比 | decode (tg) | prefill (pp) |
|:---|:---|:---|:---|
| [issue #24438](https://github.com/ggml-org/llama.cpp/issues/24438) | MoE Q8_0, llama-bench | Vulkan 49.5 vs HIP 33.7 (**Vulkan +47%**) | — |
| [nabe2030 基准](https://github.com/nabe2030/hip-vs-vulkan-evo-x2)(ROCm 7.2.2) | 6 模型交叉 | Vulkan +13~19% | HIP +42~48% |
| [h34v3nzc0dex 精度反转研究](https://github.com/h34v3nzc0dex/strix-halo-llm-finetune-guide) | Q4 vs BF16 | Q4: Vulkan +22%; BF16: ROCm +122% | — |
| [issue #21948](https://github.com/ggml-org/llama.cpp/issues/21948) | 大 MoE Q6_K | Vulkan tg 胜 | 补丁版 ROCm pp +18~32% |

**结论**: 量化模型(Q4) + 长生成负载 → **Vulkan 是唯一正确选择**(当前已是)。prefill 慢的代价由 API 服务的 prompt cache 部分摊薄。ROCm/HIP 仅在 BF16 全精度或纯 prefill 密集场景有意义, 且 ROCm 7.2.x 上 gfx1151 仍有 MoE matvec 带宽利用仅 40% 的未解问题。

### 3.3 分布式框架对比: RPC 仍是最优解

| 框架 | AMD Linux 支持 | 结论 |
|:---|:---|:---|
| **llama.cpp RPC**(当前) | ✅ 完整 | **保留**。[AMD 官方 playbook](https://developer.amd.com/playbooks/clustering-rpc-server/) 即教两台 Halo 用 RPC 跑 358B GLM-4.7; 协议缺陷是全局的但无更好替代 |
| [exo](https://github.com/exo-explore/exo) | ❌ | MLX/Apple 生态优先(RDMA over Thunderbolt 5 仅 Mac); [AMD Linux 实测失败记录](https://ai-manual.ru/article/eksperiment-s-exo-pochemu-ne-udalos-povtorit-gibridnyij-klaster-dgx-spark--mac-studio-i-rabochie-alternativyi/) |
| [hipEngine](https://github.com/shisa-ai/hipEngine) | ⚠️ 部分 | 纯 ROCm 自研引擎, decode 比 llama.cpp Vulkan +10%, prefill **>2x**; 但当前仅支持 Qwen3.6-35B-A3B 一个模型, 无 MTP, 无 RPC 多机 |
| vLLM | ❌ | GPU 服务器生态(Ray/NCCL), gfx1151 不支持 |
| rCUDA/FractOS 方案(学术) | ❌ | 研究原型, 批量化 RPC 后 decode 开销 12%, 但需深度改源码, 不可直接用 |

**架构判断**: 保持 llama.cpp RPC 做"双机大模型通道", 单机跑速度敏感任务(见 4.4)。

### 3.4 Strix Halo 系统调优基线(社区共识)

来自 [botAGI/strix-halo-multislot](https://github.com/botAGI/strix-halo-multislot)(同硬件 Beelink GTR9 Pro: 32 并发聚合 236 t/s, 30 分钟持续 226 t/s 无降频)与 [kryoz/llama-strix-halo](https://github.com/kryoz/llama-strix-halo):

- **内核参数组 reboot A/B 实测 +7~8% 生成速度 + 2x GTT 池**:
  `amd_iommu=off amdgpu.gttsize=126976 ttm.pages_limit=32505856 ttm.page_pool_size=32505856`
- kernel 6.17 → 7.0: 生成内核有增量(其 repo results/raw 有 one-shot 对比)
- `tuned-adm profile accelerator-performance` + sysctl 组: `vm.swappiness=1`, `vm.overcommit_memory=1`, `vm.vfs_cache_pressure=50`, `vm.dirty_ratio=40`, `vm.dirty_background_ratio=10`, memlock unlimited
- 多槽并发能显著摊薄单流延迟(RPC 下尤其) — 32 并发时聚合吞吐远超单流 × 32
- BIOS: iGPU Memory 512MB(已达标), 功率档拉满(B 站 GTR-Pro 散热占优, A 站按机型确认)

### 3.5 投机解码实测参考(同硬件)

| 场景 | 加速比 | 来源 |
|:---|:---|:---|
| Strix Halo + 模型内建 MTP(Qwen3.6 UD-Q4_K_M) | 单流 ~90 t/s(基线约 60) | strix-halo-multislot |
| MTP 合并主线, Qwen3.6-27B Q4(RTX 3090) | 38.9 → 65 t/s (**1.71x**) | [banandre 基准](https://www.banandre.com/blog/multi-token-prediction-lands-in-llamacpp-nearly-2x-faster-generation-but-prompt-processing-is-paying-the-price) |
| MTP draft-n-max 5, 22GB 2080 Ti | 23 → 47 t/s (**2x**) | 社区实测 |
| 外部 draft 模型(Gemma4 E2B→31B, Strix Halo) | ~1.3-1.5x(带宽受限平台低于离散 GPU) | [sleepingrobots 基准](https://sleepingrobots.com/dreams/speculative-decoding-gemma4-strix-halo/) |

**注意**: MTP 的 prefill 代价 — PP 从 1100 → 550 t/s(RTX 3090 实测), 有效 ctx 缩水(150K→110K)。对"长系统提示 + 短生成"的 agent 负载是负优化, 对"长生成"负载是纯正收益。MiniMax-M2.7 这类思考模型生成长, 收益面大。

---

## 四、行动方案(按 ROI 排序)

### Tier 1 — 零成本参数/升级(预期 17.7 → 25~30 t/s)

| # | 措施 | 说明 | 预期 |
|:---|:---|:---|:---|
| 1 | **两站同步升级 llama.cpp 至 v0.2.0+** | 必须两站 MD5 一致(RPC ABI); 同参数 `-DGGML_VULKAN=1 -DGGML_RPC=ON`; 编译产物覆盖 /opt/llama.cpp 前备份 | 协议修复 + 内核优化 + 加载 3x |
| 2 | **试 `--n-cpu-moe 0`** | 双机切分后每站 ~65GB < 117GB GTT, 全专家上 GPU; OOM 则逐步回调 4/8 | 消除每层 CPU 同步 |
| 3 | **`--no-mmap` + `--cache-type-k q8_0 --cache-type-v q8_0`** | 消除 mmap 警告; KV 减半; 吃 b10517 Vulkan KV 优化红利 | KV 路径 2x 级 |
| 4 | **单流场景 `--parallel 1 -c 32768`** | 交互/API 单用户时改单槽; 并发需求时再开 4 槽 | 单流不被稀释 |
| 5 | 启动时 `GGML_RPC_LOAD_THREADS=8` | 新版 RPC 并行加载 | 加载 4.4min → ~1.5min |

### Tier 2 — 模型层(预期 1.7~3x, 视模型选择)

| # | 措施 | 说明 | 预期 |
|:---|:---|:---|:---|
| 6 | **切 DeepSeek-V4-Flash-0731 + MTP** | B 站已有 MXFP4 分片(40GB 级); llama-gguf-split 合并后 `--spec-type draft-mtp --spec-draft-n-max 2` **+ `--parallel 1`**(规避 #27544) | decode 1.7~2.2x |
| 7 | **验证 MiniMax-M2.7 是否有 MTP/NextN 权重版 GGUF** | 社区有 uto7/llmfan46 等变体; 若有, 不换模型直接吃加速 | 同上 |
| 8 | **速度敏感任务改单机小激活模型** | gpt-oss-120b Q5(80GB, 5.1B 激活, B 站独跑, 免 RPC 税 30~55%) 或 Qwen3-Coder-Next Q6(65GB, A3B); 双机通道专职高质量长任务 | 40~50 t/s 档 |

### Tier 3 — 系统层(一次到位, +7~15%)

| # | 措施 | 说明 |
|:---|:---|:---|
| 9 | 两站内核参数 | `amd_iommu=off amdgpu.gttsize=126976 ttm.pages_limit=32505856 ttm.page_pool_size=32505856`, update-grub 重启 |
| 10 | 内核升级 7.x | mainline PPA; A/B 前后跑 llama-bench 对比 |
| 11 | tuned + sysctl + memlock | `tuned-adm profile accelerator-performance`; swappiness=1, overcommit=1, dirty_ratio=40/10, memlock unlimited |
| 12 | BIOS 功率档 | 两站拉满(注意 A 站散热); 记录温度监控确认无降频 |

### 不建议方向

- **切 ROCm/HIP**: decode 反而慢 13~47%, gfx1151 MoE matvec 带宽利用问题未解
- **换 exo**: AMD Linux 不可用
- **等待 RPC 协议重写**: issue #22850 属结构性债务, 短期无 PR 迹象
- **BF16 全精度**: Vulkan BF16 decode 比 ROCm 慢一倍, 且 130GB 单模型无分布式空间
- **借用 LM Studio 后端跑 RPC**: 见第七节专项分析 — 官方构建不含 GGML_RPC, 不可行

---

## 五、后端 Vulkan llama 版本控制方案(2026-08-27 补充)

### 5.1 现状诊断(实测)

| 项 | 现状 | 问题 |
|:---|:---|:---|
| 目录性质 | `/opt/llama.cpp` 仅二进制 + `.so` + 空 `build/`, 无源码、无 CMakeCache | 零可追溯性: 构建参数、源码来源、构建环境均未知 |
| 版本记录 | 只能靠 `--version`(9859/4fc4ec554)和对话里的 MD5 | 未持久化, 机器上无任何清单 |
| 工具链错位 | 现有二进制是 GNU 11.4 构建, 系统现装 gcc 13.3 | 原构建环境不明 — 证明"两站各自编译"路线不可控 |
| 两站一致性 | 目前 MD5 一致 | 靠历史部署事实维系, 无机制保障 |
| 网络 | B 站 GitHub 直连可达; A 站 git 协议不可达但 **HTTPS 可达**(实测 github.com / codeload 均 200) | B 站做唯一构建源; A 站 tarball 直下作应急后备 |
| 脚本路径 | start_rpc.sh / run_inference.sh / run_server.sh 全部硬编码 `/opt/llama.cpp/` | 好消息: symlink 方案零脚本改动 |
| 磁盘 | 两站各 ~900G 可用 | 多版本目录共存无压力 |

### 5.2 核心约束

1. **RPC 要求两站原子化同步**: ggml-rpc 协议 + tensor 布局无 ABI 稳定承诺, b 序号之间都可能变协议 — 升级必须两站视为一个整体, 同时切换
2. **`/opt/llama.cpp` 路径必须稳定**: 三个脚本都引用它
3. **so 内嵌版本号**(`libllama-common.so.0.0.9859`): 新旧版本混在同一目录会链接错乱 — 必须目录级隔离

### 5.3 推荐架构: B 站单点构建 + 版本目录 + symlink 原子切换

```
B 站(构建源)                     A/B 两站(运行端)
~/src/llama.cpp      git 仓库     /opt/llama.cpp-<ver>/   版本化目录
~/build/llama-<ver>/ 外部构建     │  bin + .so + MANIFEST
~/dist/llama-<ver>.tar.gz ──scp─→ /opt/llama.cpp → symlink 指向当前版本
                                 (脚本路径不变, 回滚 = 改链)
```

manifest 设计(每个版本目录内置):

```ini
# /opt/llama.cpp-<ver>/MANIFEST
commit      = v0.2.0 / b10517 等标签
build_host  = GTR-Pro (B站)
build_date  = YYYY-MM-DD
cmake_flags = -DGGML_VULKAN=1 -DGGML_RPC=ON
toolchain   = gcc <ver>, cmake <ver>, mesa-vulkan <ver>
md5:
  126494d9...  llama-cli
  a5664d8f...  libggml-vulkan.so
  362a6987...  libggml-rpc.so
  ...          llama-server / ggml-rpc-server / libggml-cpu-zen4.so
```

### 5.4 升级 SOP(两站原子切换)

```
1. 停服务:   B 站 pkill llama-server(A 站 rpc-server 可留)
2. B 站构建:  git fetch → checkout <tag> → cmake → make →
             安装到 /opt/llama.cpp-<new>/(不动 symlink)→ 写 MANIFEST → 打 tar
3. 分发:     scp tar → A 站解压 → md5sum -c 对比 MANIFEST
4. 原子切换:  A 站 ln -sfn → 重启 rpc-server →
             B 站 ln -sfn → 启动 llama-server → 冒烟测试(PONG)
5. 回滚:     两站各一条 ln -sfn 回旧目录 → 重启(分钟级)
6. 保留最近 2 个版本目录, 更旧的删除
```

日常防线: 主控站巡检脚本 ssh 两站读 MANIFEST + md5sum 比对, 不一致即告警(也可塞进 start_rpc.sh 做启动预检)。

**第 0 步(零风险先行)**: 把现有 9859 现状版本化 — `mv /opt/llama.cpp /opt/llama.cpp-9859` + 建 symlink + 补写 MANIFEST(MD5 已实测在案)。立刻获得回滚基线, 也是后续升级 v0.2.0 的退路。

**协议版本核对**: 升级后看 A 站 rpc 日志的 `Starting RPC server vX.Y.Z`(当前 v4.0.1), 作为兼容性第一信号。

---

## 六、LM Studio 后端借用分析(2026-08-27 实测)

### 6.1 两站 LM Studio 后端盘点

两站 `~/.lmstudio/extensions/backends/` 下的 vulkan 后端:

| 后端 | A 站 | B 站 |
|:---|:---|:---|
| vulkan-avx2 | 2.24.0 / 2.25.2 / 2.27.1 / 2.28.2 / **2.29.1** | 2.24.0 / 2.25.2 / 2.27.1 / 2.28.2 / **2.29.1** |

最新 **vulkan-avx2-2.29.1**(2026-08-22 下载)两站 MD5 完全一致(`llama-server` = `7e15e20...`, `libggml-vulkan.so` = `08493ed...`) — LM Studio 自动更新机制天然保持两站二进制同步。

### 6.2 结论: 不能借用于 RPC 分布式推理

| 检查项 | 实测结果 | 含义 |
|:---|:---|:---|
| **`libggml-rpc.so`** | ❌ 不存在 | **致命**。LM Studio 官方构建不含 GGML_RPC 后端, A 站无法当 Worker |
| **`--rpc` 参数** | `llama-server --help` 无此参数 | Master 端也无法发起 RPC |
| **独立 `rpc-server` 二进制** | ❌ 无 | 无法在 Worker 端启动服务 |
| **版本体系** | `version: 1 (dd1ea52)`, GNU 12.3.0 | 是 LM Studio 的 **fork** 而非上游: 版本号与上游 b 序号无法对应; 含自研引擎协议(`libllm_engine.so` + Node 绑定 `liblmstudio_bindings_vulkan.node`); 工具链被裁剪(无 llama-cli / llama-bench / llama-gguf-split) |
| **CPU 后端形态** | 单一 `libggml-cpu.so`(非上游多变体自动 dispatch) | 确认是不同构建体系 |

产品定位使然: LM Studio 面向单机桌面用户, 官方构建从未编入 RPC。

### 6.3 衍生发现

1. **A 站 GitHub HTTPS 实际可达**(实测 `github.com: 200`, `codeload tarball: 200`) — 之前 `git ls-remote` 无输出属误判。给版本控制方案增加应急后备: A 站可直接 curl 下载源码 tarball 自行构建(主方案仍是 B 站单点构建, 规避编译环境漂移)。
2. **A 站 `~/Applications/llama-gfx1151/`** 是一个 **b1292 ROCm+RPC 完整构建**(含 `rpc-server`, `libggml-rpc.so`, 全套 ROCm 7.14 运行时, 2026-06) — 历史遗留的 ROCm RPC 链路。版本太旧且 Vulkan decode 更快, 不建议启用, 但可作为"ROCm 后备"参考。
3. **LM Studio 2.29.1 的正确用途**: **单机快速通道的免费增量** — 代码新(8月下旬, 含近期上游 Vulkan 优化), B/A 站跑单机任务(如 gpt-oss-120b)时经 LM Studio 的 localhost:1234 使用, 与 RPC 链路互不干扰。

---

## 七、实施路径建议

```
第 1 步: 基准固化
  两站跑 llama-bench(MiniMax-M2.7, pp512/tg128), 存档作为升级前基线
第 2 步: Tier 1.1 升级(两站同步)
  备份 /opt/llama.cpp → 编译 v0.2.0 → MD5 校验一致 → 重跑 llama-bench 对比
第 3 步: Tier 1.2-1.5 参数试验(改 inference.conf 即可)
  --n-cpu-moe 0 → --no-mmap + KV q8_0 → --parallel 1, 每步 llama-bench 验证
第 4 步: Tier 2 模型试验
  DeepSeek-V4-Flash 合并 + MTP(单槽); 或找 MiniMax MTP 权重版
第 5 步: Tier 3 系统调优(维护窗口)
  GRUB/sysctl/tuned, reboot 前后对比
第 6 步: 汇总所有数据, 更新 分布式推理.md
```

每步都留 llama-bench 数据, 单变量推进 — 与"形式先行 → 数值证伪"的方法论一致: 先固化基线, 再逐项证伪各提速假设。

---

## 八、参考来源

| 主题 | 来源 |
|:---|:---|
| RPC 性能剖析 | [llama.cpp issue #22850](https://github.com/ggml-org/llama.cpp/issues/22850) |
| 上游版本与 changelog | [llama.cpp v0.2.0](https://github.com/ggml-org/llama.cpp) / [b10517 Vulkan KV 优化](https://theneuralfeed.com/article/b10517/c4lpuMuC) |
| RPC 加载 3x | [PR 26291 报道](https://baguaai.com/breaking-the-distributed-inference-bottleneck-llama-cpp-rpc-loading-speedup-by-300/) |
| Vulkan vs HIP | [issue #24438](https://github.com/ggml-org/llama.cpp/issues/24438) / [nabe2030/hip-vs-vulkan-evo-x2](https://github.com/nabe2030/hip-vs-vulkan-evo-x2) / [精度反转研究](https://github.com/h34v3nzc0dex/strix-halo-llm-finetune-guide) |
| Vulkan MoE prefill 瓶颈 | [issue #21948](https://github.com/ggml-org/llama.cpp/issues/21948) |
| MTP 性能与坑 | [banandre MTP 基准](https://www.banandre.com/blog/multi-token-prediction-lands-in-llamacpp-nearly-2x-faster-generation-but-prompt-processing-is-paying-the-price) / [issue #27544](https://github.com/ggml-org/llama.cpp/issues/27544) / [Strix Halo 投机解码](https://sleepingrobots.com/dreams/speculative-decoding-gemma4-strix-halo/) |
| 分布式框架 | [exo](https://github.com/exo-explore/exo) / [exo 失败记录](https://ai-manual.ru/article/eksperiment-s-exo-pochemu-ne-udalos-povtorit-gibridnyij-klaster-dgx-spark--mac-studio-i-rabochie-alternativyi/) / [hipEngine](https://github.com/shisa-ai/hipEngine) |
| AMD 官方 | [Clustering Two Ryzen AI Halos with RPC](https://developer.amd.com/playbooks/clustering-rpc-server/) / [ROCm Strix Halo 优化](https://rocm.docs.amd.com/en/docs-7.2.0/how-to/system-optimization/strixhalo.html) |
| 系统调优 | [botAGI/strix-halo-multislot](https://github.com/botAGI/strix-halo-multislot) / [kryoz/llama-strix-halo](https://github.com/kryoz/llama-strix-halo) / [amd-strix-halo-toolboxes](https://github.com/kyuz0/amd-strix-halo-toolboxes) |
| 版本控制与 LM Studio 分析 | 本地实测(2026-08-27): 两站 MD5 比对 / LM Studio 后端目录勘察 / A 站 HTTPS 连通性测试 |

---

**报告版本**: v1.1(新增第五节版本控制方案、第六节 LM Studio 借用分析)
**下一步**: 按第七节路径实施, 基准数据回填本报告; 第 0 步 = 现有 9859 版本化(零风险)
