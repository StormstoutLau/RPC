D6 blind scan BS-1..6 reproduction backfill + backup/restart runbook

- BLINDSCAN v1.1 finalized: backfill section 8 reproduction records
  - BS-1 opencode.db WAL+busy_timeout write-lock serialization confirmed but non-critical (concurrent 5.2ms vs serial 3.0ms, queued not deadlocked)
  - BS-2 LiteLLM openai/ dialect has no explicit parallel_tool_calls config; free-tier 3-sibling subagent test timed out (silent failure); /v1/models returns Internal error
  - BS-3 slot0-stuck #20906 not reproduced on either A/B under concurrent long prefill; judged probabilistic, cannot assume immunity
  - BS-6 unified-memory bandwidth: concurrent 4x ~ serial (3431 vs 3320ms), same-node parallel capped by slot/bandwidth, favor cross-node fan-out
  - BS-4/5 design-level, decisions unchanged
- Add REPRO-RUNBOOK.md backup/restart contingency (pre-op backup ironclad + key re-mint rewrite + rollback)
- Add A/B station repro + health scripts (_apre/_apost/_ahealth/_bs1/_bs2*/_bs6/_bsprobe)